// Type declarations for scripts/electron-flags.mjs.
// Hand-written because the source is plain JS (the launchers consume it
// before any TS compile step).

export const WINDOWS_SAFE_GPU_FLAGS: readonly string[];
export const SANDBOX_BYPASS_FLAG: string;
