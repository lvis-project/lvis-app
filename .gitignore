node_modules/
package-lock.json
dist/
release/
release-fast/
.cache/
*.log
.omx/
.pageindex-workspace/
.pageindex-workspace-test/
.DS_Store
.omc/

# Phase 1 인덱서 격상 작업 중 생기는 test artifacts
.meeting-sessions/

# LVIS 런타임 디렉터리 (사용자별)
.lvis/
.email-token.json

# Resources 번들 대상(개발 시 fetch-uv.mjs가 다운로드)
resources/uv/
resources/uv-runtime/
resources/uv-archives/

# Playwright UI E2E artifacts
test-results/
playwright-report/
coverage/

# Storybook build output
storybook-static/

# Dynamically installed plugins (runtime-only, not committed)
packages/hello-world/

.claude/worktrees/
.claude/scheduled_tasks.lock

# Dev runtime: plugin manifests written by scripts/run-electron-dev.mjs into the user-data dir.
.lvis-dev/

# Legacy bun binary lockfile — repo uses text bun.lock
bun.lockb

# Demo credentials — gitignored so API keys are never committed.
# Copy .env.demo.example to .env.demo and fill in the real values.
# scripts/run-electron.mjs auto-loads this file on `bun run start`.
.env.demo
.env.demo.local

# Generated by `bun run build:icons` every build. Keep the source SVG tracked,
# but ignore regenerated PNG assets so build output does not dirty worktrees.
build/icon.png
build/tray-icon.png
build/tray-icon@2x.png
build/tray-iconTemplate.png
build/tray-iconTemplate@2x.png
