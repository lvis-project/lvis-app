# Synthetic raster fixtures

These images contain an 8 by 4 generated colour grid, with no external or private
source content. The PNG and WebP fixtures include transparency. The JPEG with
orientation metadata declares orientation 6. The animated GIF contains two
frames. Tests decode these files rather than treating format headers as images.
