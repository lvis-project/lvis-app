# Single-authority consolidation survey — interim

Survey stopped early at the owner's request; this is what had been
adversarially verified at that point. NOT a complete sweep — the
loop had not gone dry, so absence from this list is not evidence.

Candidates raised: 103 · verified: 93 · refuted: 29 · judged a legitimate boundary: 21 · confirmed mergeable: 43

Ordered security/permission-relevant first. Each survived an
adversarial pass whose default was to refute.


## approval-gate-optional-ref  *(permission-relevant)*

**Capability** — The single ApprovalGate instance — one object, constructed before the plugin runtime so nothing about it is actually late.

**Wirings** — By-value everywhere: src/boot/steps/conversation-wiring.ts:314 (main loop), :481 (SubAgentRunner parentDeps), :519 (WorkBoardEngine), src/boot/steps/plugin-tool-executor.ts:114 (plugin-surface executor), src/boot/steps/plugin-runtime.ts:199 (required, non-optional, into the plugin runtime). By optional-ref for exactly one consumer: src/boot/steps/workflow-stores.ts:73 (`approvalGateRef = { fn: approvalGate }`) → :82 (`getApprovalGate: () => approvalGateRef.fn`) → src/boot/tools.ts:186, :262, :269 → src/tools/skill-load.ts:95.

**Observed drift** — One side carries a branch the other structurally cannot reach. src/boot/steps/workflow-stores.ts:69-73 admits the ref is bound at construction ("gate is constructed up-front … so this is bound immediately"), and src/boot/steps/plugin-runtime.ts:196-199 documents that the gate is REQUIRED precisely so boot-order inversion is impossible. Yet the `| undefined` typing forces `skill_load` to model and return an unreachable failure — src/tools/skill-load.ts:196-204, `"skill_load approval gate unavailable"` — while every by-value consumer has no such state. The `Ref.fn` indirection buys nothing here: the reacher could simply BE handed the gate, as the other five wirings are.

**Verifier** — CONFIRMED — one authority, six wirings, one of which is a vestigial late-binding seam.

ONE THING (verified): exactly one production `new ApprovalGate` exists — src/boot/conversation.ts:152, via `createApprovalGate` called once at src/boot.ts:203 and stored at src/boot.ts:208 (`ctx.approvalGate = approvalGate`). Every other `new ApprovalGate` in the tree is under `__tests__/`. `ctx.approvalGate` is typed non-optional (src/boot/context.ts:122) and is assigned exactly once. All six wirings therefore hold the SAME object: conversation-wiring.ts:230 (routine loop deps), :314 (main loop), :363 (side chat), :480 (SubAgentRunner parentDeps), :519 (WorkBoardEngine), plugin-tool-executor.ts:114, plugin-runtime.ts:199 — plus the ref at workflow-stores.ts:73.

ORIGIN IS VESTIGIAL (decisive): `git show 46abd5f9 -- src/boot.ts` shows the ref introduced as a GENUINE late-binding — line 353 `let approvalGateRef = { fn: undefined }`, line 361 `getApprovalGate: () => approvalGateRef.fn`, line 404 `approvalGateRef.fn = approvalGate` (assigned LATER). Gate construction has since moved ahead of this point, so the ref is now initialized eagerly (`{ fn: approvalGate }`, workflow-stores.ts:73) and never written again. The seam outlived its justification.

REFUTATIONS ATTEMPTED AND FAILED:
1. Boot-order — refuted four ways: (a) src/boot.ts:203-208 assigns before setupWorkflowStores at src/boot.ts:520, same sequential async fn; (b) src/boot/steps/plugin-runtime.ts:195-197 — gate is "Required (not optional) so that boot sequence inversion is impossible"; (c) src/boot.ts:511-514 enumerates the late bindings as "(subAgentRunner, askUserQuestionGate)" — approvalGate is deliberately NOT in that list; (d) workflow-stores.ts:69-73 says so itself.
2. Lifecycle/window-recreation (strongest available defense) — ApprovalGate captures `private readonly webContents` (approval-gate.ts:850, :896) from mainWindow.webContents (boot/conversation.ts:153), UNLIKE AskUserQuestionGate which uses a lazy resolver precisely because "dev-mode reloads destroy the captured webContents" (workflow-stores.ts:61-64). So one could argue the ref exists to allow swapping the gate after window recreation. REFUTED: nothing performs such a swap (`approvalGateRef` occurs in exactly two production lines, workflow-stores.ts:73 and :82 — no writes; `ctx.approvalGate =` occurs once, boot.ts:208), and the ref structurally CANNOT serve that purpose because the other five wirings hold the instance by value and would silently retain the stale gate. A seam that can only half-swap is not a lifecycle boundary.
3. Test ergonomics — WorkflowToolDeps is all-optional so minimal test registries stay green (boot/tools.ts:216-217), but a by-value `approvalGate?: ApprovalGate` preserves that exactly; the registration guard at boot/tools.ts:257-263 already keys on field presence, not on the thunk's return.
4. Trust/process — refuted: all six are main-process, same instance, and workflow-stores.ts:70-72 states the reuse is intentional ("skill_load reuses the same gate the executor uses").

DRIFT EVIDENCE (actual, two kinds):
(a) Divergent branch: SkillLoadToolDeps.getApprovalGate returns `ApprovalGate | undefined` (skill-load.ts:95), forcing skill-load.ts:195-204 to model and return `"skill_load approval gate unavailable"`. No by-value consumer has any such state. The branch is unreachable in production (gate bound at boot.ts:208 before workflow-stores.ts:73) and uncovered by tests — the four sites that pass `getApprovalGate: () => undefined` (workflow-tools.test.ts:1480, :1503, :1517, :1533) never reach it: :1480 stubs `isApproved: async () => true` (:1466), the other three early-return on missing skill / bad name / missing generation access. Grep for "approval gate unavailable" returns exactly one hit — the throw site itself. Dead code kept alive purely by the `| undefined` typing.
(b) Documentation already drifted: conversation-wiring.ts:286-288 comments about "approvalGateRef was bound at construction time" — an identifier that does not exist in that file (it is local to workflow-stores.ts:73). The comment survived the extraction that separated the two.
(c) Forward hazard: because the ref is writable and five holders are by-value, a future `approvalGateRef.fn = otherGate` would route the skill-approval modal through a different gate than the tool executor while every other consumer keeps the original — silent split-brain on a security surface, with no type or test to catch it.

CONTRAST THAT PROVES THE AUTHOR ALREADY DISTINGUISHES THESE CASES: at conversation-wiring.ts:517-519 the SAME call site uses `getRunner: () => subAgentRunnerRef.fn` (genuinely late — the runner is constructed at :470, after setupWorkflowStores ran) on the line immediately above `approvalGate,` passed by value. Same for workflow-stores.ts:83 (`getSubAgentRunner`, genuinely late) sitting next to :82 (`getApprovalGate`, not late).

HONEST CAVEAT: the drift here is weaker than the ToolExecutor seed. It is an unreachable branch, a stale comment, and a latent split-brain hazard — not a live user-visible behavior gap like the missing `ExecuteOptions.callbacks`. Whether the dead branch has ever masked a real failure is unestablished.

MERGEABLE: yes. No process, trust, or lifecycle boundary is crossed — the reacher (skill_load) runs in the same main process, in the same boot step that already holds the gate in local scope (workflow-stores.ts:40 destructures it, :130 registers the tools).


## approval-gate-sandbox-capability-fallback  *(permission-relevant)*

**Capability** — the OS-isolation capability shown/attributed for one tool invocation (substrate-aware, per-worker) — must never report `asrt` for a worker this process did not wrap

**Wirings** — AUTHORITY A (substrate-aware): `resolveReviewerSandboxCapability(source, toolName, mcpServerId, workerId, pluginId, hostShellExecutionPlan)` defined at src/permissions/sandbox-capability.ts:424, called for executor-raised approvals at src/tools/invocation-runner.ts:845 and src/tools/invocation-authorization.ts:1245, and for the reviewer lane via `resolveReviewerSandboxCacheState` at src/permissions/permission-manager.ts:1383-1390 (fed to the classifier at src/permissions/permission-manager.ts:1444).
AUTHORITY B (process-global): `ApprovalGate.sandboxCapabilityProvider`, declared src/permissions/approval-gate.ts:878, defaulted to `detectSandboxCapability` at src/permissions/approval-gate.ts:894, and injected into every execution-kind approval request that did not supply its own at src/permissions/approval-gate.ts:1165-1168 (guard `isExecutionKind` at src/permissions/approval-gate.ts:1142-1146). Production never overrides the default: src/boot.ts:203-207 calls `createApprovalGate` with 3 args and src/boot/conversation.ts:152-158 constructs `new ApprovalGate(...)` with only 5, leaving arg 6 at its default.

**Observed drift** — src/boot/steps/plugin-tool-executor.ts:668-686 raises `kind:"tool"` + `toolCategory:"write"` + `source:"plugin"` with NO `sandboxCapability` field, so `isExecutionKind` is true and Authority B injects the process-global capability for a PLUGIN call. src/permissions/sandbox-capability.ts:409-413 states the invariant this violates verbatim: "NO-LEAK INVARIANT ... never report `asrt` for a worker this process did not wrap", and src/permissions/sandbox-capability.ts:382-389 explains why ("plugin/MCP tool side-effects run in the unwrapped long-lived worker (isolation=none)"). The value is rendered to the user — src/ui/renderer/components/ToolApprovalDialog.tsx consumes `sandboxCapability` and src/i18n/messages/generated/toolApprovalDialog.ts:38 is the "OS isolation active ({kind})" row. EVERY other non-executor requester avoids the injection only by setting `toolCategory:"meta"`, several with an explicit comment saying so: src/permissions/effect-enforcement.ts:349 ("`meta` keeps the sandbox-capability row out of this non-execution modal"), src/tools/skill-load.ts:209, src/core/work-board-engine.ts:478, src/permissions/agent-action-requester.ts:189, src/mcp/mcp-elicitation-resolver.ts:206, src/engine/turn/intercepted-meta-gate.ts:70. plugin-tool-executor.ts:675 is the lone `toolCategory:"write"`.

**Verifier** — CONFIRMED — one capability, two authorities, actual drift, no boundary.

ONE CAPABILITY (verified). Both answer "what OS isolation applies to THIS invocation" and both land in the same field, `ApprovalRequestInput.sandboxCapability` (src/permissions/approval-gate.ts:156), rendered by the same line: `request.executionPlan?.capability ?? request.sandboxCapability` (src/ui/renderer/components/ToolApprovalDialog.tsx:1022), unconditional on kind/source inside `approvalReviewRows` (ToolApprovalDialog.tsx:1001-1067) → row "Security isolation" / "OS isolation active ({kind})" (src/i18n/messages/generated/toolApprovalDialog.ts:38,40).

AUTHORITY A verified at every cited site and two the candidate missed: src/permissions/sandbox-capability.ts:424; consumed at src/tools/invocation-runner.ts:845, src/tools/invocation-authorization.ts:1245, src/tools/pipeline/approval-memory-skip.ts:106-107, and via `resolveReviewerSandboxCacheState` (src/permissions/sandbox-capability.ts:519,527) at src/permissions/permission-manager.ts:1384-1390.

AUTHORITY B verified: src/permissions/approval-gate.ts:878, default `detectSandboxCapability` at :894, injected at :1163-1168 under `isExecutionKind` (:1142-1146). Production never overrides — src/boot/conversation.ts:152-158 passes 5 args, src/boot.ts:203-207 passes 3. A repo-wide grep for `sandboxCapabilityProvider` outside approval-gate.ts returns nothing.

DRIFT IS REAL, NOT AESTHETIC. src/boot/steps/plugin-tool-executor.ts:668-686 sets `kind:"tool"` + `toolCategory:"write"` + `source:"plugin"` with no `sandboxCapability` and no `executionPlan`, so :1163-1168 injects the process-global value. That value is published for the HOST-SHELL substrate only — src/boot/steps/sandbox-init.ts:225-238 calls `setActiveSandboxCapability({kind:"asrt", confines})` with the in-file comment "Machine-checkable confinement for the host-shell substrate". Authority A for the same call returns `{kind:"none", reason:"plugin/MCP worker not ASRT-wrapped"}` (sandbox-capability.ts:480-489) unless `isPluginWorkerWrapped(pluginId, workerId)` (:446-459). Authority B cannot consult that registry at all, so with the gate ON it reports `asrt` for EVERY plugin request — the verbatim NO-LEAK invariant at sandbox-capability.ts:408-412. I confirmed the value survives to the renderer unfiltered: the payload is `{...signedReq, args: maskArgsForDisplay(...)}` (approval-gate.ts:1483-1495) and is sent at :1505; `isReadOnly:false` + `forceExplicit:true` (plugin-tool-executor.ts:683-684) means the §S4 skip at :1214-1219 does not fire, so the dialog IS shown. Live in production via src/ipc/domains/plugins.ts:1348 and :2466.

TEMPORAL EVIDENCE OF DRIFT (not design): Authority A landed 2026-06-25 (90c67480) and gained its plugin-worker branch 2026-07-08 (286461e1 "Run Windows plugin workers through ASRT grants"); the plugin operation-grant approval was written 2026-07-23 (512be251) and did not adopt it. A requester added a month after the authority existed, missing the field.

CANDIDATE'S "LONE toolCategory:write" CLAIM VERIFIED by enumerating all 10 non-executor `requestAndWait` callers: work-board-engine.ts:471-478, intercepted-meta-gate.ts:65-70, mcp-elicitation-resolver.ts:199-206, agent-action-requester.ts:184-189, effect-enforcement.ts:343-349, permission-mode-apply.ts:93-97, agent-action-approver.ts:70-81, skill-load.ts:205-209 — all `toolCategory:"meta"`. plugin-tool-executor.ts:675 is the only `"write"`, and it is also the only one that represents a real tool execution, which is exactly why it is the one that trips the guard.

REFUTATIONS ATTEMPTED AND FAILED.
(1) "B is a legitimate injectable default, not a rival authority." Fails on fail-safety direction: A's documented behavior for an unknown substrate is `none` — sandbox-capability.ts:417-422 calls that "the historical default", explicitly designed so omitted ids keep pre-existing behavior. B's fallback claims isolation that does not exist. Two answers to one question where the wrong one is the default is not a default/override pair.
(2) "Display-only, so harmless." I checked: within the gate, `sandboxCapability` has no policy consumer (only :979 destructure, :1165 injection); away-authority reads `toolCategory`, not the capability (:1315). But the row is the user's sole isolation signal on a modal whose reason line is `Plugin '<id>' requests one execution of '<op>'` (plugin-tool-executor.ts:677) — misinforming the human decider is the harm, and the invariant comment at sandbox-capability.ts:381-389 names exactly this class.
(3) "Merging crosses a boundary." Fails hardest. plugin-tool-executor.ts:26 ALREADY imports from ../../permissions/sandbox-capability.js — `isActiveSandboxFilesystemContainedForPluginEffects`, which is itself a thin wrapper over Authority A (sandbox-capability.ts:748-762 → :754). That import is passed into the executor at plugin-tool-executor.ts:124. So the same file already routes one substrate question through Authority A and silently lets the other be answered by Authority B. Same process (main), same module, same synchronous request-build moment. No process, trust, or lifecycle boundary.

Severity limiter, stated honestly: the ASRT gate is DEFAULT-OFF (sandbox-capability.ts:164), so with the gate off both authorities return `none` and the drift is unobservable. It becomes live the moment a user enables the sandbox — which is the deployment the plugin-worker wrap feature (286461e1) exists for.

UNESTABLISHED: whether the governed Tool descriptor reachable at plugin-tool-executor.ts:687 (`toolRegistry.findByName(toolName)`, currently resolved AFTER the gate) carries `workerId`. This does not affect mergeability: `resolveReviewerSandboxCapability("plugin", toolName)` with ids omitted returns the fail-safe `none`, which already satisfies the invariant.


## approval-request-isreadonly  *(permission-relevant)*

**Capability** — "is this approval request read-only" — the isReadOnly flag on an outgoing ApprovalRequest

**Wirings** — (1) Layer 3 tool ask: src/tools/invocation-authorization.ts:1156-1158. (2) Layer 1 out-of-allowed-dir ask: src/tools/invocation-runner.ts:755. Both feed the same consumer, `ApprovalGate.requestAndWait`'s §S4 short-circuit at src/permissions/approval-gate.ts:806.

**Observed drift** — ACTUAL BUT CURRENTLY INERT — worth recording because it is the same silent-asymmetry shape as the seed. Site (1) computes `approvalReasonPrefix ? false : invocationCategory === "read"`, i.e. a cross-agent/A2A-prefixed call is never treated as read-only. Site (2) computes plain `invocationCategory === "read"` with no prefix override, even though `approvalReasonPrefix` is in scope there (it is used four lines earlier at invocation-runner.ts:750 to build the reason string). The consequence is masked today only because approval-gate.ts:809 excludes `kind !== "out-of-allowed-dir"` from the §S4 auto-approve, and site (2) sets `kind: "out-of-allowed-dir"` (invocation-runner.ts:744). I confirmed `isReadOnly` has exactly one consumer (approval-gate.ts:806 is the only `.isReadOnly` read outside tools/base.ts:297). So: the divergence is real, its safety depends entirely on a `kind` exclusion in a third module, and nothing ties the two together.

**Verifier** — CONFIRMED — and the drift is demonstrable historically, not just hypothetically.

WHAT THE DECIDING CODE ACTUALLY SAYS (line numbers differ from the candidate; see `correction`):
- Site (1), Layer 3 tool ask, C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks\src\tools\invocation-authorization.ts:1232-1234: `isReadOnly: approvalReasonPrefix || requiresRemoteLocalOneShot ? false : invocationCategory === "read"`.
- Site (2), Layer 1 out-of-allowed-dir ask, ...\src\tools\invocation-runner.ts:838: `isReadOnly: invocationCategory === "read"` — no override terms.
- Sole consumer: ...\src\permissions\approval-gate.ts:1215, inside the §S4 short-circuit at 1214-1221.

SINGLE CONSUMER CONFIRMED. `.isReadOnly` on a request is read in exactly one place: approval-gate.ts:1215. It is declared on the renderer contract at ...\src\ui\renderer\types.ts:1392 but no renderer component reads it (grep over src\ui returns only that type declaration), and it does not appear in `auditFieldsFor` output. So it is a single-authority field with multiple producers.

PROOF OF ACTUAL DRIFT (this is the strongest evidence, and the candidate missed it). `git log -L` on both lines:
- Both sites were born identical-in-spirit in f1402777 ("refactor(tools): stage the ToolExecutor pipeline").
- Commit ef60243d ("feat(platform): complete multisurface runtime P3", Aug 3 2026) introduced the remote-controller one-shot concept and touched BOTH files in the SAME commit. In invocation-authorization.ts it changed the line to `approvalReasonPrefix || requiresRemoteLocalOneShot`. In invocation-runner.ts (+94 lines) it added `requiresRemoteDirectoryOneShot` (runner:773-774) and wired it to `allowedChoices`/`forceExplicit` (runner:813-818) and to a post-decision check — but never touched line 838. `git log -L 838,838:src\tools\invocation-runner.ts` shows exactly one commit ever: the original f1402777. One author, one commit, one concept, applied to one wiring and not the other. That is the seed shape exactly.
- The two predicates are also literally the same expression under two names: invocation-authorization.ts:207-208 and invocation-runner.ts:773-774 are both `invocationPermissionContext.remoteControllerAuthority !== undefined`.

WHY SITE (1) NEEDED THE OVERRIDE — and why site (2)'s substitute does not work. §S4 (approval-gate.ts:1214-1221) tests only `isReadOnly`, `mode`, and `kind`. It does NOT test `forceExplicit`/`requireExplicit`: `requireExplicit` is computed at approval-gate.ts:1175-1177 and §S4 at 1214 returns `allow-once` without ever consulting it. So `forceExplicit: true` — which site (2) does set under the one-shot condition (invocation-runner.ts:817) — buys zero protection against the read-only auto-approve. Site (1) pins `isReadOnly: false` precisely because forceExplicit cannot stop §S4. Site (2)'s identical situation is saved only by `kind: "out-of-allowed-dir"` (invocation-runner.ts:821) hitting the exclusion at approval-gate.ts:1218. The candidate's characterization is right: safety rests entirely on a `kind` exclusion in a third module, and nothing ties the two derivations together.

REFUTATIONS I ATTEMPTED, AND WHY THEY FAIL:
1. "Legitimate boundary — Layer 1 is a scope-grant decision, Layer 3 is an execution decision" (the gate itself argues this at approval-gate.ts:1211-1213, policy P2.5). Real distinction, but it is carried by `kind`, which is the field the gate actually branches on (invocation-runner.ts:821 → approval-gate.ts:1218). Both sites compute `isReadOnly` from the identical base expression about the identical invocation. The boundary is expressed elsewhere and does not justify two derivations of THIS field.
2. "P2.5 is the intended single chokepoint; producer-side suppression would be the layered-defense antipattern." Rejected by the codebase's own convention: the other producers whose `kind` is ALREADY excluded by §S4 still pin the flag false — rationale-host-coordinator.ts:935 (`kind: "rationale"`, excluded at approval-gate.ts:1220) with `isReadOnly: false` at :955, and mcp-elicitation-resolver.ts (`kind: "agent-action"`, excluded at approval-gate.ts:1219) with `isReadOnly: false` at :212. Site (2) is the only excluded-kind producer that computes the flag dynamically. Consolidating removes a duplicate derivation; it does not add a layer.
3. "The field is inert at site (2), so this is aesthetic." Inert is not the same as absent: it is a live field on a shared request contract, with one authority consumer, whose value at site (2) contradicts site (1)'s definition of the same field name. Per the brief's own standard, one side has branches the other lacks — that is actual drift, and here it is a dated commit rather than a supposition.

MERGEABLE. No boundary is crossed. Both sites are in the same host-main process, same trust domain, same executor pipeline, and both already have every input in scope: `approvalReasonPrefix` is destructured in the runner at invocation-runner.ts:165 and used at :832 (four lines above the divergent line), and `remoteControllerAuthority` is read off the same `invocationPermissionContext` at both sites. A pure helper taking (invocationCategory, approvalReasonPrefix, remoteControllerAuthority) serves both; the change is behavior-preserving today because of the `kind` exclusion.


## auth-host-allowlist-two-matchers  *(permission-relevant)*

**Capability** — Dot-boundary host-suffix matching against a caller-declared allow-list, plus validation that the declared list is not blanket-matching (wildcard / bare public-suffix / single-label / IDN-punycode).

**Wirings** — (1) src/main/host-allow-list.ts:47 `normalizeHost`, :57 `normalizeAllowedHosts`, :107 `urlHostMatchesAllowList` — used by the partition-viewer path (src/main/auth-partition-viewer-service.ts:50-53, :234, :271) with validation applied at src/boot/steps/plugin-runtime/host-api-factory.ts:1127. (2) src/main/auth-window-service.ts:322 a private `normalizeHost` (byte-identical body to host-allow-list.ts:47-50) plus an inline suffix match at src/main/auth-window-service.ts:341-344 inside `filterCookiesByHost` (src/main/auth-window-service.ts:332), fed by src/main/auth-window-service.ts:500-506 and consumed at :692 and :914.

**Observed drift** — src/main/host-allow-list.ts:5-14 explicitly declares itself the SoT for BOTH surfaces and names `openAuthWindow.cookieHosts` as one of them, warning that "a single drifted rule on one side doesn't open a phishing window on the other". The cookie path never calls it. `normalizeAllowedHosts` rejects `*` wildcards (host-allow-list.ts:63-65), URLs (:66-68), bare public suffixes such as `com`/`co.kr` (:69-73), single-label hosts (:74-78), and `xn--*` punycode homoglyphs (:79-83), and caps the list at 16 (:89-93). The cookieHosts path applies NONE of these: src/main/auth-window-service.ts:500-504 only does `typeof === string`, `.trim()`, drop-empty, and the plugin wiring passes `opts` through untouched (src/boot/steps/plugin-runtime/host-api-factory.ts:1085-1088 — contrast :1127 on the sibling arm, which does validate). So a plugin holding `external-auth-consumer` can declare `cookieHosts: ["com"]` or a punycode homoglyph and have every matching cookie in the partition returned to plugin code, while the same string on the viewer arm throws.

**Verifier** — CONFIRMED after trying four refutations, all of which failed.

ONE CAPABILITY, SELF-DECLARED. `src/main/host-allow-list.ts:2-15` names exactly two governed surfaces and lists `openAuthWindow.cookieHosts` (`auth-window-service.ts`) FIRST (:6-7), with the stated reason "so a single drifted rule on one side doesn't open a phishing window on the other" (:12-15). The cookie path never imports it — `src/main/auth-window-service.ts:15-33` has no `host-allow-list.js` import, and the repo-wide grep for `host-allow-list` returns zero hits in that file.

MATCHER IS BYTE-DUPLICATED. `src/main/host-allow-list.ts:47-50` and `src/main/auth-window-service.ts:321-324` are the same three-token body (`raw.trim().toLowerCase()`, then strip a leading `.`). The suffix loop at `host-allow-list.ts:113-116` (`host === allowed || host.endsWith('.'+allowed)`) is re-implemented inline at `auth-window-service.ts:341-343` with identical semantics. `urlHostMatchesAllowList` takes a bare host string (:107-110), not a URL, and already calls `normalizeHost` internally (:112) — so `filterCookiesByHost` (:331) could call it directly. No adaptation needed.

REFUTATION 1 — different trust domain? FAILED. `src/plugins/public-contract.ts:1191-1193` states the `external-auth-consumer` capability is "Shared with `openAuthWindow` because both surfaces grant access to the same plugin auth partition cookie jar." Both arms are gated by the same capability at `src/boot/steps/plugin-runtime/host-api-factory.ts:1035` and :1121, both resolve the same `plugin-auth:<encodeURIComponent(pluginId)>` partition (:1073 vs. the viewer's `pluginId` pass-through at :1160), and both run in the Electron main process in the same directory (`src/main/`). Same trust domain, not adjacent ones.

REFUTATION 2 — laxer rules needed for host-internal callers? FAILED. `auth-window-service.ts:507-510` gestures at "direct service callers such as host-internal auth flows," but grep for `openAuthWindow(`/`openAuthWindowService` finds no such caller: the only production path is `src/boot.ts:52,484` → `src/boot/steps/plugin-runtime.ts:141-144,254,415` (pure threading, no interception) → `host-api-factory.ts:1098`. Every other hit is a `vi.fn()` stub. The "host-internal caller needs single-label intranet hosts" escape hatch is unestablished — it has no instance.

REFUTATION 3 — lifecycle boundary (manifest-static vs. call-time)? FAILED. The viewer arm does not validate at load time either; `host-api-factory.ts:1134-1137` reads `manifest.auth.partitionDomains` and calls `normalizeAllowedHosts` lazily *inside the async call handler*. Both arms resolve their list at call time in the same function object, ten lines apart.

REFUTATION 4 — existing tests pin the laxness? FAILED. Every fixture already satisfies `normalizeAllowedHosts`: `COOKIE_HOST = "portal.example.com"` (`src/main/__tests__/auth-window-service-e2e.test.ts:215`, `auth-window-dedup.test.ts:106`), and the unit cases use `sso.example.com`/`portal.example.com`/`space.example.com`/`example.com`/`.EXAMPLE.com`/`[]` (`src/main/__tests__/auth-window-service.test.ts:52,61,67,73,79,85,91,92`) — `example.com` is not in `FORBIDDEN_TOP_LEVELS` (only bare `com`, `host-allow-list.ts:32-44`) and `[]` returns `[]` without throwing. No module cycle blocks the merge either: `host-allow-list.ts` imports nothing, and `auth-window-service.ts:31` already transitively pulls it in via `auth-partition-viewer-service.js` (:53).

DRIFT IS REAL AND LOAD-BEARING. `auth-window-service.ts:690` does `session.cookies.get({})` — ALL cookies in the partition — and `cookieHosts` at :691 is the sole gate before they reach plugin code. That list is filtered only by `typeof === "string"` / `.trim()` / drop-empty / non-empty (`auth-window-service.ts:499-505`), and `host-api-factory.ts:1095-1098` forwards `opts` (or `{...opts, persistPartition}`) straight through. So `cookieHosts: ["com"]` is accepted, while the identical string on the sibling arm throws at `host-allow-list.ts:69-73` via :1137.

The asymmetry is now three-way, not two-way: `host-allow-list.ts` is wired into the viewer (`auth-partition-viewer-service.ts:50-53`), into network egress (`src/main/host-fetch-guard.ts:28,155` fed by `src/plugins/runtime/manifest-validation.ts:248` and `src/shared/network-access.ts:42`), and even into the ASRT translation layer, which explicitly normalizes toward it as the reference semantics (`src/permissions/asrt-sandbox.ts:1233-1249`). The cookie path is the single surface the SoT header names and the single surface that does not consume it.

SECOND, INDEPENDENT DRIFT (audit): the viewer arm logs the resolved list — `allowedHosts=${event.allowedHosts.join(",")}` (`host-api-factory.ts:1175`) plus per-deny `deniedHost` (`auth-partition-viewer-service.ts:66`). The cookie arm logs only `cookieHostCount` (`host-api-factory.ts:1033,1050,1059`), never the hosts. An operator reading the audit log cannot tell which hosts a plugin harvested. Same shape as the seed: the arm that skipped the shared authority also lost the reporting the other arm gets for free.

DEFENSE-IN-DEPTH COUNT: `docs/ko/references/plugin-tool-schema-design.md:416` documents three layers for `partitionDomains` (SDK schema → `normalizeAllowedHosts` → `urlHostMatchesAllowList`), and `schemas/plugin-manifest.schema.json:353` carries the schema layer. `cookieHosts` has no manifest field at all (it is purely a runtime argument, `public-contract.ts:93-96`) and therefore zero layers — despite `docs/ko/references/plugin-tool-schema-design.md:799` documenting it as a strict-comparison security property ("`evil-example.corp` must not match `example.corp`").


## delisted-admin-uninstall-bypasses-cleanup  *(permission-relevant)*

**Capability** — Remove an installed plugin — registry row + directories AND the host-owned residual state (config, secrets, auth partitions, cache), the cleanup journal, and the `plugin.uninstalled` event.

**Wirings** — AUTHORITY: `uninstallPluginWithLifecycle` — src/plugins/uninstall-lifecycle.ts:433 (`journal.prepare`), :449 (`deps.pluginMarketplace.uninstall(installPluginId)` nested inside `removePluginWithCommit`), :486 (`finishCommittedPluginCleanup`), :500 (`emitHostEvent("plugin.uninstalled")`). Cleanup phases at src/plugins/uninstall-lifecycle.ts:168 (config), :170 (secrets), :183 (`clearAuthPartitionService`), :201 (`forgetPluginAuthPartitionsService`), :206 (cache). SECOND WIRING: src/plugins/marketplace.ts:1387 `await this.uninstall(entry.id, { actor: "it-admin" })` inside `removeDelistedAdminInstalls` (src/plugins/marketplace.ts:1378), reached from `ensureManagedInstalled` at src/plugins/marketplace.ts:1359. The bare service method is src/plugins/marketplace.ts:1430-1516.

**Observed drift** — CONFIRMED, and it is exactly one level below the abstraction that WAS unified: `orphanedAdminInstalls` (src/plugins/marketplace.ts:252) was extracted so the two sites agree on the *predicate*, but the two *removals* still diverge. `grep -n "emitHostEvent|deletePluginSecrets|deletePluginConfig|clearAuthPartition|CleanupJournal" src/plugins/marketplace.ts` returns nothing — `marketplace.uninstall` (src/plugins/marketplace.ts:1430) only runs `executeRemovalTransaction` + `updatePluginRegistry` (:1497-1513). Consequences of the second wiring: (1) no `plugin.uninstalled` — so the registry-entry cache refresh listener (src/boot/steps/plugin-runtime.ts:543-549), the telemetry `plugin_uninstall` track (src/boot/steps/post-boot.ts:99), and every per-plugin HostApi `onEvent("plugin.uninstalled")` subscriber (src/boot/steps/plugin-runtime/host-api-factory.ts:537) are all silently skipped; (2) no cleanup journal record, so the plugin's config, its secrets, its `persist:plugin-auth:<id>` cookie jar, and its cache dir survive removal; (3) because no journal record exists, a later re-install's `ensurePluginStateReadyForInstall` (src/plugins/uninstall-lifecycle.ts:328, called for admin installs at src/plugins/marketplace.ts:1324) returns immediately at :335 and the new install silently inherits the old install's secrets. The user-initiated removal of the SAME delisted admin plugin takes the first wiring (allowed by `canUserRemoveOrphanedAdminInstall`, src/plugins/marketplace.ts:1518) and wipes all of it — so identical inputs give different residual state depending on who removed it. Runtime-stop being absent IS legitimate here (the boot pass runs before `startPlugins`, per the comment at src/plugins/marketplace.ts:1367-1368); the state cleanup is not.

**Verifier** — CONFIRMED. The bare service method is a SUBROUTINE of the authority, not a peer: `uninstallPluginWithLifecycle` calls `deps.pluginMarketplace.uninstall(installPluginId)` at src/plugins/uninstall-lifecycle.ts:449, nested inside `removePluginWithCommit`. `removeDelistedAdminInstalls` calls that same inner half directly at src/plugins/marketplace.ts:1387, skipping the outer half. One capability, two entries.

DRIFT IS EXCLUSIVE, NOT MERELY DIVERGENT. A repo-wide grep for `deletePluginConfig|deletePluginSecrets` over src/ (excluding __tests__) returns only the definitions in src/data/settings-store.ts:1089,1131 and the call sites in src/plugins/uninstall-lifecycle.ts:169,172. There is no second GC, no boot-time reconciler, nothing that could rescue the second wiring. Residual state genuinely survives. Re-install inheritance confirmed: src/plugins/uninstall-lifecycle.ts:334-335 is `const record = journal.find(pluginId); if (!record) return;` — no journal record means immediate return, and src/plugins/marketplace.ts:1324 is where the admin re-install calls it. Secrets are keyed `plugin.<pluginId>.<key>` (src/data/settings-store.ts:1134), the same key a re-install reads.

REFUTATION 1 — layering/circular import. `uninstall-lifecycle.ts` imports `PluginMarketplaceService` from marketplace.js (src/plugins/uninstall-lifecycle.ts:5), so marketplace.ts cannot import back. FAILED: the codebase already solved exactly this by injection. `ensureManagedInstalled` takes `ensurePluginStateReadyForInstall: (pluginId: string) => Promise<void>` as a REQUIRED option (src/plugins/marketplace.ts:1141,1145), hard-validates its presence (src/plugins/marketplace.ts:1172-1176), and src/boot.ts:641-654 supplies it as a closure over the full `PluginUninstallLifecycleDeps` — the identical dep bundle `uninstallPluginWithLifecycle` needs, already constructed at the same call site. src/ipc/domains/plugins.ts:431 supplies it for the other mode. The seam exists; no new plumbing.

REFUTATION 2 — unloaded runtime pre-start would throw. FAILED: `resolvePluginInstallIdIfKnown` (src/plugins/runtime/index.ts:1103-1107) is documented at :1102 as returning `undefined` for a fresh identity. The guards at src/plugins/uninstall-lifecycle.ts:367 and :420 test `=== null`, and :425 is `installClaim ?? pluginId`. Undefined falls through to `installPluginId = pluginId`, which is exactly `entry.id` from the registry. Lazily resolvable.

REFUTATION 3 — boot-only lifecycle boundary, so no subscriber exists to miss the event. FAILED, AND IT INVERTED. `await this.removeDelistedAdminInstalls(plugins, result)` at src/plugins/marketplace.ts:1359 sits OUTSIDE the mode branch, so it runs in BOTH modes. `repair-missing-only` is invoked from the `CHANNELS.bootstrap.retry` IPC handler at src/ipc/domains/plugins.ts:429-436 — user-triggered in a fully booted app. On that path the runtime is live and every subscriber the candidate lists is actually registered: src/boot/steps/plugin-runtime.ts:543-549, src/boot/steps/post-boot.ts:99, src/boot/steps/plugin-runtime/host-api-factory.ts:537. The missing `plugin.uninstalled` is a live silent skip there, not a moot one.

Confirmed identical-input divergence: the same delisted admin plugin removed by the user routes through src/ipc/domains/plugins.ts:549 → full lifecycle; removed by the boot/repair pass → registry row and directories only.


## host-secret-gate-two-implementations  *(permission-relevant)*

**Capability** — The four-tier decision "may this plugin read this host-managed secret?" (own-namespace → manifest `hostSecrets.read[]` allow-list → signed whitelist registry → active-vendor cross-check).

**Wirings** — src/boot/steps/plugin-runtime/host-api-factory.ts:595-768 — `hostApi.getSecret`: Tier-1 at :624-639 (whole `plugin.<id>.*` namespace), Tier-2 at :640-643, Tier-3+4 delegated to `runTier3Then4` at :341-352. || src/main/host-api/resolve-api-key.ts:262-386 — `hostApi.resolveApiKey`: Tier-1 at :268-287 (one key only), Tier-2 at :289-299, Tier-3+4 delegated to `runTier3Then4` at :314-321. The file header (resolve-api-key.ts:3-4) calls itself a wrapper of "the four-tier secret gate (`plugin-runtime.ts:getSecret`)".

**Observed drift** — Tiers 3+4 were correctly factored into one authority (`runTier3Then4`); tiers 1 and 2 were left duplicated and diverge. (1) Key-family coverage: `getSecret` Tier-2 handles BOTH `llm.apiKey.<vendor>` and `llm.marketplaceProvider.<presetId>.apiKey` (host-api-factory.ts:651-667 parses the marketplace form explicitly), while `resolveApiKey` constructs only `const llmKey = llm.apiKey.${vendor}` (resolve-api-key.ts:262) and tests only that at :291 — a manifest-declared marketplaceProvider key is structurally unreachable through `resolveApiKey`, even though :256-261 already computes the marketplace preset id for the Tier-4 comparison. (2) Tier-1 scope: `getSecret` returns any `plugin.<id>.*` key (host-api-factory.ts:624); `resolveApiKey` probes exactly one synthesized key `plugin.<id>.llm.apiKey.<vendor>` (resolve-api-key.ts:268). (3) Forensic drift: on a plain Tier-2 manifest allow with no whitelist involvement, resolve-api-key.ts:370 still logs `source=whitelist-registry`, while the getSecret path logs the neutral `hostSecret_read` (host-api-factory.ts:754) — two audit vocabularies for one decision.

**Verifier** — CONFIRMED. Root = C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks\.

ONE CAPABILITY (established, not inferred):
- src/main/host-api/resolve-api-key.ts:3-4 and :210-212 state the file "Wraps the four-tier secret gate (`plugin-runtime.ts:getSecret`)" / "Mirrors the 4-tier gate".
- src/plugins/whitelist/tier-order.ts:2-4 names BOTH call sites as the consumers of a single ORDER SOT — the codebase already treats this as one decision and extracted half of it (tiers 3+4).
- Only two runtime readers of the tier-2 input exist: src/boot/steps/plugin-runtime/host-api-factory.ts:640 and src/main/host-api/resolve-api-key.ts:290. (whitelist-registry.ts:296 reads the signed GRANT's hostSecrets.read — tier-3 data, different source.) Grep for `getSecret\s*[:(]` finds exactly one hostApi implementation (host-api-factory.ts:595). So: two wirings, not three.

REFUTATION ATTEMPTS, ALL FAILED:
1. Process/trust boundary — none. host-api-factory.ts:69 imports resolveApiKey directly; calls it in-process at :550. Both members live in the SAME object literal returned at host-api-factory.ts:352, closing over the same pluginId/manifest/manifestSha256/settingsService/registry entry (:557-574 vs :625-712).
2. "Different logging surfaces" (the rationale tier-order.ts:16-18 gives for not sharing more) — refuted: host-api-factory.ts:561 passes `auditLogger: bootAuditLogger`, the same instance getSecret uses at :629/:717/:749. The distinction exists only as a test seam (resolve-api-key.test.ts injects makeAuditLogger()).
3. Different return contracts (async bearer/release/baseUrl/typed SDK reason vs sync string|null) — real, but that is the wrapper AROUND the decision. tier-order.ts:80-85 explicitly documents the pattern where the shared gate returns a discriminated outcome and each caller keeps its own audit + return shape. Tiers 3+4 already work that way; tiers 1-2 were simply left behind.
4. Vendor alias normalization + default-active-vendor (resolve-api-key.ts:135-143, :240-245) is resolveApiKey-only — but it runs BEFORE the gate and only produces the key string; it is not a tier.
5. Lifecycle — no lazy-resolution problem: both are constructed at the same instant inside createHostApi, and the whole object is wrapped once by enforceActiveHostApi (host-api-factory.ts:352), which covers both members.

ACTUAL DRIFT (not aesthetic):
(a) KEY-FAMILY COVERAGE — the candidate's strongest claim, verified. getSecret handles `llm.marketplaceProvider.<presetId>.apiKey` including an installed-preset cross-check (host-api-factory.ts:651-687); resolveApiKey builds only `llm.apiKey.${vendor}` (resolve-api-key.ts:262) and tests only that (:291). The family is first-class in the gate's domain: manifest-validation.ts:58-63 and :721 accept it, whitelist-schema.ts:43 accepts it, public-contract.ts:437-438 documents it. CONSEQUENCE: with llm.provider === "openai-compatible" and a valid marketplaceProviderPresetId, resolveApiKey has NO reachable allow path for any vendor — vendor "openai-compatible" gets activeProvider=presetId (resolve-api-key.ts:256-261) → tier-4 mismatch; any other vendor gets activeProvider="openai-compatible" → tier-4 mismatch (tier-order.ts:112-113). getSecret meanwhile grants the preset key (plugin-runtime.test.ts:1276). The lifetime-safe API (bearer/release/revoke-signal) silently loses a key family the raw API supports, and returns a misleading reason ("vendor-mismatch"/"not-whitelisted").
(b) PROOF THE SPLIT COSTS — git 53df03c5 ("harden provider preset credential boundaries") edited both files in ONE commit: the deny-side hardening landed on both (resolve-api-key.ts:256-261 + test resolve-api-key.test.ts:325-354 asserting the generic key is denied under an active preset), the allow-side only on getSecret (host-api-factory.ts:673-687). Commit body carries no rationale for the asymmetry.
(c) TIER-1 TELEMETRY ASYMMETRY (missed by the candidate) — getSecret's tier-1 emits NO counter on allow OR on the endpoint-URL block (host-api-factory.ts:625-638 logs only `pluginSecret_denied`); resolveApiKey's tier-1 increments hostSecret_denied (:276) and hostSecret_read (:285). Own-namespace reads are inside host-secret telemetry on one path and invisible on the other, so hostSecret_read totals are not comparable across the two APIs.
(d) DENY-REASON VOCABULARY (missed) — same tier, different declared token: tier-2 deny is `reason=not-allowlisted` (host-api-factory.ts:764) vs `reason=not-whitelisted ... (manifest)` (resolve-api-key.ts:295); tier-4 deny is `non-active-vendor` (host-api-factory.ts:715) vs `vendor-mismatch` (resolve-api-key.ts:330). Both tokens are separate members of the HostSecretDeniedReason taxonomy at telemetry/host-secret-counters.ts:55-61, so operators pivoting on one token see only half the traffic.


## host-secret-key-allowlist-three-authorities  *(permission-relevant)*

**Capability** — "Is this string a well-formed host-secret key a plugin is permitted to name?" — the shape gate applied to `hostSecrets.read[]` entries.

**Wirings** — src/plugins/runtime/manifest-validation.ts:55 `LLM_API_KEY_PATTERN` + :58-64 `isAllowedHostSecretKey`, enforced on manifest entries at :718. || src/plugins/whitelist/whitelist-schema.ts:44 + :46-52 — a verbatim copy, enforced on signed-whitelist grant entries at :133; its comment at :43 says the quiet part out loud: "Accepted `hostSecrets.read[]` keys — matches manifest-validation.ts." || schemas/plugin-manifest.schema.json:306 — the JSON-schema `pattern`, described by manifest-validation.ts:52-53 as the thing the TS copy "mirrors ... as defence-in-depth".

**Observed drift** — Real, permissive direction for the TS copies. The JSON schema pattern is `^(?:llm\.apiKey\.[a-z]+(-[a-z]+)*|llm\.marketplaceProvider\.[A-Za-z0-9][A-Za-z0-9._-]{0,79}\.apiKey)$` plus `maxLength: 111` and `maxItems: 32` (schemas/plugin-manifest.schema.json:303-310). Both TS copies delegate the marketplace branch to `marketplaceProviderPresetIdFromSecretKey` (src/shared/marketplace-package-assets.ts:406-419), which routes through `normalizeProviderId` (marketplace-package-assets.ts:125-129) — and that function TRIMS (`const trimmed = value.trim()`). So `llm.marketplaceProvider.  foo  .apiKey` is ACCEPTED by manifest-validation.ts:718 and whitelist-schema.ts:133 but REJECTED by the JSON schema (space is not in the character class). Neither TS copy enforces the schema's length or count bounds at all. The divergence is caught downstream by luck, not design: host-api-factory.ts:659-665 slices the preset id out of the raw key WITHOUT trimming, so `isMarketplaceProviderPresetId("  foo  ")` fails at :677 and Tier-4 denies — a fourth place where the same key shape is re-derived, with a fourth rule.

**Verifier** — CONFIRMED by running the deciding logic. One predicate ("is this a well-formed host-secret key a plugin may name?") has three encodings: manifest-validation.ts:55+:58-64 (applied :718), whitelist-schema.ts:44+:46-52 (applied :133, comment at :43 admits it is a copy), and schemas/plugin-manifest.schema.json:306 (+maxLength:111 :307, maxItems:32 :309, uniqueItems :310). Executed transcription of the deciding code against the schema pattern: the TS gate ACCEPTS `llm.marketplaceProvider.  foo  .apiKey`, `...\tfoo...`, `...\nfoo...`, and `llm.apiKey.`+500 chars; the JSON-schema pattern REJECTS all four. Cause is exactly as claimed: marketplaceProviderPresetIdFromSecretKey (marketplace-package-assets.ts:406-419) routes through normalizeProviderId (:125-129) which trims at :127.

Refutation attempts all failed. (1) "Deliberate defence-in-depth" — manifest-validation.ts:52-53 claims the mirror stops a manifest installing "a wider allowlist than the schema permits", but the mirror is LOOSER than the schema so it cannot enforce that claim; the sibling rationale at :729-734 (stale-SDK-schema smuggling) is explicitly obsoleted by the same file at :122-135 ("a host-vendored schema cannot be version-skewed"). (2) "JSON Schema cannot call TS, so it is a format boundary" — true for that leg only, and the repo already built the pin and dropped it: MARKETPLACE_PROVIDER_PRESET_ID_PATTERN_SOURCE (marketplace-package-assets.ts:104) and _MAX_LENGTH (:103) are both knip-baselined dead exports (knip-baseline.json:848, :853), i.e. single-authority was intended and the wiring was never landed. (3) "Different trust domains" — does not hold for the two TS copies: same main process, same lifecycle, both already import the same shared module (manifest-validation.ts:31, whitelist-schema.ts:17), so merging them adds no dependency edge and crosses no boundary.

mergeable=true for the two TS copies (collapse into one exported predicate in src/shared/marketplace-package-assets.ts). The JSON-schema leg is a genuine language boundary that needs a generated/asserted pin rather than a runtime merge — and the pin hook already exists unused.

Currently NOT exploitable end-to-end: Tier-2 (host-api-factory.ts:643, resolve-api-key.ts:291) and Tier-3 (whitelist-registry.ts:296) both do exact-string `includes`, so a malformed whitelist-only key is inert unless the manifest also carries it, which AJV forbids. Fail-closed by conjunction, not by the gates agreeing.


## message-token-cost-two-estimators  *(permission-relevant)*

**Capability** — "How many tokens does this one GenericMessage cost on the wire?" — the number that gates compaction, preflight, and oversize archiving.

**Wirings** — AUTHORITY: `estimateMessageTokensForWire` — src/engine/auto-compact.ts:128-154. It (a) routes user messages through `estimateUserMessageTokens` at :129 so multimodal overhead is counted, (b) substitutes the wire stub for marked tool_results via `serializeMessageForWireEstimate` at :161-180, (c) adds image-tile overhead for a live view_image tool_result at :142-152. SECOND DERIVATION: src/engine/structured-compact.ts:265 — `estimateTokens(serializeMessageForEstimation(msg))`, the bare serializer with none of the three compensations. Underlying enumerator: `serializeMessageForEstimation` — src/engine/llm/types.ts:237-258 (tool_result arm at :250-257).

**Observed drift** — CONFIRMED, and it is intra-file. src/engine/structured-compact.ts:11 already imports `estimateMessageTokensForWire` and uses it correctly at :377 and :706 — but `truncateOversizeMessages` at :265 bypasses it for the raw serializer. The raw serializer's tool_result arm (src/engine/llm/types.ts:250-257) enumerates role/toolUseId/toolName/content/isError and omits `image`, which IS declared on tool_result at :209 and IS sent to Claude at src/engine/llm/vercel/message-mapper.ts:148-160 — while its own docstring at :232-236 claims it "Includes all prompt-bearing fields". Consequence at src/engine/structured-compact.ts:266: the gate is `msgTokens <= TRUNCATION_THRESHOLD_TOKENS` (30_000, src/shared/compact-status.ts:15). A tool_result carrying a large image measures near zero and is never archived+clipped; conversely an already-`compactedAt` tool_result measures at its RAW length even though the wire only carries a short stub, so it is written to disk and clipped for nothing.

**Verifier** — CONFIRMED after attempting refutation on four fronts; all four failed to save the split.

ONE CAPABILITY, NOT TWO. I looked for a legitimate reason the pre-pass would want raw rather than wire size. There is none:
(1) Same declared purpose. structured-compact.ts:434-435 states the gate's intent as "단일 거대 메시지 (>30K tokens) 가 LLM input context 초과하는 케이스 방지" — LLM input cost, the same question estimateMessageTokensForWire answers. The threshold is denominated in tokens (shared/compact-status.ts:15), not bytes, so it is not a disk/memory-footprint gate.
(2) Same pipeline, adjacent steps, already inconsistent. Step 0 (:265) uses the raw serializer; step 1a `dropOldestUntilUnderBudget` (:377) uses estimateMessageTokensForWire; step 1 `splitForBoundary` (:706) uses estimateMessageTokensForWire. All three bound the same compaction pass over the same array. structured-compact.ts:11 already imports the authority — the bypass is intra-file and one token wide.
(3) The enumerator has no other external consumer. `serializeMessageForEstimation` (llm/types.ts:237) is called from exactly four places: three inside `serializeMessageForWireEstimate` (auto-compact.ts:163, :166, :176) and structured-compact.ts:265. It is effectively a private helper of the authority with one escape hatch.
(4) No process / trust / lifecycle boundary. Same module, same synchronous call, same in-memory array, no vendor or renderer split. Nothing to lazily resolve.

DRIFT IS REAL AND BIDIRECTIONAL, verified against the producers:
- OVER-COUNT (raw > wire). `markStaleToolResults` sets meta.compactedAt while keeping content verbatim (auto-compact.ts:287-297, contract at :186-193: "memory: verbatim"). It runs post-turn (hooks/post-turn-hook-chain.ts:123) and intra-turn (engine/turn/query-loop.ts:1565) on the same history that compaction.ts:197 later hands to compactWithBoundary, so marked rows are present at :265 by construction. The wire only carries `buildToolResultStub` (auto-compact.ts:169-174), yet :265 measures raw length. A 40K-char aged tool_result is archived to disk and clipped by `rebuildMessageWithText` (:313-321) for zero wire saving — and the clip destroys the verbatim copy the marker contract at auto-compact.ts:186-193 exists to preserve. This is damage, not merely wasted work.
- UNDER-COUNT (raw < wire). types.ts:250-257 omits `image` although it is declared at :209 and sent to Claude at llm/vercel/message-mapper.ts:148-160, while the docstring at :232-236 claims "Includes all prompt-bearing fields."

NO TEST PINS THE CURRENT BEHAVIOR. src/engine/__tests__/structured-compact.test.ts contains no match for truncat|oversize|30_000, so nothing encodes :265's raw reading as intentional.


## outgoing-turn-dlp-redaction  *(permission-relevant)*

**Capability** — Applying `privacy.piiRedactEnabled` PII redaction to user-authored turn content (input + text attachments) immediately before it reaches the provider, and emitting the single `redact_notice` stream frame — `sanitizeOutgoingTurnContent`, src/ipc/handlers/chat.ts:358.

**Wirings** — (1) chat:send — src/ipc/handlers/chat.ts:523. (2) main-chat edit-resend / fork-replay / retry `streamTurn` — src/ipc/domains/chat.ts:427. (3) chat:guide — src/ipc/domains/chat.ts:564 via `sanitizeOutgoingInput` (src/ipc/handlers/chat.ts:417). (4) side-chat send — src/ipc/domains/sidechat.ts:113-134, which reaches the provider through the SAME `runStreamedTurn` transport (src/ipc/domains/sidechat.ts:124) but calls no sanitizer.

**Observed drift** — Side-chat imports and uses the shared `validateUserContentParts` from the same module (src/ipc/domains/sidechat.ts:44, :119) but not `sanitizeOutgoingTurnContent` — it passes `p.input` raw at src/ipc/domains/sidechat.ts:127 and `attachments` raw at :131. Every other user-authored entry point sanitizes. Result: with `piiRedactEnabled` on, main-chat text is redacted and produces a `redact_notice` frame (src/ipc/handlers/chat.ts:398-407) while byte-identical side-chat text reaches the provider verbatim with no notice and no count-only DLP audit row. Side-chat is genuinely a separate loop/session/MemoryManager (src/ipc/domains/sidechat.ts:14-23), but the redaction policy is a single global user setting, not a per-loop property — the isolation argument does not cover it.

**Verifier** — CONFIRMED after trying to refute it. Note: every line number in the candidate is off by ~20-140 lines against this worktree; verified anchors below.

ONE CAPABILITY (refutation attempts failed):
1. Same setting object, same process. `sanitizeOutgoingTurnContent` reads `settingsService.get("privacy")` (src/ipc/handlers/chat.ts:337,343-344). The side loop is constructed with the SAME `settingsService` instance (src/boot/conversation.ts:440, passed at src/boot/steps/conversation-wiring.ts:357), and the side registrar receives the SAME `deps` bag as the chat registrar (src/ipc/index.ts:94-95; `IpcDeps = AppServices & {...}`, src/ipc/types.ts:12). There is no second policy store to reconcile.
2. Same provider egress. Side loop gets the same `llmFetch` + `subscriptionProviderFactory` (src/boot/conversation.ts:463-464) and the same transport function `runStreamedTurn` (src/ipc/domains/sidechat.ts:127 vs src/ipc/handlers/chat.ts:537). Not a different trust domain — the side channels are INTERNAL-only (src/ipc/domains/sidechat.ts:4-10,26-28), i.e. strictly the same user, same renderer.
3. No downstream catch-all. `redactForLLM` has no call site in src/engine on the outbound user message (engine hits are `maskSensitiveData` for A2A/transcript only: src/engine/subagent-transcript.ts:25, src/engine/a2a-subagent-message-codec.ts:1). So `p.input` at src/ipc/domains/sidechat.ts:127 and `attachments` at :133 reach the provider verbatim. Confirmed drift, not aesthetics.
4. Content is user-authored, identical in kind. src/ipc/domains/sidechat.ts:111-115 takes renderer-typed text and runs the SHARED `validateUserContentParts` imported from the very module that owns the sanitizer (:45) — it imports one half of the module's turn-entry contract and not the other.
5. Every other user-authored path is already consolidated onto the one authority: chat:send (src/ipc/handlers/chat.ts:531), edit-resend/fork-replay/retry (src/ipc/domains/chat.ts:579), chat:guide via `sanitizeOutgoingInput` (src/ipc/domains/chat.ts:715 → src/ipc/handlers/chat.ts:396-402). External/remote actors (Telegram, Tailnet) are NOT a fifth gap — they funnel into `handleChatSend` (src/main/conversation-command-port.ts:229-246). The only genuinely unsanitized `runStreamedTurn` besides side chat is src/ipc/domains/chat.ts:653, which is `inputOrigin: "agent-message"` mailbox guidance — not user-authored, correctly out of scope.
6. User-facing promise is surface-agnostic: the toggle description says PII is replaced "before being sent to the LLM" with no surface qualifier (privacyTab.piiRedactDescription / piiRedactToggleDescription in src/i18n/messages/generated-locales/*.ts).

MERGEABLE (no process/trust/lifecycle barrier):
- Notice transport already exists on the side channel. The legacy projection maps `privacy.redacted` → `redact_notice` (src/api/platform-conversation-legacy-adapter.ts:146-152), and `createLegacyChatStreamEventSink` — the function side chat uses at src/ipc/domains/sidechat.ts:118 — is documented at :156 as exactly "a compatibility sink for side chat's deliberately isolated loop". The sink type at the side call site IS `ConversationStreamEventSink`, the sanitizer's second parameter type.
- No stale-frame hazard: the notice frame is emitted without a streamId (adapter:147-152), and the side hook's guard only runs when `typeof event.streamId === "number"` (src/ui/renderer/hooks/use-side-chat.ts:133-146), so the notice cannot be adopted as a turn's first frame or dropped as a straggler.
- Both registrars run in the same main process, in the same `registerHandlers` call, against a loop that already exists at boot. Nothing needs lazy resolution beyond `deps.settingsService`, which is already in hand.

STRONGEST COUNTER-EVIDENCE (found, weighed, does not defeat the finding): the gap is DOCUMENTED as a deliberate deferral — docs/development/mcp-resources-policy.md:350-360 scopes the DLP boundary to "the main-chat provider boundary" and states side chat "currently has no DLP redaction, notice, or side-session audit correlation. Extending it needs an explicit side-channel and audit design rather than silently reusing main-chat behavior." That makes this a known deferral rather than unnoticed drift, but the two reasons it names are wiring work, not boundaries: the channel already carries the frame (above), and the audit-correlation constraint is a process-global — `initDlpAudit(deps.auditLogger, () => deps.conversationLoop.getSessionId())` binds DLP row attribution to the MAIN loop's session id (src/ipc/index.ts:90). Today side-chat DLP rows would be stamped with the main session id. That is a real defect to fix during the merge, resolvable by threading a session source or reusing the existing optional `turnId` parameter (src/audit/dlp-filter.ts:56,73-87) — not a lifecycle that cannot be lazily resolved. No test asserts side chat must not redact (no redact/dlp/privacy match in src/ipc/domains/__tests__/sidechat.test.ts).


## permission-config-broadcast-four-wirings  *(permission-relevant)*

**Capability** — Notify all renderer windows that permission config mutated (PERMISSIONS.configChanged fan-out)

**Wirings** — Canonical single point exists: `permissionManager.setBroadcastConfigChanged` (src/permissions/permission-manager.ts:623, field at :425), whose own doc at :420-423 says it exists so multi-window PermissionsTab refreshes "without each call site re-implementing the broadcast wiring"; wired at src/boot/steps/reviewer-permission-wiring.ts:196-198. Three boot sites re-implement it anyway, each hand-building `{ getMainWindow, getAppWindows: () => BrowserWindowValue.getAllWindows() }` and casting it with `as Parameters<typeof broadcastPermissionConfigChangedFromIpc>[0]`: src/boot/steps/reviewer-permission-wiring.ts:179, src/boot/steps/conversation-wiring.ts:322-325, src/boot/steps/plugin-tool-executor.ts:131-133. Target: src/ipc/domains/permissions.ts:196-202.

**Observed drift** — The ConversationLoop-dep variant (src/boot/steps/conversation-wiring.ts:322) is passed ONLY to the main loop. `SideChatConversationLoopDeps` (src/boot/conversation.ts:385-405) and `RoutineConversationLoopDeps` (:259-282) do not Pick `broadcastPermissionConfigChanged`, and neither `createSideChatConversationLoop` (:439-472) nor `createRoutineConversationLoop` (:330-360) passes it to `new ConversationLoop`. Every consumer is optional-call: src/engine/conversation-loop.ts:502 (`addSessionAdditionalDirectory`), src/engine/turn/commands.ts:293 (hook-trust slash), src/engine/turn/session.ts:170 (workspace-root revocation) — all three are silent no-ops on the side-chat and routine loops. This is the seed's `ExecuteOptions.callbacks` failure verbatim: one surface passes the callback, the other does not, and every report site is `?.()`. Not a trust boundary — src/boot/conversation.ts:378-383 states side chat shares the same permissionManager and approvalGate, so it mutates the same permission surface the PermissionsTab is displaying.

**Verifier** — CONFIRMED — and the drift is worse than claimed. (Read in worktree root C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks; paths below are relative to it.)

ONE CAPABILITY: src/ipc/domains/permissions.ts:196-202 `broadcastPermissionConfigChanged(deps)` — reads exactly two fields, `deps.getMainWindow?.()` and `deps.getAppWindows?.() ?? [mainWindow]`, then `sendToWindow(win, PERMISSIONS.configChanged, {})`. Every wiring calls this same function.

NO BOUNDARY. All four wirings are main-process boot steps importing the identical `BrowserWindow as BrowserWindowValue` from "electron" (reviewer-permission-wiring.ts:12, conversation-wiring.ts:15, plugin-tool-executor.ts:15) and the identical `getMainWindow` destructured off the same BootContext (context.ts:86/93/238; used at reviewer-permission-wiring.ts:42, conversation-wiring.ts:207, plugin-tool-executor.ts:62). No process/trust split. The one lifecycle note — plugin-tool-executor.ts:106-108 "created before ConversationLoop" — does not block: the broadcast is a lazy closure (`getMainWindow` is a getter, `getAllWindows()` fires at emit time). And boot steps already import from `../../main/` (conversation-wiring.ts:27, plugin-runtime.ts:19, audit-notification.ts:22), so nothing forces the hand-built stub.

DRIFT #1 (stronger than the candidate's, and I found it while trying to refute): the four stubs pass a DIFFERENT window set than every other caller. Real IpcDeps gets the curated getter — main.ts:315 → ipc/index.ts:72,81 → src/main/main-window.ts:371-383 `getAppWindows()` = mainWindow + settingsWindow + `getWindowManager().getDetachedWindows()`, deduped by id and destroyed-filtered. The four stubs instead pass raw `BrowserWindowValue.getAllWindows()` (reviewer-permission-wiring.ts:179, :197; conversation-wiring.ts:323; plugin-tool-executor.ts:132). The curation is deliberate and documented: window-manager.ts:817-829 states auth windows are NOT in `_children` and therefore "can never be reached" via `getDetachedWindows()`. `getAllWindows()` reaches them anyway — the OAuth window (main/auth-window-service.ts:542), external-link window (main/link-window-service.ts:244), and partition viewer (main/auth-partition-viewer-service.ts:239) are all top-level BrowserWindows. `sendToWindow` (ipc/safe-send.ts:28-35) checks only `isDestroyed()`, and `sendToWebContents` (ipc/safe-send.ts:7-26) does no origin check — it calls `webContents.send` unconditionally. So the same event fans out to a curated host-renderer set from permissions.ts:426/800/826 and to every window in the process, including one displaying a third-party OAuth page, from the four boot stubs. One side has a curation the other lacks: literal drift in the deciding data.

DRIFT #2 (the candidate's claim) — VERIFIED, including reachability, which is what would have refuted it. The dep is optional at engine/turn/types.ts:290; conversation.ts:495 passes it only in `createConversationLoop`; `SideChatConversationLoopDeps` (conversation.ts:385-405) and `RoutineConversationLoopDeps` (:259-282) do not Pick it and :439-472 / :330-360 never pass it. Side chat is not a different pipeline: ipc/domains/sidechat.ts:109+ → ipc/handlers/chat-stream.ts:120 `conversationLoop.runTurn(...)` — the SAME runTurn. From there: run-turn.ts:201 → conversation-loop.ts:943 → commands.ts:127 → handlePermissionCommand → commands.ts:293 `broadcastPermissionConfigChanged?.()`; and query-loop.ts:1237 `onSessionDirectoryGrant: (path) => self.addSessionAdditionalDirectory(path)` → conversation-loop.ts:502, same `?.()`. Side chat shares scriptHookManager (conversation.ts:450) and permissionManager/approvalGate (:447-448), per the doc at :379-383, so `/permission hooks accept` in the side chat mutates the SAME global trust state the PermissionsTab quarantine banner shows — silently. That this must broadcast is already asserted in-repo: permission-slash.test.ts:401-412 ("FU2 — slash `/permission hooks accept|disable|reject` must broadcast … so the renderer PermissionsTab quarantine banner live-refreshes"). Third site: ipc/domains/workspace.ts:375-390 explicitly fans `revokeWorkspaceRoot` to `deps.sideChatConversationLoop`, whose session.ts:170 broadcast is a no-op; if only the side loop held a directory under the removed root, no window is ever told.

REFUTATIONS I TRIED AND DROPPED: (a) "IpcDeps.getAppWindows is never populated, so IPC only reaches mainWindow" — false, main.ts:315 wires it. (b) "engine must not import ipc" — conversation-wiring.ts:319-321 gives that as the reason for the callback shape; it justifies dependency injection, not withholding the callback from two of three factories. (c) "side chat is a separate trust domain" — refuted by conversation.ts:379-383.


## permission-mode-approval-bypass  *(permission-relevant)*

**Capability** — "is this permission-mode change eligible to skip the ApprovalGate modal" — approval-bypass trust narrowing

**Wirings** — CLAIMED AUTHORITY: `resolveApprovalBypass` src/ipc/handlers/permissions.ts:80, whose own doc at permissions.ts:41-46 says "All narrowing from this loose shape into the strict, trust-checked PermissionModeApprovalBypass happens EXCLUSIVELY inside resolveApprovalBypass — that is the one auditable choke point where the fail-closed trust conditions live" and at :55-57 "This is the ONLY place the strict bypass is built". RE-DERIVATION: `hasTrustedBuiltInConfirmation` inline in `applyPermissionModeCommand`, src/permissions/permission-mode-apply.ts:78-84, gating the durable-change ask at permission-mode-apply.ts:86. SECOND PRODUCER that never passes through the claimed choke point: src/engine/turn/commands.ts:212-219 calls `applyPermissionModeCommand` directly with a hand-built `{source:"builtin-slash", trustOrigin:"user-keyboard", explicitUserAction:true}` literal.

**Observed drift** — ACTUAL — the re-derivation is strictly WEAKER. resolveApprovalBypass requires three conditions for the external surface: `source === "local-api-approval"` AND `isExternalOrigin(trustOrigin)` AND `explicitUserAction` (permissions.ts:81, 88-92). The consumer's copy at permission-mode-apply.ts:82-84 requires only `bypass.source === "local-api-approval"` (plus explicitUserAction) — the `trustOrigin` check is simply absent, so at runtime any trustOrigin value rides through. TypeScript's union type would normally catch that, but commands.ts:212 proves the function is called with caller-constructed literals rather than only with resolveApprovalBypass output, so the type is the only thing standing between a third caller and the gap. Separately, the doc at permissions.ts:66-67 describes the external branch as "(local-api / cli)" while `EXTERNAL_ORIGINS` (src/contract/trust-origin.ts:200) also contains `"plugin-frame"`, so `isExternalOrigin` admits a third origin the comment does not mention; whether any producer can supply that origin here is UNESTABLISHED.

**Verifier** — CONFIRMED by executing both deciders, not by reading alone.

ONE CAPABILITY, TWO DERIVATIONS. Both docs state the same question verbatim. src/ipc/handlers/permissions.ts:56-59 says resolveApprovalBypass decides what "lets applyPermissionModeCommand skip the in-app approval modal"; src/permissions/permission-mode-apply.ts:85 is literally `if (cmd.durable && !hasTrustedBuiltInConfirmation)` — the modal-skip branch. Same predicate, two files.

DRIFT IS ACTUAL AND RUNTIME-OBSERVABLE. src/ipc/handlers/permissions.ts:88-92 requires `source==="local-api-approval"` AND `isExternalOrigin(trustOrigin)` AND explicitUserAction. src/permissions/permission-mode-apply.ts:83 requires only `bypass.source === "local-api-approval"` — the isExternalOrigin branch has NO counterpart. One side has a branch the other lacks.

I ran both on identical inputs (temp vitest probe under the Electron runner, since node vitest is blocked by scripts/assert-electron-node-vitest.mjs; probe deleted, tree clean). With source="local-api-approval": trustOrigin="user-keyboard" → choke point REJECTS, consumer SKIPS THE MODAL and persists the durable mode change; same for "garbage-origin", "renderer", and undefined. 4 of 5 shapes diverge. Worse, all five wrote `confirmationSource:"local-api-approval"` into the permission audit chain, because permission-mode-apply.ts:132 reuses the SAME weaker predicate for forensic attribution — so the weak copy also decides what the audit chain records.

SECOND PRODUCER CONFIRMED, doc invariant already false. permissions.ts:56 claims "This is the ONLY place the strict bypass is built" and :76 "Must NEVER be called outside handleSetPermissionMode". src/engine/turn/commands.ts:230-238 calls applyPermissionModeCommand directly with a hand-built `{source:"builtin-slash", trustOrigin:"user-keyboard", explicitUserAction:true}` literal, never touching resolveApprovalBypass. The choke point is not a choke point.

SECOND, ALREADY-LIVE DRIFT IN THE SAME WIRING PAIR (this is the seed shape exactly). handleSetPermissionMode broadcasts PERMISSIONS.modeChanged to every app window (permissions.ts:106-112, called at :150); the renderer subscribes at src/preload/internal-api-surface.ts:939. The commands.ts wiring (:226-247) never broadcasts — it only fires the per-stream `callbacks?.onPermissionModeChanged?.` at :241 (src/ipc/handlers/chat-stream.ts:210). So a durable `/permission mode` via builtin-slash mutates persisted state without notifying other windows. Not hypothetical: this drift exists today.

REFUTATIONS ATTEMPTED AND DEFEATED.
(1) "Parse-don't-validate layering: parser at the untrusted edge, consumer trusts the type." Fails — permission-mode-apply.ts:78-82 does NOT trust the type. It re-checks explicitUserAction AND re-checks the settings-ui|builtin-slash → user-keyboard pairing, then drops only isExternalOrigin. A trust-the-type consumer would read `if (cmd.durable && !deps.approvalBypass)`. It is a partial, strictly weaker copy — the worst of both patterns.
(2) "Layering: src/engine/turn/ must not import src/ipc/handlers/." Real constraint, but it argues the predicate is in the WRONG PLACE (transport layer), not that it must exist twice. Relocating it beside the type it produces (src/permissions/) or into src/contract/ lets all three wirings share one authority. Not a process, trust, or lifecycle boundary.
(3) "The discriminated union at permission-mode-apply.ts:42-53 pins trustOrigin:ExternalOrigin for the local-api-approval variant, so the gap is unreachable." Strongest objection, and true that all three production callers pass conforming literals today. But the repo itself rejects this standard at this exact seam: src/api/local-api.ts:236-241 re-checks isExternalOrigin at runtime with the comment "The type already excludes `renderer`, but a bad cast at a JS call site could smuggle one in — reject anything outside the external-origin set." Applying that fail-closed rule at local-api.ts:239 and not at permission-mode-apply.ts:83 IS the inconsistency. The type is also routinely defeated in-repo via `as never` (permission-mode-apply.test.ts:62, :90).
(4) "Process boundary — local-api is out-of-process." No. src/api/http-server.ts:231 → api.dispatch → src/api/local-api.ts:189 → handleSetPermissionMode, all in the Electron main process.
(5) "Lifecycle that cannot be lazily resolved." No. Both deciders are pure functions over a plain struct; no injection, no async, no lifecycle.

HONEST SCOPE. The isExternalOrigin gap is a verified predicate divergence but is not exploitable by any current production caller — all three (domains/permissions.ts:300, local-api.ts:189, commands.ts:234) supply conforming shapes. The modeChanged-broadcast gap, by contrast, is live today. Both are the same root cause: one capability, three wirings, no shared authority.


## plugin-disable-loop-invalidation  *(permission-relevant)*

**Capability** — Invalidate a live ConversationLoop's carried-forward plugin scope when a plugin is disabled

**Wirings** — src/boot/steps/plugin-runtime/lifecycle.ts:85 (`onDisable`) and :94 (`onActiveStateChange` when !enabled) both call `lateBinding.conversationLoopRef.fn?.onPluginDisabled(pluginId)` — the SAME ref the seed reaches through. That ref is assigned exactly once, with the main loop only: src/boot/steps/conversation-wiring.ts:380. A second interactive loop is constructed at src/boot/steps/conversation-wiring.ts:356 and published at :377 (`ctx.sideChatConversationLoop`).

**Observed drift** — Direct in-repo contrast on the identical reach-in pattern: src/ipc/domains/workspace.ts:636-641 writes `workspaceRootLifecycle` into BOTH `deps.conversationLoop.deps` (:637) and `deps.sideChatConversationLoop.deps` (:640) — two loops. The plugin-disable path knows only one. `onPluginDisabled` (src/engine/conversation-loop.ts:260-262) prunes `lastTurnScope`, which is set on EVERY ConversationLoop instance by the shared turn path at src/engine/turn/run-turn.ts:500 and read back as the carried-forward active plugin set at src/engine/turn/tool-scope.ts:58-64. Side chat is explicitly documented as running "the full active plugin/tool set, exactly like the main chat" (src/boot/conversation.ts:382-383), so a disable (or disable-then-re-enable) leaves the plugin id sitting in the side-chat loop's `lastTurnScope`, silently re-scoping it without a fresh request_plugin. Same class also covers routine loops (src/boot/conversation.ts:330) and sub-agent child loops.

**Verifier** — CONFIRMED, but only one of the two cited arms is load-bearing — see correction.

ONE AUTHORITY: `lateBinding.conversationLoopRef` is typed as a SINGLE `ConversationLoop | null` (src/boot/steps/plugin-runtime.ts:112-114, init `{fn:null}` at :365) and has exactly three consumers in production code: src/boot/steps/plugin-runtime/lifecycle.ts:85, :94, and the seed's reach-in at src/boot/steps/plugin-tool-executor.ts:125. It is not a "main-chat feature handle" that happens to be main-only; it exists solely for cross-cutting reach-ins into "the live loop". It is assigned once, main loop only (src/boot/steps/conversation-wiring.ts:380), three lines after the side-chat peer is published (:377).

TWO DERIVATIONS OF THE SAME SET, ALREADY DRIFTED: src/ipc/domains/workspace.ts:636-641 derives "every live loop" for a cross-cutting mutation and enumerates BOTH (`deps.conversationLoop.deps` :637, `deps.sideChatConversationLoop.deps` :640). src/boot/steps/plugin-runtime/lifecycle.ts:85 derives the same set and gets {main}. Same repo, same pattern, different answer.

REFUTATION ATTEMPT 1 (strongest, and it kills HALF the candidate): src/engine/turn/tool-scope.ts:80-94 already applies a LIVE `pluginRuntime.isPluginEnabled(pluginId)` drop on every turn, for every loop. For the `onActiveStateChange` arm (lifecycle.ts:94) this fully compensates: src/plugins/runtime/index.ts:974 adds to `inactivePluginIds` BEFORE the callback fires at :1054, and `isPluginEnabled` (index.ts:811-812) reads exactly that set. The bypass at tool-scope.ts:88-89 (`sessionActivatedPluginIds`) cannot fire for side chat — side chat passes no `allowedPluginIds` (src/boot/conversation.ts:439-472; documented "not scope-isolated" at :381-383), so src/engine/turn/query-loop.ts:1045-1047 filters disabled ids out of `availablePluginIds` and src/engine/turn/plugin-expansion.ts:106-107 never records one. So lifecycle.ts:94 is belt-and-braces on both loops. CORRECTION to the candidate: only lifecycle.ts:85 carries the defect.

REFUTATION 1 FAILS for the `onDisable` arm. `onDisable` fires from teardown paths where the id is NOT in `inactivePluginIds`, so the live gate returns "enabled" and does not compensate: removePlugin deletes it from `inactivePluginIds` at src/plugins/runtime/runtime-lifecycle-capability-operations.ts:378 then fires onDisable at :383; disable adds to `disabledPluginIds` (a DIFFERENT set) at :1301 then fires at :1304; also src/plugins/runtime/runtime-state.ts:1191 (failClosedLoadedPlugin) and src/plugins/runtime/preparation.ts:197 (restart/reload stop phase) — contract documented at src/plugins/runtime/index.ts:284-298. For all of these, `ConversationLoop.onPluginDisabled` (src/engine/conversation-loop.ts:260-262, prunes `lastTurnScope` only) is the ONLY invalidation, and it reaches one loop.

DRIFT IS EXERCISABLE, NOT THEORETICAL: side chat runs the shared turn path (src/ipc/domains/sidechat.ts:126-135 → runStreamedTurn), so src/engine/turn/run-turn.ts:500 sets its `lastTurnScope`, read back as carried-forward scope at src/engine/turn/tool-scope.ts:58-64. Sequence: side chat earns scope via request_plugin (src/engine/turn/query-loop.ts:1034-1052) → user disables/reloads → main pruned, side chat not → re-enable/restart re-registers the tools → the next side-chat turn re-scopes that plugin with no fresh request_plugin, while main chat requires one. Only side-chat session reset clears it (src/engine/turn/session.ts:226, :303).

SEVERITY, STATED HONESTLY: this is scope-hygiene/context-shape drift, not a permission bypass. While the plugin is disabled, execution is still refused at Gate 4 (src/mcp/plugin-runtime-delegate.ts:132-140). The un-pruned scope only becomes live again after re-enable — at which point it was never re-earned.

REFUTATION 2 (lifecycle): "the side loop doesn't exist when the lifecycle callbacks are built." Refuted — the callbacks resolve through the mutable ref at FIRE time, which is the whole documented point of the module (src/boot/steps/plugin-runtime/lifecycle.ts:6-9, deref at :85). Both loops exist in the same function, same tick, same process (conversation-wiring.ts:356/377 vs :380). Lazily resolvable.

REFUTATION 3 (trust/process): none. Same main process; the same `toolRegistry`, `pluginRuntime`, `permissionManager`, `approvalGate` instances go to both loops (conversation-wiring.ts:299-337 vs :356-376), and conversation.ts:381-383 explicitly documents side chat as running the full active plugin set "exactly like the main chat".

SCOPE I WOULD DROP FROM THE MERGE: the candidate's "same class also covers routine loops and sub-agent child loops" is weaker and I do not endorse it. Those are constructed per-fire/per-spawn (src/boot/conversation.ts:284-360; SubAgentRunner at conversation-wiring.ts:470-505), hold no addressable handle, start with `lastTurnScope = null` (src/engine/conversation-loop.ts:155), and carry an allow-list gate (tool-scope.ts:65-74) — "not addressable because ephemeral" is a real lifecycle argument there. Separately noted, not part of this finding: `onPluginDisabled` never clears `sessionActivatedPluginIds` (conversation-loop.ts:260-262 vs the bypass at tool-scope.ts:87-92), which would matter for a routine loop live across a disable — whether any routine loop is actually live across a disable in practice is unestablished.


## plugin-execution-gate-parity  *(permission-relevant)*

**Capability** — Gate 4 — the runtime-state admission check "this plugin is registry-enabled OR session-activated for the calling session, and not manifest-integrity-disabled".

**Wirings** — src/mcp/plugin-runtime-delegate.ts:131-146 (loopback MCP `tools/call`) and src/plugins/runtime/index.ts:742-755 (`readUiResource`, whose comment at :718-721 explicitly calls itself "the same fail-closed gates `pluginRuntimeToolDelegate` applies" — i.e. a hand-copied duplicate). A third, differently-shaped derivation of the same predicate lives at src/engine/turn/tool-scope.ts:87-92 for per-turn model exposure.

**Observed drift** — Already diverged three ways. (a) The two "parity" copies read `pluginRuntime.isSessionActivated(...)` (src/plugins/runtime/index.ts:896); the tool-scope copy reads a completely different store, `state.sessionActivatedPluginIds` (src/engine/turn/tool-scope.ts:89). (b) Three sibling entry points on the same class apply NEITHER half of the gate: `callDeclaredAppOnlyTool` (src/plugins/runtime/index.ts:478-541), `callFromUi` (:553-602) and `callFromApp` (:656-703) go straight to `withPinnedGeneration` + `assertAppVisibleToolInvokable` with no `isPluginEnabled` and no `isSessionActivated` check — so a disabled plugin's app-visible tools are refused over loopback `tools/call` and refused a card body, but not refused on the panel/app IPC dispatch. (c) `manifestIntegrityState.isDisabled` has exactly two callers repo-wide (src/mcp/plugin-runtime-delegate.ts:141 and src/plugins/runtime/index.ts:751) — the same duplicated pair — so the integrity kill-switch does not cover the panel/app path either. (`ManifestIntegrityState` at src/permissions/manifest-integrity.ts:159-205 is itself a third `disabledPluginIds` set with no non-test clear path.)

**Verifier** — CONFIRMED for the two-copy core; the supporting drift claims are largely wrong and are corrected below. All paths under C:\Users\ikcha\workspace\lvis-project\lvis-app (I verified against the MAIN checkout, whose line numbers match the candidate; the worktree at .worktrees\plugin-callbacks has the same code at shifted lines).

ONE THING: C:\Users\ikcha\workspace\lvis-project\lvis-app\src\mcp\plugin-runtime-delegate.ts:131-146 and C:\Users\ikcha\workspace\lvis-project\lvis-app\src\plugins\runtime\index.ts:742-755 are the SAME predicate over the SAME state, expression for expression: `sessionContext.getStore()?.sessionId` → `!isPluginEnabled(id) && !(sessionId !== undefined && isSessionActivated(sessionId, id))` → refuse, then `manifestIntegrityState.isDisabled(id)` → refuse. Same process, same PluginRuntime object, same AsyncLocalStorage store (src/engine/session-context.ts), same process-wide singleton (src/permissions/manifest-integrity.ts:208). index.ts:718-721 self-declares it as a copy ("the same fail-closed gates `pluginRuntimeToolDelegate` applies"), and index.ts:739-741 repeats "Gate parity with pluginRuntimeToolDelegate (Gate 4)". Only the reporting shape differs (errorOutcome vs throw), which is caller-local.

CAN DRIFT: nothing but prose links them — no shared function, no shared constant, no test asserting equality of the two branches. Git shows the copy direction and the window: the delegate gate landed e06435de (2026-06-06), its session-activation half 23420ac0 + f0eb0018 (2026-06-29); readUiResource's copy landed 9cd04ba8 (2026-07-12), i.e. hand-written from the then-current original. The predicate family demonstrably drifts when copied (see correction 1).

MERGEABLE: no boundary is crossed. Both run in the Electron main process, both already call the same PluginRuntime instance methods, both read the same ALS store, neither is lazily-resolved or lifecycle-deferred. A single `PluginRuntime`-owned predicate (e.g. `assertRuntimeAdmitted(pluginId)` returning a typed refusal reason) called by both, with each site formatting its own error, is a mechanical extraction.

I could not find a legitimate reason for THIS split. The one substantive difference I looked for — different trust domain — does not exist: the loopback delegate and readUiResource are both reached from the same in-process loopback MCP server (tools/call vs resources/read).


## plugin-shell-frame-predicate  *(permission-relevant)*

**Capability** — Deciding whether an Electron frame is the plugin UI shell (`plugin-ui-shell.html`) — the predicate separating the plugin trust domain from the host renderer.

**Wirings** — FOUR independent spellings: (1) src/ipc/gated.ts:51 inside `validateHostRendererSender` (declared src/ipc/gated.ts:45): `url.protocol === "file:" && url.pathname.toLowerCase().endsWith("/plugin-ui-shell.html")` reject. (2) src/ipc/gated.ts:103 inside `validatePluginFrame` (declared src/ipc/gated.ts:95): `url.protocol !== "file:"` early-return then `pathname.endsWith("/plugin-ui-shell.html")` accept. (3) src/ipc/domains/ui.ts:195 inside `hostWindowForUiEvent` (declared src/ipc/domains/ui.ts:187, used at src/ipc/domains/ui.ts:265 and :283): a verbatim re-typing of #1's expression, reached via `validateSender` (src/ipc/domains/ui.ts:188) rather than by calling `validateHostRendererSender`. (4) src/main/webview-navigation-policy.ts:26: `input.currentUrl.includes("plugin-ui-shell.html")`.

**Observed drift** — #4 has already diverged in matching semantics from #1-#3: it is a substring test against the WHOLE url with no protocol check, no pathname extraction, and no case folding, where the other three anchor on `protocol === "file:"` and a `/`-prefixed pathname suffix. A url such as `file:///host.html?x=plugin-ui-shell.html` (or any http(s) page whose query carries the name) is classified as a plugin shell frame by #4 and as a host frame by #1-#3; conversely a mixed-case path is matched by #1/#2/#3 (they `.toLowerCase()`) and by #4 only by accident of the literal. #3 is a second, independent divergence axis: because it open-codes the predicate instead of calling `validateHostRendererSender`, it does not inherit that function's fail-closed-on-empty-frame-url rule (src/ipc/gated.ts:47-48, `if (!rawUrl) return false`) — it instead relies on `new URL("")` throwing into its catch at src/ipc/domains/ui.ts:198 — and any future change to what counts as a plugin frame (a second shell document, an `mcp-app:` scheme frame) lands in gated.ts and silently misses src/ipc/domains/ui.ts:195 and src/main/webview-navigation-policy.ts:26.

**Verifier** — HOLDS, and the codebase is stronger evidence than the candidate states — but two of the candidate's specific claims are wrong and are corrected below.

I tried to refute it on four axes and all four failed:

(1) PROCESS/TRUST BOUNDARY — none. All four sites are main-process. `src/ipc/domains/ui.ts:21` ALREADY imports from `../gated.js` (`auditUnauthorized, UNAUTHORIZED_FRAME, validateSender`), so for site #3 `validateHostRendererSender(event)` is a same-module, drop-in one-liner. `src/main/webview-navigation-policy.ts` is deliberately electron-free/pure, but `src/ipc/gated.ts:9` imports electron as `import type` only, so a shared pure `isPluginShellUrl(rawUrl: string): boolean` module is importable by both without dragging electron into the pure module. No lifecycle issue: every site already has the raw URL string in hand before it decides.

(2) IS #3 BEHAVIORALLY EQUIVALENT TO #1 (i.e. is the retype gratuitous)? Yes, gratuitously. `src/ipc/gated.ts:45-61` = validateSender && rawUrl non-empty && !pluginShell. `src/ipc/domains/ui.ts:187-204` = validateSender (188) then the open-coded plugin reject (195). Traced both edge cases: absent `senderFrame` → validateSender returns true (gated.ts:27), then ui.ts rawUrl="" → `new URL("")` throws → catch at ui.ts:198 → null; gated.ts:48 `if (!rawUrl) return false`. Same answer. Empty `senderFrame.url` → validateSender itself already returns false (gated.ts:30,35-37). Same answer. So substituting `validateHostRendererSender` at ui.ts:188 is behavior-preserving. There is no reason for the split.

(3) ARE #1 AND #2 REALLY THE SAME EXPRESSION? Yes, exactly complementary, same file. gated.ts:51 `url.protocol === "file:" && url.pathname.toLowerCase().endsWith("/plugin-ui-shell.html")` → deny; gated.ts:101-103 `protocol !== "file:"` early-return then the identical lowercased `/plugin-ui-shell.html` suffix → accept.

(4) THE STRONGEST EVIDENCE, WHICH THE CANDIDATE MISSED — the authority already exists and is a FIFTH derivation that does it correctly. `src/main/html-preview-partition.ts:37` computes `pluginShellHtmlPath = normalize(runtimeAssetPath("plugin-ui-shell.html"))` (resolver at `src/main/main-paths.ts:82-84`), and `isAllowedPluginShellFile` at `src/main/html-preview-partition.ts:242-250` decides by EXACT absolute-path equality (`normalize(fileURLToPath(url)) === pluginShellHtmlPath`). `src/preload.ts:35` independently MINTS the canonical shell URL the renderer mounts (`safeResolveFileUrl("plugin-ui-shell.html")`, preload.ts:27-33). So the host knows the one exact path of the one shell document, and sites #1-#4 all ignore it and re-derive the answer by string matching. That is "one authority in principle, N wirings in practice" in its purest form.

ACTUAL DRIFT ALREADY PRESENT (not hypothetical, not aesthetic): two layers guarding the SAME plugin-shell frame give DIFFERENT answers to "what may this frame load." `src/main/html-preview-partition.ts:242-250` permits exactly TWO file paths (plugin-ui-shell.html, plugin-ui-shell.js). `src/main/webview-navigation-policy.ts:27-41` permits ANY file under `resolve(distRoot, "src")`. The navigation policy is broader than the network gate that is supposed to be the same boundary.

UNTESTED SEAM: `src/main/__tests__/webview-navigation-policy.test.ts:56` exercises the look-alike case for `url` (`https://lvis-mcp-app.evil.example/` must still block), but there is NO equivalent test for `currentUrl` — lines 35 and 39 only ever pass a genuine shell URL. The substring gap at webview-navigation-policy.ts:26 is unguarded by tests.

MERGE CAUTION (must be stated, or the merge introduces a hole): match polarity is INVERTED across the sites. Matching means DENY at #1 (gated.ts:51-53) and #3 (ui.ts:195-197), but ALLOW at #2 (gated.ts:103) and #4 (webview-navigation-policy.ts:27). A consolidation that adopts the loosest existing spelling (#4's `includes`) would loosen the host-IPC denial at #1/#3. The merge must adopt the exact-path form already present at html-preview-partition.ts:242-250, or at minimum the gated.ts:51 form.


## plugin-skill-turn-scope  *(permission-relevant)*

**Capability** — Decide which plugin-owned surfaces the model may see and pull into context for the current turn

**Wirings** — Tools (the enforced arm): scope computed at src/engine/turn/tool-scope.ts:122-129, threaded as `permissionContext.allowedPluginIds` at src/engine/turn/query-loop.ts:1222, enforced as a Layer-0 deny at src/tools/invocation-runner.ts:1214-1219. Skills (three separate derivations): (a) prompt catalog filter — src/prompts/system-prompt-builder.ts:603-606; (b) `skill_list` — src/tools/skill-list.ts:14 (`store.listCatalogSync()`, unfiltered; the catalog includes every plugin skill, src/main/skill-store.ts:274-278); (c) `skill_load` admission — src/tools/skill-load.ts:164-171, whose only plugin gate is `acquirePluginGeneration`, wired at src/boot/steps/workflow-stores.ts:108-112 to `lifecycle.acquire(owner.pluginId)`.

**Observed drift** — The prompt-side filter's own comment (src/prompts/system-prompt-builder.ts:597-602) asserts the scoping "removes the case where the model sees (or loads) a skill that references Tools it currently cannot call" — but neither half holds. `skill_list` (a builtin, always eagerly exposed via boot/tools.ts:274) returns the same `listCatalogSync()` the prompt filtered, without the `activePluginIds` filter, handing the model the `plugin:<id>:<localId>` selector the catalog hid. `skill_load` then admits it: it holds a generation lease only, and never reads `activePluginIds`, `allowedPluginIds`, or `isPluginEnabled`. Concretely: a routine or sub-agent whose `allowedPluginIds` is narrowed to plugin A (tool-scope.ts:65-74) can `skill_load("plugin:B:x")` and get plugin B's skill body into its system prompt, while `callTool` for any of plugin B's tools is denied at invocation-runner.ts:1214-1219 with `permBlockPluginOutOfScope`. Same turn, same plugin, two different answers.

**Verifier** — CONFIRMED — I tried to refute this four ways and each attempt failed; the drift is real and is documented as unintended by the repo's own policy.

ONE CAPABILITY (not aesthetic similarity). The scope set is literally one object: `ToolScope.activePluginIds` built once per turn at src/engine/turn/tool-scope.ts:122-129. Two consumers read that same field — the prompt catalog filter (src/prompts/system-prompt-builder.ts:603-606, `this.toolScope?.activePluginIds`, set via src/engine/conversation-loop.ts:400) and the enforcement arm (src/engine/turn/query-loop.ts:1222 `allowedPluginIds: new Set(scope.activePluginIds)` → Layer-0 deny at src/tools/invocation-runner.ts:1214-1228). The intent that these are ONE authority is written down, not inferred: docs/development/skill-loading-policy.md:70-83 ("Catalog Scope Mirrors Tool Scope … This removes the case where the model sees skill metadata — or loads a skill body — that references Tools it currently cannot call") and its own Verification line 181-182 ("the same active-plugin scope drives both tool schemas and the skill catalog in one turn"). The host also already classes `skill_load` with `request_plugin`/`tool_search` as surface-expanding operations in one set (src/permissions/permission-manager.ts:65-74).

THE DRIFT IS MECHANICAL, NOT HYPOTHETICAL.
- `skill_list` (src/tools/skill-list.ts:13-15) is the entire execute: `store.listCatalogSync()`, no filter. That catalog emits every plugin skill (src/main/skill-store.ts:272-279) and each entry's `name` IS the loadable selector `plugin:<id>:<localId>` (src/main/skill-store.ts:138,154). It is a builtin, and builtins are unconditionally exposed whenever `includeBuiltins` is set (src/tools/registry.ts:558-569), which tool-scope.ts:126 hardcodes to `true` — so it is present on exactly the turns whose catalog was filtered (registered at src/boot/tools.ts:274).
- `skill_load` (src/tools/skill-load.ts:155-172) reads neither `activePluginIds` nor `allowedPluginIds` nor `isPluginEnabled`. Its only plugin gate is `acquirePluginGeneration` → src/boot/steps/workflow-stores.ts:108-112 → `lifecycle.acquire(pluginId)` → src/plugins/plugin-generation-coordinator.ts:120-146, which checks liveness only (`no active generation` / `admission has expired`). Nothing there knows about turn scope.
- The body then reaches the prompt: overlay register (skill-load.ts:260) → src/main/skill-overlay.ts:107-153 `buildSection`, which has no scope filter → rendered at system-prompt-builder.ts:668-679. And it lands in the SAME turn: query-loop.ts:1579-1587 rebuilds the system prompt whenever the round contained `skill_load`.
- Main chat sets no `forcedActivePluginIds` (only routines, boot/conversation.ts:322, and sub-agents, subagent-runner.ts:1776 do), so `activePluginIds` starts empty (tool-scope.ts:59-61). On turn 1 of any main chat the two arms give opposite answers for every installed plugin: tools = deny-all at Layer 0, skills = load-anything.

REFUTATIONS I ATTEMPTED AND REJECTED.
(1) "The approval modal is the real gate." It is a genuine mitigation for the FIRST load, but not a scope check and not equivalent: approval is persisted and hash-bound to the generation (approvalKey = pluginId|version|generationId|localId|fingerprint, skill-store.ts:158; checked skill-load.ts:191-194), so every later turn/session loads silently; the modal text names only the skill (skill-load.ts:217) and cannot convey that the plugin is out of this turn's tool scope. Decisively, the tools arm ALSO has downstream approval layers and still carries a separate Layer-0 scope deny (invocation-runner.ts:1214) — so "there is a user gate" is not treated as a substitute for scope on the arm that got it right.
(2) "`skill_list` is the deliberate escape hatch (policy §3:98)." Its documented purpose is token-budget overflow WITHIN the in-scope set — the builder computes `hiddenCount` from `inScope` (system-prompt-builder.ts:639) and the message says "call skill_list to inspect the full catalog" (line 655). Filtering skill_list to `activePluginIds` preserves that purpose exactly; today it instead hands back the global catalog.
(3) "Different lifecycle — the generation lease can't be resolved against a turn." The lease and the scope check are orthogonal concerns (liveness vs. exposure), so the merge ADDS a check rather than replacing one; policy §4:143 keeps the lease. No boundary is crossed.
(4) "The scope value isn't reachable from a tool's execute." Same process, same call stack: invocation-runner.ts:1214 holds `invocationPermissionContext.allowedPluginIds` at the moment it invokes the tool, and `ToolExecutionContext.metadata` is the documented plumbing bag (src/tools/types.ts:88-92,120) already used to thread `sessionId`, which skill_load reads (skill-load.ts:213,245). Lazily resolvable; no process/trust boundary.

LEGITIMATE BOUNDARIES THE MERGE MUST PRESERVE (not reasons to drop it): user-owned skills stay unscoped (policy §1 formula line 76; test at src/prompts/__tests__/system-prompt-builder.test.ts:273-287), and the generation lease stays.


## plugin-storage-audit-sink  *(permission-relevant)*

**Capability** — per-plugin sandboxed PluginStorage (containment-checked access to the plugin data dir), including its rejection reporting

**Wirings** — src/boot/steps/plugin-runtime/host-api-factory.ts:354 builds `createPluginStorage(pluginId, pluginDataDir, <bootAuditLogger sink>)` for HostApi.storage; src/plugins/runtime/index.ts:1256 (`PluginRuntime.getPluginStorage`, declared at :1252) builds `createPluginStorage(pluginId, pluginDataDir)` with NO third arg for the plugin-webview IPC bridge (consumers src/ipc/domains/plugins.ts:2573 storageGet, :2598 storageSet); src/plugins/runtime/sandbox.ts:116 builds a third instance, also without the sink. Definition + 3rd param: src/plugins/storage.ts:45, :48.

**Observed drift** — ACTUAL. Every containment rejection inside the storage engine reports through the optional `log?.` callback — src/plugins/storage.ts:79 (`rejected symlink escape`), :97 (`rejected dangling symlink`), :144 and :171 (`rejected escape attempt`). On the host wiring those four land in bootAuditLogger (host-api-factory.ts:354-363); on the webview wiring `log` is undefined so all four are no-ops, and the IPC handlers only convert the thrown PluginStorageError into a renderer reply (`return { ok:false, error }` at src/ipc/domains/plugins.ts:2579 and the identical line in the storageSet handler at :2598-2607) without any auditLogger write — the handlers audit only unauthorized senders (:2562, :2585). Same seed shape: identical engine, one call site passes the reporting callback, the other does not, so denials on the second path are silent. Effect instrumentation is NOT part of the drift: it is applied inside the factory at src/plugins/storage.ts:311, so both instances are instrumented.

**Verifier** — HOLDS. Paths below are relative to the worktree root C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks.

ONE CAPABILITY (refutation attempts failed):
- Same engine: both production wirings call the same `createPluginStorage` (src/plugins/storage.ts:45).
- Same root: host wiring receives `pluginDataDir` from `buildHostApiIncarnation` -> `this.createHostApi(pluginId, manifest, pluginDataDir, ...)` (src/plugins/runtime/runtime-state.ts:496-504); the webview wiring computes it at src/plugins/runtime/index.ts:1255 via `this.ensureDataDir`. Both resolve through the SAME `ensurePluginDataDir(pluginId, pluginRoot, this.pluginsRoot)` (src/plugins/runtime/runtime-state.ts:416-418, src/plugins/runtime/sandbox.ts:95-104). Identical directory, identical containment semantics.
- Same process/trust domain: the webview storage instance is built and used inside `ipcMain.handle` in the main process (src/ipc/domains/plugins.ts:2573, :2598). The webview is the caller across IPC, not the location of the instance. No process boundary is crossed by merging.
- Same sink already in hand: `PluginRuntime` declares `auditLog?: (level, message, data) => void` (src/plugins/runtime/index.ts:283) and uses it 20 lines below `getPluginStorage` at src/plugins/runtime/index.ts:1273. Boot wires that field from THE SAME `bootAuditLogger` the host wiring uses: src/boot/steps/plugin-runtime.ts:441-453 (`auditLog: (level, message, data) => { bootAuditLogger.log({...}) }`) vs src/boot/steps/plugin-runtime/host-api-factory.ts:354-363. So the third arg is lazily resolvable in-place — no new dependency, no lifecycle inversion. mergeable=true.

DRIFT IS ACTUAL (confirmed, with a magnitude correction — see correction field):
- The webview handlers never audit a containment rejection. Their catch blocks only reply to the renderer: src/ipc/domains/plugins.ts:2578-2579 and :2603-2604. `auditLogger` IS in scope in those exact closures (used at :2562 and :2587 for `auditUnauthorized`), so the omission is not a scoping constraint.
- No global safety net: no `ipcMain.handle` wrapper exists (grep for `ipcMain.handle =` returns nothing); every handler in src/ipc/domains/plugins.ts calls `ipcMain.handle` directly.
- The threat is live: plugin host code is dynamically imported into the main process (src/plugins/runtime/plugin-loader.ts:57 `await import(buildImportUrl(...))`), so a plugin has raw `node:fs` and can plant a symlink inside its own data dir. The same planted symlink is audited when hit via `hostApi.storage` and silent when hit via the webview bridge.
- No documented intent: nothing in src or docs marks the webview instance as deliberately unsinked; grep for `ui-storage` yields only the implementation sites and a docs line (docs/ko/architecture/architecture.md:3716). No test references `getPluginStorage`.

SECOND, LARGER DRIFT IN THE SAME CAPABILITY (not in the candidate, established not inferred):
The host wiring wraps storage in `enforceMutatingEffects` (src/boot/steps/plugin-runtime/host-api-factory.ts:352); the webview wiring gets the raw object. That wrapper DOES reach storage methods: it recurses into plain namespaces (src/permissions/effect-enforcement.ts:491-492) and `instrumentEffectsByPath` returns a plain object literal (src/permissions/hostapi-effect-recorder.ts:178, 227), so `isPlainNamespace` (src/permissions/hostapi-effect-recorder.ts:144-149) is true and recursion happens with prefix "storage". `storage.write/writeJson/rm/mkdir/writeEncrypted` are write-classified AND async (src/permissions/effect-kind.ts:301-313) and are absent from `ENFORCEMENT_EXCLUSIONS` (only config.set, agentApproval.respond, hostFetch — src/permissions/effect-enforcement.ts:108-126), so they land in `GATED_EFFECT_PATHS` (src/permissions/effect-enforcement.ts:152-163). Net: `storage.writeJson` passes the effect-boundary approval gate on the host path and bypasses it entirely on the webview path (src/ipc/domains/plugins.ts:2600). The enforcement completeness test partitions SOT *paths*, not *call sites* (src/permissions/effect-enforcement.ts:143-146), which is exactly why a second instance ships ungated without tripping CI. Blast radius is bounded (writes confined to `ui-storage/<key>.json`), and a per-write approval modal for UI state would be bad UX — so this may be intended — but it is undocumented, so it is drift, not a decision.

Absence of `enforceActiveHostApi` on the webview instance is NOT a finding: that path has its own liveness gate via `access.acquireExact(binding.pluginId, binding.generationId)` (src/ipc/domains/plugins.ts:2571, :2596).


## plugin-surface-directory-scope  *(permission-relevant)*

**Capability** — Deriving the effective allowed-directory set for one tool invocation, plus the turn/session grant sinks that widen it

**Wirings** — Engine implementation: src/engine/conversation-loop.ts:486-492 (`getTurnAdditionalDirectories` = persisted-or-static ∪ session ∪ turn), grant sinks at src/engine/conversation-loop.ts:469-484, installed per turn at src/engine/turn/query-loop.ts:1211-1222. Second, independent implementation: src/boot/plugin-surface-permissions.ts:28-60 (`sessionAdditionalDirectories` Map keyed by plugin subject, `getAdditionalDirectories` composed at :44-48, sinks at :54-59), constructed at src/boot/steps/plugin-tool-executor.ts:128-133 and passed per call at src/boot/steps/plugin-tool-executor.ts:545.

**Observed drift** — Revocation exists on only one side. src/ipc/domains/workspace.ts:375-381 hand-enumerates the live scope owners — conversationLoop, sideChatConversationLoop, routineEngine, getSubAgentRunner() — and calls `revokeWorkspaceRoot` on each (src/engine/turn/session.ts:105-173 strips session+turn dirs). `PluginSurfacePermissionScope` exposes only `createPermissionContext` (src/boot/plugin-surface-permissions.ts:9-14) — no revoke API, and its Map is not in the enumeration — so a session-scope grant taken on the plugin surface (reachable: origin "ui"/"mcp-app" is non-headless per src/boot/steps/plugin-tool-executor.ts:555, so src/tools/invocation-runner.ts:976-986 fires `onSessionDirectoryGrant`) survives removal of that workspace root for the app's lifetime. Second drift: the engine broadcasts permission-config-changed from inside the grant (src/engine/conversation-loop.ts:476) while the plugin copy needs an injected callback (src/boot/plugin-surface-permissions.ts:34, wired at src/boot/steps/plugin-tool-executor.ts:130-132). The per-plugin subject keying (src/boot/plugin-surface-permissions.ts:42) is the one genuinely different requirement; it does not require a second composition or a second revocation story.

**Verifier** — CONFIRMED — one capability, two compositions, and the drift is a live security gap, not aesthetics.

ONE THING (verified, not inferred):
- The consumer does not branch on surface. `createInvocationContext` reads the set once (src/tools/pipeline/invocation-context.ts:136-140); the widen/propagate logic is single-copy at src/tools/invocation-runner.ts:1049-1064 and :1074-1118.
- The contract is 4 fields on `ToolPermissionContext` (src/tools/executor-contract.ts:74-90). The plugin module literally defines itself as "the engine contract minus exactly those 4": `Omit<ToolPermissionContext, "additionalDirectories" | "getAdditionalDirectories" | "onTurnDirectoryGrant" | "onSessionDirectoryGrant">` (src/boot/plugin-surface-permissions.ts:4-7). It exists only to re-supply what the engine already supplies.
- The composition is the same 3-way union twice: src/engine/conversation-loop.ts:512-518 vs src/boot/plugin-surface-permissions.ts:44-48.
- There is no third store to appeal to: `allow --session` deliberately persists nothing — "caller owns live-scope mutation" (src/permissions/permission-slash.ts:171, 205-216). The sink IS the authority.

DRIFT 1 — revocation exists on one side only (confirmed, with a reachable path):
- Engine: src/engine/turn/session.ts:105-173 strips session+turn dirs by canonical prefix, rebinds the project, aborts the turn, broadcasts.
- Owners are hand-enumerated at src/ipc/domains/workspace.ts:375-381 and driven at :386-427. The same removal flow also prunes durable per-tool grants (:446-455) and routine scopes (:472-486) — it is designed as a total sweep.
- The plugin scope is not in that list and has no revoke to call: the interface exposes only `createPermissionContext` (src/boot/plugin-surface-permissions.ts:9-14), and the Map at :28 has no removal path. Whole-src grep finds exactly two non-test references (src/boot/steps/plugin-tool-executor.ts:129, :551), both construction/use.
- Reachable, end to end: origin "ui"/"mcp-app" ⇒ `headless: false` (src/boot/steps/plugin-tool-executor.ts:561) ⇒ approval branch taken (src/tools/invocation-runner.ts:810); no `remoteControllerAuthority` on that context (:551-571) so choices are unrestricted (invocation-runner.ts:813-819); the renderer offers session scope (src/ui/renderer/components/permissions/OutOfAllowedDirCard.tsx:188); invocation-runner.ts:1109 pushes it into the plugin Map. Both Layer-1 entry points reach it (:1143 shell, :1288 path fields).
- Consequence: after workspace-root removal the persisted portion shrinks (re-read fresh at plugin-surface-permissions.ts:45) but the session entry survives for the app's lifetime, keyed to that plugin, while chat/side-chat/routine/sub-agent all lose it.

DRIFT 2 — lifecycle reset also one-sided: engine clears session dirs on new/switch session (src/engine/turn/session.ts:211, :285) and reseeds with the project root (:65). The plugin Map never resets, and the subject key is the bare pluginId (src/boot/plugin-surface-permissions.ts:21-23), so a reinstall/new generation inherits the prior generation's grants. Whether that was intended is unestablished — no comment or test addresses it; the creating commit 4d60cc89 only fixed grants being dropped entirely.

DRIFT 3 — structural (no bug yet): broadcast is inside the engine sink (src/engine/conversation-loop.ts:502) but injected on the plugin side (src/boot/plugin-surface-permissions.ts:34, wired at src/boot/steps/plugin-tool-executor.ts:130-133); the engine has a static-array fallback (`?? this.deps.additionalDirectories`, conversation-loop.ts:514) the plugin lacks.

REFUTATIONS ATTEMPTED, ALL FAIL:
1. "Per-plugin isolation is a trust boundary." Real requirement (src/boot/__tests__/plugin-surface-permissions.test.ts:56-63), but it is subject keying, which one subject-keyed authority satisfies. The engine already runs N subjects — 4+ ConversationLoop instances (src/boot/conversation.ts:330, :439, :478; src/engine/subagent-runner.ts:2008, :3138) — through ONE implementation with per-instance state. Isolation never required a second composition, and it certainly never required a missing revoke.
2. "Different lifecycle: the plugin executor is built before the loop." Acknowledged in-code (src/boot/steps/plugin-tool-executor.ts:106-108) and already lazily resolved for another arg on the very same constructor (:125, via `lateBinding.conversationLoopRef`). Lazily resolvable ⇒ not a boundary.
3. "Different process/trust domain." Same main process, same `ToolExecutor` class, same `permissionManager`/`approvalGate` instances (destructured from ctx at src/boot/steps/plugin-tool-executor.ts:62; the same manager is what workspace.ts:446 prunes through). Durable Layer-3 grants are already single-authority and ARE swept; directories are the lone outlier.
4. "Maybe session dirs are also in settings, so removal covers them." Refuted at src/permissions/permission-slash.ts:205-216 (session branch returns without persisting).
5. "The engine's session bucket is semantically different (it carries the project root, session.ts:65)." True, but that is per-subject bucket CONTENTS, not a different composition or a different revoke rule.

MERGEABLE: yes. The shape is a subject-keyed scope registry — one composition, one canonical-prefix revoke, subjects = loop instances + plugin ids — with engine-only behaviors (project rebinding, turn abort, session reseed) staying layered above exactly as they are today in src/engine/turn/session.ts. Nothing crosses a process, trust, or unresolvable-lifecycle boundary.


## registry-manifest-path-trust  *(permission-relevant)*

**Capability** — Resolve a registry entry's `manifestPath` to an absolute path and decide whether it may be trusted/read

**Wirings** — 1. `assertCanonicalRegistryOwnership` — src/plugins/plugin-removal-transaction.ts:162-172 (exact equality with `<pluginsRoot>/<id>/plugin.json`). 2. `isTrustedRegistryManifestPath` — src/plugins/runtime/snapshots.ts:22-35, applied at src/plugins/runtime/snapshots.ts:76 (realpath containment; drops the row). 3. `PluginDeploymentGuard.isPathUnderUserInstalledDir` — src/plugins/deployment-guard.ts:139-146, applied at :62-68 (path.relative containment, no realpath). 4. `PluginUpdateDetector.readInstalledVersion` — src/plugins/update-detector.ts:117-129 (canonicalizeExistingPath + isWithin, throws). 5. `resolveManifestPathsFromRegistry` — src/plugins/registry.ts:406-416 (NO check), consumed by src/main/python-runtime.ts:382. 6. `PluginMarketplaceService.readInstalledManifestVersion` src/plugins/marketplace.ts:1950-1957 and `loadInstalledManifests` src/plugins/marketplace.ts:2158-2165 (NO check).

**Observed drift** — Four different containment semantics plus two wirings with none. Wiring 4 explicitly documents rejecting a crafted `"../../etc/passwd"` entry (update-detector.ts:123-129) while wiring 6 resolves and reads the same field with a bare `resolve(dirname(this.registryPath), entry.manifestPath)`. Wiring 5 is the sharpest: `resolveManifestPathsFromRegistry` filters only `pendingUpdate` (registry.ts:414) — it lacks the trust-root branch its sibling `resolveManifestLoadPlan` applies at snapshots.ts:76 — and python-runtime.ts:382 feeds each resulting path to `lockCandidatesFromManifest` (src/main/python-runtime.ts:335-365), which reads the manifest and derives lockfile candidates contained relative to `dirname(manifestPath)` — i.e. containment anchored to whatever directory the untrusted entry named. The chosen lockfile then drives the `uv` bootstrap, which runs BEFORE the ASRT sandbox is initialized by explicit design (python-runtime.ts comment above `runUv`). Wiring 1 proves a single canonical answer already exists in this codebase.

**Verifier** — CONFIRMED. I tried four refutations; all failed.

REFUTATION 1 — "readPluginRegistry already normalizes/rejects, so the downstream checks are redundant defense-in-depth." FAILED. `validateRegistryEntry` (src/plugins/registry.ts:251-295) validates `id` with `assertSafeArtifactSlug` (:263) but validates `manifestPath` ONLY as `typeof value.manifestPath !== "string" || value.manifestPath.length === 0` (:259-260). No containment, no normalization. `readPluginRegistry` (:138-185) calls `validateRawPluginRegistry` (:172) and `validatePluginRegistry` (:185), neither of which touches the path. A crafted `"../../etc/passwd"` survives the read path intact, so each consumer really is the sole decider.

REFUTATION 2 — "different anchors mean different intent (pluginsRoot vs dirname(registryPath))." FAILED — they are provably the same directory. `resolvePluginPaths` sets `registryPath: resolve(pluginsRoot, "registry.json")` (src/plugins/plugin-paths.ts:52), so `dirname(registryPath) === pluginsRoot` by construction. update-detector.ts:124-127 states this in its own comment ("beneath the registry directory (= pluginsRoot ...)"). The anchors are identical; only the predicates differ.

REFUTATION 3 — "a lifecycle boundary: python-runtime runs at boot and cannot get pluginsRoot." FAILED. src/main/python-runtime.ts:379 already calls `resolvePluginPaths()` and discards everything but `.registryPath`; that same return object carries `pluginsRoot` (plugin-paths.ts:46-56). The trust root is available for free at the exact call site, one property access away. No process boundary either — all six wirings are main-process Node (src/plugins/*, src/main/python-runtime.ts).

REFUTATION 4 — "some legitimate entries live outside pluginsRoot (e.g. local-dev), so the strict rule cannot be universal." FAILED. plugin-paths.ts:11-16 is explicit: "Directory where every plugin lives — `<pluginsRoot>/<id>/plugin.json`. `installSource` is metadata only; admin / user / local-dev entries all share this root (no physical user/managed split)." One canonical shape for every install source.

ACTUAL DRIFT (mechanical, not aesthetic):
(a) Verbatim-duplicated resolve idiom `isAbsolute(entry.manifestPath) ? ... : resolve(dirname(registryPath), entry.manifestPath)` appears at plugin-removal-transaction.ts:164-166, snapshots.ts:73-75, deployment-guard.ts:60-62, update-detector.ts:119-123, marketplace.ts:1951-1953, marketplace.ts:2159-2161 — six copies, then six different (or absent) trust decisions.
(b) Symlink handling splits: snapshots.ts:30-31 realpaths BOTH sides and fails closed on throw (:32-33); update-detector.ts:170-173 realpaths only if the path exists, else falls back to lexical; deployment-guard.ts:144-150 and plugin-removal-transaction.ts:164-168 do NO realpath at all. src/plugins/__tests__/trust-boundary.test.ts:150-174 plants a symlink under pluginsRoot pointing outside and comments "Without realpath defeat, this would naively pass the containment check" — that is a precise description of `PluginDeploymentGuard.isPathUnderUserInstalledDir`, which the SAME test file imports (:17) and exercises but never subjects to that case.
(c) Even the same-shaped helpers disagree on the self case: snapshots.ts:44 `rel === "" || rel === "."` → false; update-detector.ts:177 `rel === ""` → true; python-runtime.ts:105 `relativePath === ""` → true; deployment-guard.ts:146 → false. And argument order is inverted between `isWithin(base, target)` (update-detector.ts:175) and `isWithinDirectory(candidate, directory)` (python-runtime.ts:103).
(d) Wirings 5 and 6 have no check at all, confirmed by reading: registry.ts:406-416 filters only `pendingUpdate` (:414); marketplace.ts:1950-1963 and :2153-2174 resolve and `readFile` with nothing in between.

THE WRITE SIDE ALREADY ASSUMES A SINGLE READ-SIDE AUTHORITY. `toRegistryRelativeManifestPath` (plugin-paths.ts:69-80) documents at :66-67 that it "returns the absolute path with POSIX separators otherwise — runtime will reject those entries via the trust-root check." The normalizer's contract explicitly delegates rejection to a check that only ONE of the six read paths (snapshots.ts:76) actually performs. That is the candidate's shape stated in the codebase's own words.

WHY IT PERSISTS UNNOTICED: because the write path normalizes (plugin-paths.ts:69-80), legitimate registries never contain an escaping path, so the drift has zero functional effect in normal operation and only a security effect — exactly the condition under which divergent copies survive review.

SHARPEST CONSEQUENCE, with a caveat. registry.ts:406-416 has exactly one consumer, python-runtime.ts:382 (grep over src/ confirms), which feeds each path to `lockCandidatesFromManifest` (python-runtime.ts:336-366). That function anchors its containment to `path.dirname(manifestPath)` (:340, :354) — i.e. containment relative to whatever directory the registry entry named — and unconditionally pushes `path.join(manifestDir, lockFileName)` (:364). The selected lockfile then drives the uv bootstrap, and python-runtime.ts:420-428 confirms verbatim that runUv spawns "at boot BEFORE the ASRT sandbox is initialized ... they are NOT routed through the sandbox." CAVEAT — I did NOT establish who can write registry.json (whether a sandboxed plugin can reach it), nor that a crafted lockfile reaches code execution through uv. So "reads and acts on an attacker-named path pre-sandbox" is established; the full escalation is unestablished. The threat model itself is not my invention: update-detector.ts:124-129 names the `"../../etc/passwd"` attack explicitly, and deployment-guard.ts:71-85 treats the local user as the adversary.

NOT A CLEAN SUPERSET IN EITHER DIRECTION — which is itself the drift. Wiring 1 is lexically strictest (exact equality with `<pluginsRoot>/<id>/plugin.json`, plus `assertSafeArtifactSlug`, plugin-removal-transaction.ts:162-172) yet is symlink-blind; wiring 2 catches symlinks but is looser lexically. Neither dominates.


## reviewer-containment-copy-inert  *(permission-relevant)*

**Capability** — "Is this call's path argument inside the authorized directory scope?" — the containment predicate plus the path-argument extractor and dotted-field reader it needs.

**Wirings** — src/permissions/reviewer/risk-classifier.ts:477-482 `isInsideAllowed`, :454-466 `extractDeclaredPaths`, :431-439 `getDottedFieldValue` — drives real verdicts at :585 (auto-LOW "write inside owner plugin sandbox", :588) and :596 / :618 ("write outside allowed dirs" HIGH). || src/permissions/reviewer/host-risk-inspector.ts:683-688 `isInsideAllowed`, :652-666 `extractCallPaths`, :669-677 `getDottedFieldValue` — byte-identical private copies, used at :234 inside `inspectHostRisk`, the enforced-category authority (src/tools/pipeline/risk-classification.ts:62-68). || A third `getDottedFieldValue` at src/tools/pipeline/path-extraction.ts:48-56.

**Observed drift** — Two divergences already present. (1) The inspector's copy is INERT: host-risk-inspector.ts:234 computes `const escapes = paths.some((p) => !isInsideAllowed(p, signals.allowedDirectories))`, then line 235 returns "write" when it escapes and line 238 returns "write" when it does not. The containment answer is discarded — the extractor+predicate pair is maintained but load-bearing on nothing, so tightening one copy provably cannot reach the other. (2) The copies do not see the same inputs: risk-classifier.ts:576 keys its auto-LOW on `ctx.ownerPluginSandboxRoot` and canonicalizes it at :583, but `HostRiskSignals` (host-risk-inspector.ts:179-192) has no `ownerPluginSandboxRoot` field at all — a plugin write into its own `~/.lvis/plugins/<id>/` sandbox is "contained" to the classifier and unknown to the inspector. (3) Normalization differs from the third copy: both reviewer extractors run `caseFoldForMatch(canonicalizePathForMatch(value))` with realpath walk-up (risk-classifier.ts:461, host-risk-inspector.ts:661) while path-extraction.ts:64 does a bare `pathResolve` — no realpath, no case fold.

**Verifier** — VERIFIED as one capability with no defensible boundary, but the cost story is wrong in kind (see correction).

CAPABILITY IS ONE THING. Prefix-containment of a frozen-canonical path against a frozen-canonical scope list. FOUR implementations, all pure string compares, all in the main process, all in the permissions layer, all under the same "inputs are already canonicalized" contract — no process, trust, or lifecycle boundary separates them:
- src/permissions/allowed-directories.ts:164-182 `isPathAllowed` — the ENFORCED Layer-1 predicate (src/tools/invocation-runner.ts:1206-1211, `PermissionManager.checkPathScope`).
- src/permissions/reviewer/risk-classifier.ts:477-482 `isInsideAllowed` — LIVE; drives :585/:588 auto-LOW, :596 and :618 HIGH. Reached in production from src/permissions/permission-manager.ts:1482, :1511, :1541, :1563 and src/tools/pipeline/approval-memory-skip.ts:129.
- src/permissions/reviewer/host-risk-inspector.ts:683-688 — DEAD (below).
- `getDottedFieldValue` triplicated, bodies byte-identical: risk-classifier.ts:431-439, host-risk-inspector.ts:669-677, src/tools/pipeline/path-extraction.ts:48-56.
(Minor: "byte-identical" is loose — host-risk-inspector.ts:683 takes `readonly string[]` vs risk-classifier.ts:477 `string[]`, and :652 `extractCallPaths(input, pathFields)` vs :454 `extractDeclaredPaths(ctx)`. Bodies are line-for-line the same.)

CLAIM (1) CONFIRMED AND STRONGER THAN STATED — but it is not drift. host-risk-inspector.ts:230-242: paths present + escaping → "write" (:235); paths present + contained → "write" (:238); no paths at all → "write" (:242). The ENTIRE filesystem branch is inert, not just the containment answer, so `extractCallPaths` (:652) and `getDottedFieldValue` (:669) are dead as well — while still paying `realpathSync` walk-up I/O per path argument on every plugin/MCP invocation (:662 → sensitive-paths.ts:139-181). `git blame -L 226,243` attributes BOTH :235 and :238 to f1dedee1e (2026-06-22): the branch was born collapsed, it is not a live branch that decayed.

CLAIM (2) TRUE BUT DERIVATIVE. `HostRiskSignals` (host-risk-inspector.ts:179-193) indeed has no `ownerPluginSandboxRoot`, vs risk-classifier.ts:576/:583. But that follows from the inspector never classifying down (documented default-strict, :206), so it is a consequence of inertness, not independent evidence of drift.

CLAIM (3) CONFIRMED AND UNDERSTATED. path-extraction.ts:58-65 `resolveToolPathForPermission` expands `~` and resolves against the tool's `executionCwd` (invocation-runner.ts:1193); the reviewer side calls `canonicalizePathForMatch` (sensitive-paths.ts:140) which does `pathResolve(rawPath)` against process.cwd() with NO tilde expansion. Identical argument strings therefore yield different absolute paths in reviewer vs enforcement for relative args and for `~/...` args.

LIVE-vs-LIVE DRIFT THE CANDIDATE MISSED. risk-classifier.ts:477-482 is a fourth copy of the exported authority isPathAllowed (allowed-directories.ts:164-182) minus two guards: `if (!dir) continue` (:171) and the win32 separator fallback (:179). Without :171, an empty scope entry makes `path.startsWith("" + "/")` true for every POSIX-absolute path, so the reviewer would report "contained" and suppress the :596 HIGH. Reachability of an empty entry is UNESTABLISHED — sanitizeAllowedDirectoryEntries (allowed-directories.ts:135) drops empty strings, and the reviewer ctx takes `input.allowedDirectories` verbatim (permission-manager.ts:1435), but I did not trace every producer of that input.


## sensitive-path-two-denylists  *(permission-relevant)*

**Capability** — "which filesystem paths are secret and must never be read" — the sensitive-path hard-block set

**Wirings** — AUTHORITY A (host tool layer, glob patterns): src/permissions/sensitive-paths.ts:55 `SENSITIVE_PATH_PATTERNS`, consumed via `isSensitivePath` (src/permissions/sensitive-paths.ts:228) by approval-gate.ts:782, permission-manager.ts:553, file-read-core.ts:82, file-tools.ts:716, shell-path-policy.ts:77/91/134, allowed-directories.ts:96/144/218. AUTHORITY B (OS sandbox layer, literal absolute paths): src/permissions/asrt-sandbox.ts:525 `getDefaultSensitiveReadDenyPaths`, consumed by asrt-sandbox.ts:633 (write floor), asrt-sandbox.ts:740 (runtime config), mcp/mcp-client.ts:1767, main/terminal/pty-manager.ts:252, permissions/worker-spawn.ts:74.

**Observed drift** — BIDIRECTIONAL AND ALREADY REAL. The code itself admits the obligation: src/permissions/asrt-sandbox.ts:556 says "FIX 3: drift-sync with SENSITIVE_PATH_PATTERNS (src/permissions/sensitive-paths.ts). Both lists must be kept in sync. When adding to either, mirror it here." It has not held. In B but NOT in A (readable by host tools once the containing dir is granted, since Layer 0 never fires): `~/.git-credentials` (asrt-sandbox.ts:577, a plaintext credential store), `~/.gitconfig` (:576), `~/.config/gh` (:569, GitHub CLI OAuth token), `~/.config/git` (:570), `~/.lvis/routine` (:548, routine session history), `~/.lvis/plugins/auth-partitions.json` (:555), the Electron userData dir (:563, holds plugin OAuth cookies/tokens). In A but NOT in B (denied to host tools, readable by any sandboxed shell/worker): `/etc/shadow`+`/etc/sudoers`+`/etc/passwd-` (sensitive-paths.ts:68-70), `**/.config/lvis/hooks/**` (:108, labelled "hook supply-chain protection"), `**/.python_history` (:76), `**/.psql_history` (:77), `**/.viminfo` (:78). The glob-vs-literal argument (asrt-sandbox.ts:508-512: bwrap cannot glob) explains the `**/.env`/`**/id_rsa` generic entries staying out of B, but every path listed above is a LITERAL home- or root-relative path expressible on both sides — the constraint does not cover them. Two divergences ARE documented as intentional (Library/Cookies, Library/Keychains at asrt-sandbox.ts:503-506) and I excluded those.

**Verifier** — CONFIRMED by running the deciding code, not by reading. Both preconditions hold.

ONE CAPABILITY: the code names itself the authority twice. sensitive-paths.ts:10-13 ("Hard-blocklist of filesystem patterns that should NEVER be read/written by agent tools") and asrt-sandbox.ts:449 ("The SINGLE SOURCE OF TRUTH for the host-secret / sensitive read deny-list"). asrt-sandbox.ts:556-557 states the obligation explicitly: "FIX 3: drift-sync with SENSITIVE_PATH_PATTERNS ... Both lists must be kept in sync. When adding to either, mirror it here." Neither side is a consumer of the other; the drift-sync is hand-maintained prose. Both are single-authority WITHIN their own layer (worker-spawn.ts:311,463; pty-manager.ts:252; mcp-client.ts:1767 all call B's function; approval-gate.ts:1191, permission-manager.ts:587, file-read-core.ts:82, file-tools.ts:138, allowed-directories.ts:96,144,218 all call A's) — so it is exactly two authorities, not N.

DRIFT IS REAL AND EXPLOITABLE AT DEFAULT CONFIG — proven by execution, not inference. I ran the actual host guard `assertReadableFilePath` (file-read-core.ts:62) with `buildRuntimeAllowedDirectories(undefined, cwd)` (allowed-directories.ts:339) and no user additions, against a real temp ~/.lvis:
  host=ALLOWED   sandbox=DENIED   <lvis>/routine/sess.jsonl
  host=ALLOWED   sandbox=DENIED   <lvis>/plugins/auth-partitions.json
  host=DENIED    sandbox=DENIED   <lvis>/secrets/k.key
Same paths, opposite verdicts. The reason it is reachable with zero user action: `lvisHome()` is itself a DEFAULT runtime allowed root (allowed-directories.ts:15-17, :67, :85 `const candidates = [cwd, lvisHome()]`), and `~/.lvis` bare is not sensitive under A, so Layer 1 grants it and Layer 0 never fires on the routine/auth-partitions children. Direct `isSensitivePath` runs also return null for ~/.git-credentials, ~/.gitconfig, ~/.config/gh/hosts.yml, ~/.config/git/credentials, and the Electron userData dir — all of which B denies (asrt-sandbox.ts:563, 569-570, 576-577).

MERGEABLE: no process, trust, or lifecycle boundary blocks a shared authority. Both modules are main-process TS in one module graph; asrt-sandbox.ts:530 explicitly avoids any `electron` import and sensitive-paths.ts imports only node:fs/node:path/glob-matcher, so neither constrains the other. allowed-directories.ts already imports sensitive-paths.ts AND lvis-home.ts (:27-32), so a module importing both anchors is precedented. The lifecycle difference is lazily resolvable by construction: B already re-resolves `homedir()`/`lvisHome()`/`XDG_CONFIG_HOME`/`userDataDir` per call (asrt-sandbox.ts:525-543 — and the test at :722-729 mutates LVIS_HOME between cases, so it must stay a function), while A is anchor-free `**/`-prefixed. A shared table of (anchor, segments, flags) projects to both without freezing anything.

I attempted four refutations and none survives as a reason for TWO lists (each survives only as a reason for per-entry FLAGS in one table): (1) glob-vs-literal (asrt-sandbox.ts:504-508, bwrap cannot glob) legitimately keeps `**/.env`, `**/id_rsa*`, `**/Login Data` out of B — but every drifted entry above is already a literal on B's side, so the constraint does not cover them; (2) over-deny safety (asrt-sandbox.ts:491-502, test :794-804) legitimately makes the sets unequal, since a B deny breaks a whole toolchain while an A deny costs one refused call — but nothing legitimate requires the host agent to read auth-partitions.json; (3) read-vs-write asymmetry is real (B splits :525 read floor / :631 write floor, A uses one set for both) and (4) A conflates secrecy with integrity (audit "self-tampering" :103-106, hooks "supply-chain protection" :108) — both are shape differences a flagged table expresses, not trust boundaries. The two enforcement POINTS (in-process JS predicate vs ASRT seatbelt/bwrap/ACL config) must both survive; only the data table merges.


## shell-structural-command-deny-two-wirings  *(permission-relevant)*

**Capability** — structural (pre-execution) deny of a dangerous shell command string for the two builtin shell tools

**Wirings** — AUTHORITY A (host pipeline stage, pre-approval): `BashAstValidator.validate` invoked as "Step 2.5" at src/tools/invocation-runner.ts:1177-1191; the validator itself is src/main/bash-ast-validator.ts:27, gated on tool name by `_isBashTool` at src/main/bash-ast-validator.ts:188-190 (`/^(bash|shell|exec|run_command|terminal)/i`). Constructed once at src/boot/services.ts:109 and threaded into both executors (src/engine/conversation-loop.ts:230, src/boot/steps/plugin-tool-executor.ts:113).
AUTHORITY B (inside the tool, post-approval): `validatePowerShellCommand` at src/tools/powershell.ts:245-248 (impl at src/tools/powershell.ts:288). Both tools are treated as one class elsewhere — src/permissions/sandbox-capability.ts:222 `ASRT_WRAPPED_SHELL_TOOLS = new Set(["bash", "powershell"])`.

**Observed drift** — `_isBashTool`'s regex is `^`-anchored, so "powershell" does NOT match — Authority A never runs for the powershell tool, and Authority B never runs for bash. The two wirings sit at DIFFERENT points in the permission ladder, and the powershell one sits after the one-shot host-shell permit is consumed: src/tools/powershell.ts:223-244 calls `consumeHostShellExecutionPermit` and only then does src/tools/powershell.ts:245 run the AST preflight. So a requested-sandbox-fallback command that the AST gate rejects has already burned the user's allow-once permit, with no refund path. The audit shape also differs: Authority A's deny writes a permission audit row with `layer: 0` and `denyReasons` and fires `callbacks.onToolEnd` (src/tools/invocation-runner.ts:1181-1186), while Authority B returns a plain tool error carrying only `metadata: { preflightDenied: true }` (src/tools/powershell.ts:247). The surrounding gate ladder is otherwise a near-verbatim copy between src/tools/bash.ts:150-232 and src/tools/powershell.ts:189-270 (same `validateShellWorkingDirectory` → `validateShellCommandPathPolicy` → `isIssuedHostShellExecutionPlan` → `requiresExplicitHostShellFallbackApproval` → `mode==="blocked"` → `deriveSandboxWritePaths` sequence).

**Verifier** — CONFIRMED — one capability (structural pre-execution deny of a dangerous shell command string), two wirings at different ladder positions, with git-attributable drift.

REFUTATION ATTEMPTS THAT FAILED:
1. "Different trust/process domain" — no. Both run in the main process on the same model-supplied `finalInput`. Authority A is boot-constructed and injected (src/boot/services.ts:109 `new BashAstValidator({mode:"deny"})`, threaded at src/engine/conversation-loop.ts:230 and src/boot/steps/plugin-tool-executor.ts:113); Authority B is a free async function with an injectable parser (src/tools/powershell.ts:290-293). Its PowerShell child (src/tools/powershell.ts:412-450) only calls `Parser::ParseInput` (src/tools/powershell.ts:477) under `buildSafeChildEnv()` (src/tools/powershell.ts:420) — parse, never execute. Not a boundary.
2. "Lifecycle that cannot be lazily resolved" — no. Step 2.5 already sits in an async body that awaits on the very next line (src/tools/invocation-runner.ts:1179 → await at :1185), so hoisting the async PowerShell AST call needs no new lifecycle.
3. "The rule sets are language-specific, so two authorities are correct" — this is the ONLY partly-valid objection and it does not save the split. POSIX regexes (src/main/bash-ast-validator.ts:41-115) are meaningless for PowerShell and the cmdlet blocklist (src/tools/powershell.ts:101-122) is meaningless for bash, so the ANALYZERS must stay separate. But that argues for one dispatch STAGE with two dialect backends, not two ladder positions — and the runner has already computed the authoritative dialect discriminator by canonical-instance identity 480 lines earlier: src/tools/invocation-runner.ts:696-703 (`isCanonicalBashTool` → "bash", `isCanonicalPowerShellTool` → "powershell"). It then throws that away and lets Authority A re-derive "is this a shell tool" from a name regex (src/main/bash-ast-validator.ts:188-190, `/^(bash|shell|exec|run_command|terminal)/i`) that yields a different answer set. That is two derivations of one predicate inside a single function.

EVIDENCE OF ACTUAL DRIFT (not aesthetic):
(a) Git-attributable reordering. Before commit 532e4a09 "fix(sandbox): seal host shell fallback plans", `validatePowerShellCommand` ran FIRST in `executeTyped`, right after the path-policy checks. That commit inserted `consumeHostShellExecutionPermit` ABOVE it, so the structural deny now runs after the permit is burned (current: consume at src/tools/powershell.ts:222-245, preflight at :246-249). The SAME commit left bash's in-tool preflight above its permit consume (`preflightInteractiveCommand` at src/tools/bash.ts:140-147, consume at :183-206). Two near-verbatim ladders, opposite sides of the permit burn, from one commit, with no comment or test justifying the asymmetry.
(b) The burn is irreversible and unrefundable: `consumeHostShellExecutionPermit` deletes the record before comparison (src/permissions/host-shell-execution-permit.ts:170) and re-minting requires a fresh HMAC-verified allow-once receipt (`mintHostShellExecutionPermit`, src/permissions/host-shell-execution-permit.ts:118-149). So on the requested-sandbox fallback path the user is shown an approval modal for a PowerShell command the host was always going to structurally refuse — impossible for bash, whose structural deny (Step 2.5, src/tools/invocation-runner.ts:1173-1191) fires before any approval or permit minting (binding built at :1308-1315).
(c) The exclusion is unasserted. src/main/__tests__/bash-ast-validator.test.ts contains zero occurrences of "powershell"; its "non-bash tool → allow" cases are file_read/knowledge_search/agent_list (lines 257-274) and its name-matching cases are shell/run_command/terminal/exec (lines 306-328). Nothing pins that powershell must be excluded, and nothing pins that a plugin tool whose name starts with bash/shell/exec/terminal must NOT get POSIX regexes applied to a non-POSIX command string.
(d) Third derivation of the same "which tools are shell tools" concept: `ASRT_WRAPPED_SHELL_TOOLS = new Set(["bash","powershell"])` at src/permissions/sandbox-capability.ts:222 — exactly the pair, by name, disagreeing with both the regex and the instance-identity check.

The capability is live, not dead code: src/boot/services.ts:177 registers `new PowerShellTool()`.

MERGE SHAPE (mergeable=true): one structural-preflight stage at Step 2.5 that selects the dialect analyzer from the discriminator already computed at src/tools/invocation-runner.ts:696-703, keeping the two rule sets separate. Accepted cost to state honestly: the PowerShell parser child would then spawn pre-approval, i.e. also for calls the user subsequently denies.


## subagent-prompt-scope-two-authorities  *(permission-relevant)*

**Capability** — What a sub-agent's system prompt is allowed to advertise (its tool/plugin surface) — should be bounded by exactly one thing, the scoped ToolRegistry the child is frozen with.

**Wirings** — (1) The boot factory closes the prompt's plugin-card reader over the GLOBAL registry: src/boot/conversation.ts:85 (`getPluginCards: () => pluginRuntime.listPluginCards(toolRegistry)`, where `toolRegistry` is `opts.toolRegistry`, wired from the global one at src/boot.ts:557-559). (2) The sub-agent narrows the registry: src/engine/subagent-runner.ts:1770 (`createScopedView(baseToolNames)`) and hands it to the builder at src/engine/subagent-runner.ts:1815-1818 via `parentPromptBuilder.createIsolated({memoryManager, toolRegistry: scopedRegistry})`. (3) A third, hand-rolled arm when the duck-type check at src/engine/subagent-runner.ts:1814 fails: src/engine/subagent-runner.ts:1820-1834 builds `new SystemPromptBuilder({memoryManager: <6-method shim cast `as MemoryManager` at :1834>, toolRegistry: scopedRegistry})` with no plugin-card / skills / app-context readers at all.

**Observed drift** — CONFIRMED and load-bearing. `createIsolated` is `new SystemPromptBuilder({...this.deps, ...overrides})` (src/prompts/system-prompt-builder.ts:218-221) — it replaces `deps.toolRegistry`, but `deps.getPluginCards` is an ALREADY-CLOSED closure over the boot-time global registry (src/boot/conversation.ts:85), so the override never reaches it. `listPluginCards` genuinely filters on the registry it is passed (src/plugins/runtime/index.ts:1129-1131 builds `visibleNames` from `toolRegistry.getModelVisibleTools()` and feeds it to `buildPluginCard` at :1155), so the child's prompt lists plugin tools computed from the FULL registry while its executable surface is `scopedRegistry`. The same inheritance carries `getAvailableSkills`, `getAppModelContext` (MCP-app card slots) and `mcpServerGuidanceProvider` (every connected MCP server's `instructions`) from src/boot.ts:561-575 into a scoped sub-agent. Meanwhile the loop is explicitly denied the runtime — `pluginRuntime: undefined` at src/engine/subagent-runner.ts:1854 — but the prompt builder holds its own captured `pluginRuntime` reference, which is exactly the reach-back. Arm (3) produces a THIRD prompt shape (no cards, no skills, no app context) from the same spawn.

**Verifier** — CONFIRMED, and I reproduced the drift by running the deciding code.

ONE capability, TWO authorities. A sub-agent's advertised surface should be bounded by the registry it is frozen with. Enforcement is: `scopedRegistry = this.deps.toolRegistry.createScopedView(baseToolNames)` (src/engine/subagent-runner.ts:1770), which builds a NEW `ToolRegistry` containing only allowed tools (src/tools/registry.ts:672-679), handed to the child loop (src/engine/subagent-runner.ts:1839) and to the builder (:1815-1818). Advertisement is: `SystemPromptBuilder.createIsolated` = `new SystemPromptBuilder({ ...this.deps, ...overrides })` (src/prompts/system-prompt-builder.ts:218-221) where overrides are only `memoryManager | toolRegistry` — so `deps.getPluginCards`, an ALREADY-CLOSED closure `() => pluginRuntime.listPluginCards(toolRegistry)` (src/boot/conversation.ts:85), survives the override and keeps reading the global registry.

Identity of the two registries is established, not inferred: boot creates one `toolRegistry` and stores it (src/boot.ts:187), passes that same variable into `createSystemPromptBuilder` (src/boot.ts:557-559); `wireConversation` destructures BOTH `systemPromptBuilder` and `toolRegistry` from the same BootContext (src/boot/steps/conversation-wiring.ts:178-183) and hands the registry to SubAgentRunner as `parentDeps.toolRegistry` (:476). So `createScopedView`'s receiver IS the object the card closure captured.

The registry argument is load-bearing, not decorative: `listPluginCards` derives `visibleNames` from `toolRegistry.getModelVisibleTools()` (src/plugins/runtime/index.ts:1129-1131) and `buildPluginCard` filters the manifest's tools through it (src/plugins/runtime/cards.ts:34-38); a card with zero surviving tools is dropped by `if (card.sampleTools.length === 0) return false` (src/prompts/system-prompt-builder.ts:44), so the section (id 65, :816-836) renders or not purely as a function of WHICH registry was passed.

EMPIRICAL (tsx, real SystemPromptBuilder + real ToolRegistry + real buildPluginCard): parent builder with a global-registry card closure, `createIsolated({toolRegistry: global.createScopedView(["read_file"])})`, child `build()` emits
  `- **com.example.secret** — Secret Plugin: does secret things: secret_tool, secret_tool2`
while `scoped.getModelVisibleTools()` = `['read_file']`. The identical builder with the closure over the SCOPED registry emits no such line (section drops). Same spawn, two answers.

Reachability in production: scoped children are the normal case — `input.sourceTools` from agent_spawn/profile (src/engine/subagent-runner.ts:1996-2000), the A2A wire path with `forceExplicitToolScope` and `binding.profile.sourceTools` (:1907), and resume, which ALWAYS passes a non-null frozen list (:1726-1730, :3125).

Cost is proven by an actual contradiction, exactly like the seed: the catalog header is literally "Available Plugins (not selected for this turn — select via request_plugin)" (src/i18n/messages/generated/be_systemPromptBuilder.ts:46), but the child is explicitly denied the runtime — `pluginRuntime: undefined // Sub-agent does not request_plugin` (src/engine/subagent-runner.ts:1852-1853) — and the executor computes `availablePluginIds: self.deps.pluginRuntime?.listPluginIds() ?? []` (src/engine/turn/query-loop.ts:1039-1040), i.e. the empty list, so every request_plugin returns "unknown pluginId … none available" (src/engine/turn/plugin-expansion.ts:71-79). The prompt advertises an affordance the enforcement path unconditionally refuses, and leaks the parent's installed-plugin inventory + tool names into an A2A-ingress child's context.

Refutation attempts that FAILED: (a) "the child scopes it later" — no, the section's only other filter is `toolScope.activePluginIds` (:823), which for a child is `forcedActivePluginIds` derived from the SCOPED tools (subagent-runner.ts:1776-1782), so out-of-scope plugins are precisely the ones that pass the filter and get printed; (b) "it's a lifecycle/trust boundary" — no: same process, same main-process lifecycle, and `this.deps.toolRegistry` is in scope at the call site, so a one-line signature change (`getPluginCards?: (registry: ToolRegistry) => Card[]`, builder passes `this.deps.toolRegistry`) collapses it to one authority with no laziness problem; (c) "the tool-schema section drifts too" — no, that one correctly reads the overridden `toolRegistry` (:762-765), which is what makes the divergence a genuine two-authority split rather than a uniformly-stale builder.


## tilde-expansion-permission-vs-tool  *(permission-relevant)*

**Capability** — Expand a leading `~` in a path argument to the user's home directory, before the path is resolved/canonicalized. This one string decides which file the permission layer judges AND which file the tool opens; the two must be the same string.

**Wirings** — src/tools/pipeline/path-extraction.ts:58-65 `resolveToolPathForPermission` — handles `~`, `~/`, AND `~\` (line 61). PERMISSION-side answer: called from `extractTargetFilePaths` (path-extraction.ts:39), whose output is canonicalized at src/tools/invocation-runner.ts:1197-1200 and fed to `PermissionManager.checkPathScope` (invocation-runner.ts:1207-1210) for the Layer-0 sensitive hit and Layer-1 allowed-dir check. || src/tools/file-read-core.ts:31-35 `expandTilde` — `~` and `~/` ONLY. TOOL-side answer: file-read-core.ts:68 (`assertReadableFilePath`), re-exported into src/tools/file-tools.ts:29 → file-tools.ts:129 (`FileTool.resolvePath`, the path actually opened) and file-tools.ts:149 (`resolveApprovalPath`, the path baked into the persisted grant). || src/sandbox/path-validator.ts:90-98 `expandTilde` — `~`, `~/` only; applied to the candidate path at :70 and to every extra-allowed entry at :57. || src/permissions/allowed-directories.ts:363-367 `expandHomeTilde` — `~`, `~/` only; applied to user allow-list entries at :138 and :250. || Contrast, same repo: src/hooks/hook-config-trust.ts:53-59 and src/hooks/script-hook-runner.ts:137-143 `expandHome` — both DO handle `~\`.

**Observed drift** — Concrete one-sided branch. The `~\` case exists at src/tools/pipeline/path-extraction.ts:61 and in the hooks pair (hook-config-trust.ts:55, script-hook-runner.ts:139); absent from file-read-core.ts:33, path-validator.ts:94, allowed-directories.ts:365. Nothing upstream compensates: `canonicalizePathForMatch` (src/permissions/sensitive-paths.ts:139-141) does NOT expand `~` — it opens with a bare `pathResolve(rawPath)`. So on win32, tool input `~\Documents\x` yields permission target `<home>/Documents/x` (path-extraction.ts:62-64) and tool target `<cwd>\~\Documents\x` (file-tools.ts:129). The Layer-0 sensitive verdict, the approval modal text, the persisted grant pattern, and the audit row are all computed about a file that is never touched. Direction is non-escalating today (the tool's bogus path lands under cwd, always allowed), but decision and effect are provably about different files, and `~\` is the native form a Windows user types.

**Verifier** — CONFIRMED — I tried four refutations and all failed.

EMPIRICAL DRIFT (ran both functions verbatim on this win32 box, cwd=C:\work\proj, home=C:\Users\ikcha):
  input "~\\Documents\\x"  → PERM C:\Users\ikcha\Documents\x   TOOL C:\work\proj\~\Documents\x   (differ)
  input "~\\.ssh\\id_rsa"  → PERM C:\Users\ikcha\.ssh\id_rsa   TOOL C:\work\proj\~\.ssh\id_rsa   (differ)
  input "~/Documents/x", "~" → identical (which is why the suite never sees it).
PERM = src/tools/pipeline/path-extraction.ts:58-65 (the `~\` branch is at :61). TOOL = src/tools/file-read-core.ts:31-35 (`~/` only, :33) as consumed by src/tools/file-tools.ts:129-130.

Nothing upstream compensates: src/permissions/sensitive-paths.ts:139-140 opens with a bare `pathResolve(rawPath)` and never expands `~` (verified: `path.resolve("~/.ssh/id_rsa")` → `<cwd>/~/.ssh/id_rsa`).

REFUTATION 1 — "the effect path has its own guard, so it's harmless." Partly true and it is why nothing escalates: src/tools/file-tools.ts:133-143 (`ensureAllowed`) independently re-runs sensitive + `validateSandboxPath` on the real target. But the finding was never escalation. Every *user-facing and persisted* artifact of the invocation is computed from the PERM string and names a file that is never touched: the Layer-0 deny message (src/tools/invocation-runner.ts:1231-1245), the Layer-1 modal and its grant suggestion (invocation-runner.ts:1264-1293), the reviewer/audit evaluation context (invocation-runner.ts:1211, 1317-1321). Concretely, `write_file {path:"~\\notes.md"}` prompts the user to grant `C:\Users\ikcha`, and on approval creates a literal `~` directory at `C:\work\proj\~\notes.md`.

REFUTATION 2 — "PERM expanding more forms is a fail-safe superset." Fails on the code: src/tools/pipeline/path-extraction.ts:39 pushes exactly ONE interpretation per field value, discarding the literal reading. A union (judge both `<home>\Documents\x` and `<cwd>\~\Documents\x`) would be a defensible asymmetry; this is not that.

REFUTATION 3 — "one side is just the hardened side; that's a fix pending, not drift." Fails because the wrongness INVERTS by platform. Neither branch is platform-guarded. On POSIX, `~\foo` is a legal single filename, and path-extraction.ts:61 still expands it to `<home>/foo` while file-tools.ts:129-130 correctly yields `<cwd>/~\foo` — there the PERMISSION side judges the wrong file. So neither copy is authoritative; this is definitionally one function with two answers.

REFUTATION 4 — "legitimate boundary." No. Both are pure synchronous `(string, cwd) → string` with `homedir()` read at call time, no injected state, no lifecycle, same process, same trust domain, same `src/tools/` tree, and no import cycle blocks a shared module. file-read-core.ts:4-11 literally states its own reason for existing ("makes that equality a compile-time fact instead of a copy-paste that rots") — the copy-paste it warns about is what happened one directory over.

THIRD IN-REPO ANSWER, agreeing with PERM: src/tools/shell-path-policy.ts:439-443 handles `~\`; and the hooks pair src/hooks/hook-config-trust.ts:53-59 + src/hooks/script-hook-runner.ts:137-143 handle it too. So 4 of 5 tool-input-path expanders in this repo accept `~\`; file-read-core.ts:33 is the outlier — and it is the one that decides the byte written.

FOURTH DIVERGENT STRING (candidate understated this): the persisted grant is keyed on the TOOL-side expansion via src/tools/file-tools.ts:145-151 → :428/:513/:568/:626/:675, wired in at src/tools/invocation-runner.ts:721 (`approvalCacheKeyFor`). So within a single `~\` invocation the modal text, the Layer-0/1 verdict, and the grant cache key are three strings about two different files. src/permissions/permission-manager.ts:890-898 already documents a store-time-vs-canonical form mismatch on this same grant pattern and papers over it with a dual-form compare — that mitigation covers realpath/NFC, not tilde.

NO TEST PINS THE SPLIT: the only tilde test in the suite is `read_file {path:"~/.ssh/id_rsa"}` (src/tools/__tests__/executor.test.ts:258) — POSIX form, where both copies agree. Nothing asserts `~\` is deliberately left unexpanded, so there is no evidence the divergence was a decision.

SCOPE — what to merge and what to drop:
  MERGE (the real finding, one authority): path-extraction.ts:58-65 ∥ file-read-core.ts:31-35 (feeding file-tools.ts:129 and :149) ∥ shell-path-policy.ts:439-443. One expander over tool-argument paths; the permission answer and the effect answer must be the same string by construction.
  DROP as a different input domain: src/permissions/allowed-directories.ts:363-367 (`expandHomeTilde`) is applied only to user CONFIG allow-list entries (:138, :250), not to tool arguments. Same primitive, different domain — not the shape.
  NOTE, do not merge as active drift: src/sandbox/path-validator.ts:90-98. Its candidate-path use at :70 is effectively dead for every current caller — file-tools.ts:138, file-read-core.ts:84 and shell-path-policy.ts:81/99/143 all pass an already-resolved absolute path. Its extra-allowed use at :57 is live; whether raw `~` entries survive `sanitizeRuntimeAllowedDirectories` (allowed-directories.ts:129-150) to reach it is unestablished. Treat it as a trap to delete, not as evidence.


## tool-result-image-wire-vendor-gate  *(permission-relevant)*

**Capability** — "Does this tool_result's image actually reach the provider?" — the predicate that must be identical in the serializer, the estimator, and the vendor mapper.

**Wirings** — WIRE MAPPER (the real decision): src/engine/llm/vercel/message-mapper.ts:148-163 — `vendor === "claude" && msg.image`; every other vendor drops the image (comment at :143-147). WIRE SERIALIZER (field whitelist): src/engine/wire-serialize.ts:86-93 — rebuilds a marked tool_result as `{role, toolUseId, toolName, isError, content, meta}`, silently dropping `image` (declared at src/engine/llm/types.ts:209). ESTIMATOR mirroring the whitelist: src/engine/auto-compact.ts:142-152, whose comment at :139-141 says it is "gated on the same not-stubbed predicate the serializer uses".

**Observed drift** — CONFIRMED asymmetry. The estimator and the whitelist agree with each other on the meta-marker half of the predicate, but neither knows about the vendor half that the mapper applies: src/engine/auto-compact.ts:128 takes only a `GenericMessage` and has no vendor parameter, so it adds 85 + tiles*170 image tokens (src/shared/multimodal-token-estimate.ts:81-86) for every non-Claude turn where message-mapper.ts:149 will drop the image entirely. Two places decide whether the image is on the wire; only one of them sees the vendor. Consequence is confined to estimation (over-counted context fill / early preflight on Gemini/OpenAI turns), not correctness of the request itself.

**Verifier** — CONFIRMED after five refutation attempts, all of which failed.

THE PREDICATE IS ONE THING, AND THE CODEBASE SAYS SO IN PROSE THREE TIMES:
- src/tools/types.ts:127-134 declares the contract: `image` is a sibling field "so `output` stays a plain-text placeholder for every string-only consumer (token estimation, persistence, renderer replay); only the Claude message mapper reads this". src/engine/auto-compact.ts:144 reads `message.image` inside token estimation. The declared single-reader contract is already violated in-tree — that is the split, stated and then broken.
- src/tools/file-tools.ts:246-247 restates it ("dropped to the placeholder on vendors whose tool results are text-only").
- src/main/subscription-llm-provider.ts:353-356 restates it again ("API-key paths only replay tool-result image bytes for the Claude mapper").

WIRINGS (the candidate undercounts; there are four, not three):
1. src/engine/llm/vercel/message-mapper.ts:148-163 — `vendor === "claude" && msg.image`; vendor supplied at src/engine/llm/vercel/adapter.ts:234-239. The real API-key decision.
2. src/main/subscription-llm-provider.ts:352-363 `serializedMessage` — a SECOND tool_result field whitelist `{role,toolUseId,toolName?,content,isError}` that drops `image` on ALL subscription transports. The candidate missed this one.
3. src/engine/wire-serialize.ts:86-93 — third whitelist, drops `image` on stub conversion.
4. src/engine/auto-compact.ts:142-152 — the estimator, signature at :128 takes only `GenericMessage`, no vendor.

DRIFT IS ACTUAL, NOT LATENT, AND REACHABLE:
- `view_image` is registered unconditionally (src/boot/services.ts:179 → src/tools/file-tools.ts:700), and the image is attached to history unconditionally with no vendor gate (src/engine/turn/query-loop.ts:1475-1483). Eviction (query-loop.ts:1537-1551) is age-based only. So a live non-Claude turn genuinely holds a tool_result with `image` set.
- Magnitude is fixed at 765 tokens per row: ViewImageTool never sets width/height (file-tools.ts:287-291; both optional at types.ts:138-139) → src/shared/multimodal-token-estimate.ts:82-85 defaults 1024×1024 → 4 tiles → 85+680. Pinned by src/engine/__tests__/auto-compact.test.ts:244-250 ("~765 image overhead added").
- Both sides are pinned by tests that would both have to move together: src/engine/llm/vercel/__tests__/message-mapper.test.ts:175-181 asserts non-Claude drops the image; auto-compact.test.ts:244-250 asserts the vendor-blind count.
- SECOND drift instance the candidate missed: grok-build subscription. src/main/subscription-llm-provider.ts:462 returns undefined → :523 makes projectRequestInput return undefined → src/engine/request-input-projection.ts:55-56 falls through to the generic vendor-blind estimator, which adds 765 tokens for a tool_result image that serializedMessage (:352-363) never sends on ANY subscription transport. Meanwhile codex/kimi get the correct answer because their provider-owned projection wins — i.e. two derivations of the same predicate currently return different numbers for the same history, and only one is transport-aware.

REFUTATIONS ATTEMPTED AND DEFEATED:
1. "view_image is Claude-only" — false; boot/services.ts:179 registers it for every vendor, and file-tools.ts:246-247 explicitly anticipates the non-Claude drop.
2. "The estimator cannot know the vendor" — false. src/engine/conversation-loop.ts:414 passes the live provider, which carries `.vendor` (adapter.ts:204, fallback-chain.ts:224). src/engine/request-input-projection.ts:52 deliberately narrows it to `Pick<LLMProvider,"projectRequestInput">`, throwing the vendor away at the seam. Availability is a design choice here, not a constraint.
3. "The provider hook already covers it" — only for subscription. Grep for `projectRequestInput` finds implementations only at subscription-llm-provider.ts:504 and a pass-through at fallback-chain.ts:245-249; VercelUnifiedProvider has none, so every API-key vendor runs the vendor-blind fallback at request-input-projection.ts:64.
4. "Fallback chain makes the serving vendor unknowable at estimate time" — the strongest objection and it still fails. streamWithFallback (fallback-chain.ts:296-311) can switch vendor mid-turn, but the codebase has already settled the convention: FallbackProvider.vendor = primary.vendor (:232) and projectRequestInput delegates to primary only (:248), and request-input-projection.ts:57-60 declares the projection advisory. Lazily resolvable → not a boundary. The `provider === null` case (conversation-loop.ts:104) has a matching existing default (message-mapper.ts:44 defaults vendor to "claude").
5. "The over-count is deliberate conservatism" — unestablished, and contradicted by the module's own stated intent: auto-compact.ts:131-134 exists precisely to stop preflight firing "far earlier than the actual payload".

MERGEABLE=TRUE. No process boundary (message-mapper, wire-serialize, auto-compact, subscription-llm-provider all run main-process/engine-side), no trust boundary, and the only lifecycle concern (fallback vendor switch) is already resolved in-tree by the primary-projection convention. src/shared/multimodal-token-estimate.ts:14-19 is already the browser-safe shared home for exactly this kind of predicate, so the merge shape exists: one `vendorCarriesToolResultImage(vendor)` consumed by message-mapper.ts:149 and threaded into estimateMessageTokensForWire via the provider already in hand at conversation-loop.ts:414, with subscription transports answering false uniformly.


## user-approval-memory-two-implementations  *(permission-relevant)*

**Capability** — decide whether a prior stored user approval (Store B) may skip the ask for this invocation, including the re-classify escalation guard and the user-facing disclosure of the auto-approve

**Wirings** — AUTHORITY A (reviewer lane): `PermissionManager.dispatchReviewer` — lookup at src/permissions/permission-manager.ts:1375-1381, legacy-null guard at src/permissions/permission-manager.ts:1462-1472, fresh rule verdict at src/permissions/permission-manager.ts:1482-1483, `maxVerdict` composition at src/permissions/permission-manager.ts:1488, renderer disclosure at src/permissions/permission-manager.ts:1502-1506.
AUTHORITY B (foreground modal lane): `tryUserApprovalMemorySkip` at src/tools/pipeline/approval-memory-skip.ts:57 — lookup at :76-82, legacy-null guard at :88-96, fresh rule verdict at :129, `maxVerdict` at :131-134, disclosure at :197 (console only). Wired in as a service at src/tools/invocation-services.ts:43 and src/tools/executor-implementation.ts:565; called from src/tools/invocation-authorization.ts:694 and src/tools/invocation-authorization.ts:1085. The module header at src/tools/pipeline/approval-memory-skip.ts:22-27 states outright: "This function mirrors the reviewer lane's lookup."

**Observed drift** — TWO concrete asymmetries. (1) DISCLOSURE — Authority A calls `this.broadcastUserApprovalHit?.({toolName, scope, verdictAtApproval})` at src/permissions/permission-manager.ts:1502, which reaches the renderer via src/boot/steps/reviewer-permission-wiring.ts:186 (`PERMISSIONS.userApprovalHit`) and renders the toast at src/ui/renderer/ChatView.tsx:658-678. Authority B has NO broadcast — src/tools/pipeline/approval-memory-skip.ts:197 is a bare `console.info` plus a sandbox-audit write at :143-190. So a foreground memory auto-approve is invisible in the UI while the reviewer-lane one is announced, even though src/permissions/permission-manager.ts:621 documents the broadcast as the disclosure contract for exactly this event. This is the seed's shape: one call site reports, the other silently does not. (2) CLASSIFIER INPUT — Authority A classifies `input.finalInput`, which src/tools/invocation-authorization.ts:726-727 supplies as `reviewerInput` (= `maskToolInputForDisplay(finalInput)`, built at src/tools/invocation-runner.ts:732, DLP-masking every string via src/tools/pipeline/display-mask.ts:21-37). Authority B classifies the RAW `finalInput` (src/tools/invocation-authorization.ts:699 → src/tools/pipeline/approval-memory-skip.ts:129). Same `RuleBasedRiskClassifier`, same `maxVerdict` guard, two different input objects for the same invocation — the classifier's path extraction canonicalizes those strings at src/permissions/reviewer/risk-classifier.ts:661-662, so a masked path canonicalizes to a different path than the raw one.

**Verifier** — CONFIRMED. Root = C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks (paths below are repo-relative file:line).

ONE CAPABILITY — verified, not assumed. Both lanes read the SAME Store B entry with the SAME key: A at src/permissions/permission-manager.ts:1375-1381 uses `canonicalStringify(cacheIdentityInput)` where cacheIdentityInput = raw finalInput (:1344); B at src/tools/pipeline/approval-memory-skip.ts:75-82 uses `canonicalStringify(finalInput)` (raw). I checked the fifth key component for drift and found none: `approvalCacheKey` passed by B (src/tools/invocation-authorization.ts:703) and `context.approvalCacheKey` passed by A (src/tools/pipeline/reviewer-dispatch.ts:237) are the same value — src/tools/invocation-runner.ts:721 computes it once and :727-730 spreads it into invocationPermissionContext. The store's own SoT names both readers as one contract: src/permissions/user-approval-store.ts:18-21 ("Read by the reviewer lane ... AND by the foreground modal-skip path ... lets a repeat call with the same tuple skip the modal"). B's header at src/tools/pipeline/approval-memory-skip.ts:22-27, 36-39 says it mirrors A.

THEY RUN BACK-TO-BACK ON THE SAME INVOCATION. src/tools/invocation-authorization.ts:683-715 calls B, then :716-752 calls A, both gated on `decision === "ask" && reviewer.route === "foreground-auto"`. Foreground-auto asks are layer 6 with no forceModal (src/permissions/permission-manager.ts:1300-1307, 1807-1814), so B's guard at :687-692 always passes. This is not two lanes in separate worlds — it is two authorities consulted in sequence for the identical question.

DRIFT 1 (disclosure) — CONFIRMED. A broadcasts at src/permissions/permission-manager.ts:1502-1506 → src/boot/steps/reviewer-permission-wiring.ts:184-187 → toast at src/ui/renderer/ChatView.tsx:658-678. B has no broadcast; only `console.info` at src/tools/pipeline/approval-memory-skip.ts:197. src/permissions/permission-manager.ts:618-622 states the contract as "every user-approval memory hit emits ... to the renderer". Worse than the candidate claims: because B runs FIRST on the only interactive route, the toast is effectively unreachable for foreground memory hits — A's broadcast now fires only on the headless lane (src/tools/pipeline/reviewer-dispatch.ts:95) and the cases B skips. I checked for a compensating signal: the only `emitPermissionReview` outside reviewer-dispatch.ts is src/tools/invocation-authorization.ts:1322, gated on `requiresRemoteLocalOneShot`. Nothing pins B's silence — src/tools/__tests__/executor-approval-memory-skip.test.ts:1-23 enumerates (a)-(h) and disclosure is absent. Omission, not decision.

DRIFT 2 (classifier input) — CONFIRMED, with a caveat (see correction). A classifies `input.finalInput` (src/permissions/permission-manager.ts:1434, 1478-1483) = `reviewerInput` = `maskToolInputForDisplay(finalInput)` (src/tools/invocation-authorization.ts:726, built src/tools/invocation-runner.ts:732). B classifies raw (src/tools/pipeline/approval-memory-skip.ts:120, 129). I ran the deciding regexes from src/shared/dlp.ts:83/94/108 against realistic paths: `/home/u/proj-alpha-beta/src/main.ts` → `/home/u/[REDACTED:TOKEN]/src/main.ts` (the `\b(?:sk|pk|rk|proj|test|live)-[A-Za-z0-9_-]{8,}\b` rule), also mutated by a 16-digit dir name and an email-shaped filename. `extractDeclaredPaths` (src/permissions/reviewer/risk-classifier.ts:454-465) then feeds `isInsideAllowed` (:477-482), so a masked path flips the rule at :590-600 to HIGH "write outside allowed dirs" and defeats the plugin-sandbox auto-LOW at :574-588. Same stored entry, same classifier class, opposite verdicts.

DRIFT 3 (output semantics) — found, not in the candidate. A's memory hit yields a VERDICT that is then threshold-mapped by `resolveReviewerDecision` (src/tools/pipeline/reviewer-dispatch.ts:292-300 → src/permissions/permission-manager.ts:507-546), so a stored HIGH → ask, and the mapping honors `interactiveAutoApprove` (:512-514). B returns unconditional `{decision:"allow", layer:5}` (src/tools/pipeline/approval-memory-skip.ts:200-204) for any stored level and ignores the policy. Consequence of the ordering above: B's escalation refusal (`return null` at :141) hands control to A one branch later, where A re-looks-up the same entry and re-decides with a differently-fed guard and a different mapping. Two answers, one question, one call frame.

NO BOUNDARY. Process: both are main-process (B imports src/audit/sandbox-audit-sink.ts and src/permissions/user-approval-store.ts). Trust: same store, same key, same `RuleBasedRiskClassifier`. Lifecycle: B's ToolExecutor wrapper at src/tools/executor-implementation.ts:324-358 is a pure pass-through touching no `this` state, and both ToolExecutor wirings pass the same PermissionManager (src/engine/conversation-loop.ts:225, src/boot/steps/plugin-tool-executor.ts:112), so the broadcast sink A owns is reachable from B's call site. The only real constraint is that B must work when `permissionManager` is undefined (src/tools/invocation-services.ts:32) — that argues for a free function in src/permissions/ with an injected disclosure sink, not for two implementations.


## workspace-root-lifecycle-wirings  *(permission-relevant)*

**Capability** — The permission-directory lifecycle (persist/remove a workspace root) that backs every "allow-always" directory approval

**Wirings** — Built once at C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\keywords-tolerance\src\ipc\domains\workspace.ts:628-634, then delivered four ways: (a) src/ipc/domains/workspace.ts:635 onto the IPC deps (read at src/ipc/domains/permissions.ts:409); (b) src/ipc/domains/workspace.ts:637 MUTATES the engine's own deps object `deps.conversationLoop.deps.workspaceRootLifecycle`; (c) src/ipc/domains/workspace.ts:640 mutates `deps.sideChatConversationLoop.deps` the same way; (d) src/boot/steps/plugin-tool-executor.ts:124 reaches back through the late-binding ref to read it off the chat loop: `() => lateBinding.conversationLoopRef.fn?.deps.workspaceRootLifecycle`. Consumers: src/engine/conversation-loop.ts:229 and src/tools/invocation-runner.ts:826.

**Observed drift** — The sub-agent path gets NONE of the four. `parentDeps` is a hand-built literal at src/boot/steps/conversation-wiring.ts:372-399 with no `workspaceRootLifecycle` key, and it is a DIFFERENT object from `conversationLoop.deps`, so the ipc mutation at workspace.ts:637 never reaches it. `buildChildDeps` spreads that literal (src/engine/subagent-runner.ts:1772-1773) into `new ConversationLoop(childDeps)` (src/engine/subagent-runner.ts:1942 and :3072), whose executor is built with `() => deps.workspaceRootLifecycle` = undefined (src/engine/conversation-loop.ts:229). Sub-agents DO reach the approval modal (parent gate wrapped at src/engine/subagent-runner.ts:1758-1759) and DO read persisted dirs (getAdditionalDirectories is in parentDeps, src/boot/steps/conversation-wiring.ts:387) — so a user can be prompted, choose "allow-always", and then hit src/tools/invocation-runner.ts:830-845, which produces `{ok:false, error:"workspace lifecycle unavailable"}` and fails the tool with dirPolicySaveFailed. Routine loops are missing it too (src/boot/conversation.ts:310-338).

**Verifier** — CONFIRMED — one capability, four hand-maintained delivery points, and a fifth consumer (sub-agents) that silently gets none of them.

ONE AUTHORITY, MANY WIRINGS (verified in .worktrees/plugin-callbacks):
- Built once: src/ipc/domains/workspace.ts:628-634 (`permissionDirectoryLifecycle`).
- Delivered by three hand-written assignments: workspace.ts:635 (IPC deps, read at src/ipc/domains/permissions.ts:409), workspace.ts:637 (mutates `deps.conversationLoop.deps`), workspace.ts:640 (mutates `deps.sideChatConversationLoop.deps`). A fourth reaches back through late binding: src/boot/steps/plugin-tool-executor.ts:125 `() => lateBinding.conversationLoopRef.fn?.deps.workspaceRootLifecycle`.
- `Grep workspaceRootLifecycle` over src/ (non-test) returns exactly these three assignments — there is no other producer. Consumers: src/engine/conversation-loop.ts:243 `() => deps.workspaceRootLifecycle`, src/engine/turn/commands.ts:166, src/tools/invocation-runner.ts:949.

THE DRIFT IS REAL AND LIVE (sub-agent path):
- `parentDeps` is a hand-built literal at src/boot/steps/conversation-wiring.ts:471-500 with no `workspaceRootLifecycle` key. Its declared type is `ConversationLoopDeps` (src/engine/subagent-runner.ts:541) and that type already declares the field (src/engine/turn/types.ts:296) — so this is an omitted key on the identical type, not a different contract.
- It is a DIFFERENT object from `conversationLoop.deps` (it lists services individually and only cherry-picks three optional fields off `conversationLoop.deps` at :488-496), so the mutation at workspace.ts:637 provably never reaches it.
- `buildChildDeps` spreads it verbatim (src/engine/subagent-runner.ts:1837-1838) into `new ConversationLoop(childDeps)` at :2008 (spawn) and :3138 (resume). Nothing in subagent-runner.ts ever mentions `workspaceRootLifecycle` (grep: zero matches), so the child executor's provider (conversation-loop.ts:243) resolves `undefined`.

THE PATH IS REACHABLE, NOT THEORETICAL:
- Sub-agents are NOT headless: `permissionContext.headless` comes from `self.deps.headless` (src/engine/turn/query-loop.ts:1221); neither the parentDeps literal nor buildChildDeps sets it, so it is undefined/falsy — hence the interactive branch `if (services.approvalGate && !headless)` at src/tools/invocation-runner.ts:810 is taken.
- The gate is present: `makeSubAgentApprovalAdapter` (src/engine/subagent-runner.ts:1789-1791) only relabels `reason` (:810-826) — it does not restrict choices.
- The modal offers allow-always: src/ui/renderer/components/permissions/OutOfAllowedDirCard.tsx:200-209, gated only on retype + adjacency ack (:65), nothing sub-agent-specific.
- Sub-agents read persisted dirs (conversation-wiring.ts:487 → conversation-loop.ts:512-514), so they hit the same Layer-1 out-of-allowed-dir gate as chat.
- Result: user consents, then src/tools/invocation-runner.ts:946-967 takes `lifecycle = undefined` → `{ok:false, error:"workspace lifecycle unavailable"}` (:960) → tool fails with `be_executor.dirPolicySaveFailed` ("[Directory policy save failed] Tool '{name}' — {error}", src/i18n/messages/generated/be_executor.ts:18). Note the allow-session branch at :982-987 calls `dispatchPermissionDirCommand` with NO lifecycle and works fine — only the durable branch is broken, which is exactly the field-present-on-one-wiring/absent-on-another signature.

REFUTATIONS I TRIED AND REJECTED:
1. "Deliberate policy: sub-agents must not durably widen scope." Refuted — the codebase HAS that mechanism and does not apply it here: remote-controller one-shots set `allowedChoices: ["allow-once","deny-once"]`, `durableApprovalRecordAllowed: false`, `forceExplicit: true` (invocation-runner.ts:813-819). Sub-agents set none, so the user is offered a choice that then errors with a defect-shaped message, not a policy denial.
2. "Lifecycle boundary — the object does not exist at wiring time." Refuted as a *boundary*: true that `wireConversation` (src/boot.ts:598) runs before `registerIpcHandlers` (src/main.ts:312) which creates the lifecycle, but that is precisely why both existing wirings are already lazy (`() => deps.workspaceRootLifecycle`, conversation-loop.ts:243; `() => lateBinding.conversationLoopRef.fn?.deps…`, plugin-tool-executor.ts:125). The same lazy accessor works for parentDeps. Lazily resolvable ⇒ not a boundary.
3. Process/trust boundary. Refuted — same main process, same ConversationLoop class, shared permissionManager/approvalGate/additionalDirectories; the only intentional isolations in buildChildDeps are memoryManager, systemPromptBuilder, pluginRuntime, postTurnHookChain (subagent-runner.ts:1840-1862), each with an explicit rationale comment. `workspaceRootLifecycle` has no such comment — it is simply absent.
4. "Maybe a defensive fallback exists." Refuted — the only fallback is `?? (() => undefined)` at src/tools/executor-implementation.ts:132, and src/tools/__tests__/executor.test.ts:2788-2845 pins the unwired case as fail-closed for a "standalone executor". That test documents the safety net; it does not sanction a production loop being unwired.


## ensure-install-ready-three-wirings

**Capability** — Pre-install residual-state reconciliation — draining the uninstall cleanup journal before a plugin may be reinstalled (`ensurePluginStateReadyForInstall`, src/plugins/uninstall-lifecycle.ts:328-355).

**Wirings** — src/ipc/domains/plugins.ts:344-357 (via `requireCleanupServices`, :328-343); src/main/lvis-deep-link.ts:653-664 (via `requirePluginCleanupServices`, :53-85 — a second, separately-written copy of the same required-services assertion); src/boot.ts:630-643 (managed bootstrap).

**Observed drift** — `emitHostEvent` and `refreshPluginNotifications` are optional deps (src/plugins/uninstall-lifecycle.ts:46-47) fired when a pending cleanup completes (:349-354). The IPC wiring passes both (src/ipc/domains/plugins.ts:353-354) and the deep-link wiring passes both (src/main/lvis-deep-link.ts:661-662), but the boot wiring passes NEITHER (src/boot.ts:631-643 supplies only pluginPaths, the three auth-partition services, the drain service and `log`). So a cleanup that completes during managed bootstrap emits no `plugin.uninstalled` and refreshes no notification badge — and that missing event is exactly the one the registry-entry cache subscribes to at src/boot/steps/plugin-runtime.ts:536-542.

**Verifier** — Verified in worktree C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks.

ONE THING — confirmed. `ensurePluginStateReadyForInstall` is one exported function (src/plugins/uninstall-lifecycle.ts:328-355) with one dep interface (:20-49) and its own required-services guard (`requirePluginStateCleanupDeps`, :112-127). The three call sites are not three capabilities but three hand-spelled versions of ONE value set, and they resolve to the SAME function identities: boot.ts:53-55,61 imports `clearAuthPartition`/`forgetTrackedPluginAuthPartitions`/`getTrackedPluginAuthPartitions` from src/main/auth-window-service.js plus `drainPluginInstallLockOperations` from src/plugins/install-lifecycle.js, and src/boot/assemble-services.ts:14-18 imports the identical three symbols under the identical alias names into AppServices. src/ipc/types.ts:12 is `IpcDeps = AppServices & {…}`, and lvis-deep-link.ts reads `getServices()` (AppServices), so the IPC and deep-link wirings are literally reading the same object built at src/boot.ts:735. No process boundary (all main process), no trust boundary (same identities), no differing lifecycle between the IPC and deep-link pair at all.

CAN AND DID DRIFT — confirmed. src/ipc/domains/plugins.ts:353-354 passes `refreshPluginNotifications` + `emitHostEvent`; src/main/lvis-deep-link.ts:661-662 passes both; src/boot.ts:642-654 passes NEITHER (only pluginPaths, the three auth-partition services, the drain service and `log`). The same asymmetry repeats at the fourth, uncited assembly of the same dep bag: src/boot.ts:523-535 (`recoverPendingPluginUninstallCleanups`, which fires the same optional callbacks at uninstall-lifecycle.ts:309-312) also omits both. Independent of the callbacks, the required-services assertion itself exists in three separately-written copies with two of them sharing a byte-identical message: src/ipc/domains/plugins.ts:328-343 and src/main/lvis-deep-link.ts:53-85 both throw "plugin lifecycle cleanup services are not fully wired", and both are redundant with the module's own guard at src/plugins/uninstall-lifecycle.ts:112-127 ("plugin uninstall cleanup services are not fully wired"). Two copies of one assertion over one object is the drift surface, not aesthetic duplication.

REFUTATION ATTEMPTS THAT FAILED. (a) "boot cannot reach emitHostEvent": false — `emitHostEvent` is just `emitEvent` from src/boot/types.ts:46, a module-global synchronous dispatcher over a module-scoped Map (:44-54), and src/boot.ts:65 ALREADY imports `onEvent` from that exact module. Wiring it is an import-specifier edit. (b) "refreshPluginNotifications does not exist yet at boot": AppServices is assembled at src/boot.ts:735, after :639 — but every piece the closure at src/boot/assemble-services.ts:106-111 captures (`ctx.disposePluginNotifications`, `ctx.pluginEventBridgeWindow`, `ctx.replacePluginEventBridge`) is assigned at src/boot/steps/conversation-wiring.ts:264-270, reached via `await wireConversation(...)` at src/boot.ts:598 — i.e. before the managed bootstrap at :639. Lazily resolvable, so not a blocking lifecycle boundary. (c) "the deep-link is a different trust domain (untrusted lvis:// URI)": the trust decision is the dialog at src/main/lvis-deep-link.ts:621-633, upstream of the dep bag; the bag itself is identical to the IPC one.

WHAT DOES NOT SURVIVE: the claimed consequence. See correction.


## install-result-broadcast-shape-five-declarations

**Capability** — The `lvis:plugins:install-result` broadcast payload — what a failed/succeeded plugin install tells the renderer.

**Wirings** — PRODUCER (failure): src/ipc/domains/plugins.ts:481-486 — `{ slug, success:false, error: code ?? message, ...(code ? { message } : {}) }`. PRODUCER (success): src/ipc/domains/plugins.ts:489. PRODUCER (dev local install): src/ipc/domains/plugins.ts:628-631 and :635-639. CONSUMER DECLARATION 1 (preload): src/preload/internal-api-surface.ts:744 — `{ slug: string; success: boolean; error?: string }`. CONSUMER DECLARATION 2 (renderer API type): src/ui/renderer/types.ts:1158 — `{ slug; success; preparing?: boolean; error? }`. CONSUMER DECLARATION 3 (local re-spelling in a hook): src/ui/renderer/hooks/status-bar/use-status-bar-install.ts:22 — `type InstallResultPayload = { slug; success; preparing?: boolean; error? }`.

**Observed drift** — CONFIRMED in BOTH directions. (1) The producer attaches a fourth field `message` (src/ipc/domains/plugins.ts:485) specifically so the renderer can show human copy for the incompatible-app-version case — the comment at :474-477 says the renderer "maps it to the Korean 'needs newer app' copy + update link" — yet none of the three consumer declarations carries `message`, and the toast handler renders the raw code instead: src/ui/renderer/hooks/status-bar/use-status-bar-install.ts:88 does `error: safeField(error)`, so the user sees the literal string `incompatible-app-version` (the i18n mapping at src/ui/renderer/format-ipc-error.ts:188 exists but is never applied on this path). (2) In the other direction, `preparing?: boolean` is declared on the result channel by TWO consumers (src/ui/renderer/types.ts:1158, use-status-bar-install.ts:22) and is set by NO producer — a repo-wide grep for `preparing` finds it only as an install-PROGRESS phase (src/plugins/install-lifecycle.ts:102, broadcast at :1019 on `installProgress`) and as an unrelated generation-scope state. It drifted from the progress channel into the result channel's declaration.

**Verifier** — HOLDS — I tried to refute it on three fronts (process boundary, lifecycle boundary, package-type boundary) and each attempt failed against the deciding code.

ONE CAPABILITY, CONFIRMED: one channel constant (src/contract/app-contract.ts:139 `installResult: "lvis:plugins:install-result"`), one preload bridge method, and every consumer is in the same renderer bundle. There is NO shared payload type anywhere — grep for `InstallResultPayload|PluginInstallResult` across src returns only the three inline re-spellings plus test mocks.

PRODUCERS — 6 sites, 2 helpers (candidate said 4):
- src/ipc/domains/plugins.ts:481-486 (marketplace failure), :489 (success), :628-631 (dev-local success), :635-639 (dev-local failure)
- src/main/lvis-deep-link.ts:672-675 (deep-link success), :678 (deep-link failure)
Two distinct broadcast helpers: src/ipc/domains/plugins.ts:323 vs src/main/lvis-deep-link.ts:682, and the deep-link side hardcodes the channel string instead of using CHANNELS.

CONSUMERS — 3 declarations, 4 subscriptions:
- src/preload/internal-api-surface.ts:744 `{slug, success, error?}`
- src/ui/renderer/types.ts:1158 `{slug, success, preparing?, error?}`
- src/ui/renderer/hooks/status-bar/use-status-bar-install.ts:22 (same, and reused for the UNINSTALL handler at :93, so `preparing?` bleeds onto a second channel)
- subscribers: use-status-bar-install.ts:109, use-plugin-lifecycle-refresh.ts:46 (`success` only), use-installing-plugins.ts:30 (`slug` only), PluginConfigTab.tsx:405 (`slug, success`)

DRIFT 1 (`message` produced, declared nowhere, read nowhere) — CONFIRMED. Producer attaches it at plugins.ts:485 with the intent stated at :474-477. No declaration carries it; no consumer reads it (a `.message` read on this payload exists nowhere; every `result.message` hit in src/ui — use-plugin-marketplace.ts:75, MarketplaceTab.tsx:364 — is the invoke/response shape, not the broadcast). The toast renders `safeField(error)` (use-status-bar-install.ts:88) → literal `incompatible-app-version`. The i18n mapping exists (format-ipc-error.ts:188) and is not applied on this path. Also note the invoke path cannot carry the code at all: plugins.ts:487 rethrows the raw Error, so `normalizePluginActionResult` never runs and use-plugin-marketplace.ts:87 shows `(e as Error).message`. The code lives ONLY on the broadcast, whose consumers discard its human-readable half.

DRIFT 2 (`preparing?` declared, produced never) — CONFIRMED. Declared at types.ts:1158 and use-status-bar-install.ts:22; set by none of the 6 producers. Repo-wide, `preparing` is only an install-PROGRESS phase (src/plugins/install-lifecycle.ts:102, broadcast at :1019; declared preload internal-api-surface.ts:828 and types.ts:1179) or a card `loadStatus` (types.ts:159). No consumer reads `payload.preparing` on the result channel either — dead in both directions.

DRIFT 3 (new, candidate missed it) — the SAME error class yields two different payloads by entry point. Both wirings call the same `installMarketplacePluginWithLifecycle` (plugins.ts:459, lvis-deep-link.ts:646), so `IncompatibleAppVersionError` propagates identically — but only the IPC wiring maps it to the stable code (plugins.ts:478-480). The deep-link wiring sends `error: err.message` raw (lvis-deep-link.ts:678), and dev-local likewise (plugins.ts:637). Same failure, three different `error` values.

WHY THE SPLIT IS NOT LEGITIMATE:
- Preload/renderer process boundary is already crossed by type-only imports in these exact files: internal-api-surface.ts:19-39 imports shared payload types from src/shared/*, and types.ts:1139-1141 imports the SoT `UpdateState` from src/shared/update-state.ts — with a comment at :1135-1137 saying "never inline the discriminated literals here (Field-Addition Sweep rule)". The repo already has a rule against precisely this.
- The third declaration (use-status-bar-install.ts:22) is in the same bundle as types.ts (it imports `LvisApi` from ../../types.js at :3). No boundary at all.
- Package-type divergence (`agentId`/`skillId` at types.ts:1164-1167, deep-link:289/318) is a REAL boundary — those are separate channels (app-contract.ts:212/221) and I drop them.
No test pins the drifted fields: plugins-broadcast.test.ts asserts exact payloads at :268-270, :330-332, :349-351, :728-730, :771-774 — all `{slug, success, error?}` — and the whole ipc/domains/__tests__ tree has zero `incompatible` matches, so the `message` field is untested.

MERGEABLE: a single exported payload type in src/shared (e.g. plugin-lifecycle-events.ts) imported by the preload declaration, types.ts and the status-bar hook, with the producers typed against it. Lazily resolvable — type-only, no runtime coupling, no process crossing.


## main-window-handle

**Capability** — "The window a host→renderer send should target" after Electron close+reopen / dev reload

**Wirings** — BootContext carries two authorities side by side — `mainWindow` (eagerly captured) and `getMainWindow` (live getter) — at src/boot/context.ts:81-82 and :88-89. A third appeared to patch the first: the mutable `pluginEventBridgeWindow` + `replacePluginEventBridge` pair at src/boot/context.ts:200-201, driven from src/boot/assemble-services.ts:90-96. Within one boot step the two sibling factories are wired differently: `createHostApiFactory` receives only `mainWindow` (src/boot/steps/plugin-runtime.ts:399-418; `getMainWindow` appears nowhere in src/boot/steps/plugin-runtime/host-api-factory.ts), while `createLifecycleCallbacks` receives both (src/boot/steps/plugin-runtime.ts:420-424) and prefers the live one at src/boot/steps/plugin-runtime/lifecycle.ts:57 (`getMainWindow?.() ?? mainWindow`).

**Observed drift** — Live: the plugin overlay send uses the boot-time capture (src/boot/steps/plugin-runtime/host-api-factory.ts:1338-1339, `if (!mainWindow.isDestroyed()) mainWindow.webContents.send(OVERLAY_V1.show, ...)`), while the routines events use the live getter (src/boot/steps/routines-wiring.ts:36, :68, :78, :87). The event bridge and plugin notifications capture the boot window (src/boot/steps/conversation-wiring.ts:187-191, consumed at src/boot/plugins.ts:150-152 and :251) and are the only ones that get repaired on window recreation, via the third mechanism above. The staleness is known and written down twice — src/boot/steps/workflow-stores.ts:61-64 ("Lazy resolver — dev-mode reloads destroy the captured webContents") and src/boot/plugins.ts:188-191 ("notificationService ... has its own live getMainWindow getter") — yet the captured handle is still the default in the plugin HostApi.

**Verifier** — CONFIRMED, and the refutation attempt produced stronger evidence than the candidate's own framing.

ONE CAPABILITY, TWO AUTHORITIES ON ONE OBJECT. BootContext carries `mainWindow` (src/boot/context.ts:92) and `getMainWindow` (:93), both assigned from the same inputs (:236-238). main.ts:153 calls `bootstrap(projectRoot, getMainWindow()!, () => getMainWindow())` — the eager handle IS the live getter, evaluated once. boot.ts:120-127 documents the split as a known hazard: bootstrap-time consumers take the resolved window, runtime consumers take the getter, and the former "will silently lose IPC after window recreation."

DECISIVE ACTUAL DRIFT — a twin producer of the identical renderer message, which the candidate did not cite. The same channel OVERLAY_V1.show, same OverlayItem shape, same renderer consumer (src/preload/internal-api-surface.ts:1355; src/ui/renderer/hooks/use-routine-overlay.ts:153) has two wirings:
  - plugin hostApi.triggerConversation: boot capture, hand-rolled guard `if (!mainWindow.isDestroyed()) mainWindow.webContents.send(...)` (src/boot/steps/plugin-runtime/host-api-factory.ts:1348-1349), then returns `{accepted: true, ...}` regardless (:1352).
  - MCP-app ui/message: live getter `const win = getMainWindow()`, explicit failure result `{ok:false, error:"no-window"}` (src/ipc/domains/plugins.ts:1530-1532), send via the shared helper `sendToWindow(win, OVERLAY_V1.show, overlayItem, log)` (:1534).
The authors ALREADY unified the sibling concern across exactly these two paths — `deriveOverlaySummaryForDisplay` at host-api-factory.ts:1337 and plugins.ts:1525, with the comment "one card, one rule, and the less-trusted source does not get the weaker one" (plugins.ts:1516-1519). They merged the sanitizer and left the window handle split. Consequence is the seed's exact shape: after recreation the plugin path is a silent no-op that still reports success, while its twin reports no-window.

WINDOW RECREATION IS REAL AND DESIGNED FOR: createWindow → setMainWindow(win) (src/main/main-window.ts:190); win.on("closed") → setMainWindow(null) (:246); app.on("activate") → showOrCreateMainWindow (src/main.ts:677) → createWindow (src/main/app-tray.ts:28).

NOT SELF-HEALING: `mainWindow` is destructured at factory-construction (host-api-factory.ts:205), so a plugin reloaded AFTER recreation still gets a HostApi bound to the destroyed window — dead until app restart. Contrast the same step's deliberate getter treatment for bindings that change (src/boot/steps/plugin-runtime.ts:401-404).

NO BOUNDARY CROSSED: same main process, same trust domain. `getMainWindow` is destructured at src/boot/steps/plugin-runtime.ts:253 and passed to the sibling factory four lines below the createHostApi call (:427), where src/boot/steps/plugin-runtime/lifecycle.ts:57 prefers it (`getMainWindow?.() ?? mainWindow`). The sibling proves lazy resolution works at this exact site.

REFUTATIONS TRIED AND FAILED: (a) per-window overlay semantics — refuted, the twin resolves live into the same hook; (b) getter unavailable at that site — refuted by the sibling factory; (c) boot window never dies — refuted by the activate/tray recreation path; (d) a test pins the capture — none found (src/boot/steps/__tests__/trigger-conversation-capability.test.ts and plugin-runtime-trigger-gating.test.ts cover gating, not window resolution).

ADJACENT DRIFT, SAME ROOT: registerPluginNotifications captures mainWindow (src/boot/steps/conversation-wiring.ts:266) and is repaired only by refreshPluginNotifications (src/boot/assemble-services.ts:106-111), called from src/ipc/domains/plugins.ts:627 and src/main/lvis-deep-link.ts:600 — never from the recreation path, which calls only registerMainWindowPluginEventBridge (src/main/app-tray.ts:30 → src/main/main-window.ts:385-386 → replacePluginEventBridge, conversation-wiring.ts:269-273). So after recreation, non-bypassFocusGate plugin notifications are suppressed and audited as reason "window-destroyed" (src/boot/plugins.ts:257) while a live window exists. The event bridge registered at conversation-wiring.ts:267 is repaired; the notifications registered on the line before are not.


## max-verdict-composition

**Capability** — "which of two risk verdicts wins" — verdict composition / max

**Wirings** — AUTHORITY: `maxVerdict` src/permissions/reviewer/risk-classifier.ts:85 (exported), used at risk-classifier.ts:983 (rule × llm composition), permission-manager.ts:1393 (rule × stored-approval), and tools/pipeline/approval-memory-skip.ts:131. PRIVATE COPY: `maxVerdict` src/tools/pipeline/rationale-pr1-contract.ts:110, used both as the PRODUCER of the sealed effectiveVerdict (rationale-pr1-contract.ts:382) and as its VERIFIER (rationale-pr1-contract.ts:421 `if (!equal(effective, maxVerdict(initial, reevaluated))) return false;`).

**Observed drift** — ACTUAL — the tie-break is inverted. Authority: `LEVEL_RANK[b.level] >= LEVEL_RANK[a.level] ? b : a` (risk-classifier.ts:86), explicitly documented at :84 as "Ties prefer `b` (LLM) for reason text". Copy: `rank[b.level] > rank[a.level] ? b : a` (rationale-pr1-contract.ts:112) — ties prefer `a`. So for equal levels the two return different verdict objects: the rationale contract seals the STALE `initial` reason string as `effectiveVerdict` where the reviewer authority would have surfaced the re-evaluated reason. This is the exact producer/verifier-diverge-from-authority pattern: the copy's producer and verifier agree with each other, so both of its own tests pass, while it disagrees with the module that owns the operation. Level selection itself is unaffected (only reason text), and I found no site today that compares an authority-produced verdict against this verifier — so the live blast radius is the reason string, not a decision.

**Verifier** — CONFIRMED — tried to refute on trust, process, cycle, and lifecycle grounds; all four fail.

ONE CAPABILITY: exactly two risk-level rank tables exist in src/, byte-identical literals (risk-classifier.ts:82, rationale-pr1-contract.ts:111 — grep for `low: 0, medium: 1, high: 2` returns only these). Both modules document the same named operation: risk-classifier.ts:8 "verdict composition `final = max(rule, llm)`"; rationale-pr1-contract.ts:334 "`effectiveVerdict = max(initialVerdict, reevaluatedVerdict)`". Same `RiskVerdict` type ({level, reason}, risk-classifier.ts:50-53) — the copy already imports it type-only at rationale-pr1-contract.ts:3.

DRIFT IS REAL AND EXECUTED. Authority risk-classifier.ts:86 `>=` (ties→b); copy rationale-pr1-contract.ts:112 `>` (ties→a). Ran both on initial={medium,"bounded workspace deletion"} vs reevaluated={medium,"…same risk"}: authority yields the reevaluated reason, copy yields the initial reason; an authority-produced verdict FAILS the copy's verifier (rationale-pr1-contract.ts:421). The tie semantic is not incidental on the authority side — it is explicitly documented (risk-classifier.ts:84) and pinned by a test at risk-classifier.test.ts:620-624 ("ties prefer the second arg (LLM) for reason text"). The copy has no tie test: the only tie-reachable assertions check `.level` alone (rationale-pr1-contract.test.ts:1180) or sit on the failure path where reevaluated===initial so tie-break is inert (:1208, and :409-410). Producer (:382) and verifier (:421) call the same private copy, so both pass — the exact symptom described.

TIE IS REACHABLE IN PRODUCTION. `isRationaleEligible` (rationale-control.ts:1178-1197) gates on decision/layer/route/provenance/trust-origin — there is NO verdict-level constraint; the test fixture's initialVerdict is `medium` (rationale-pr1-contract.test.ts:55-61). The fresh reevaluated verdict is free-form LLM output parsed at rationale-scope-reviewer.ts:121-128 and passed at :187. Same-level-different-reason is the most likely outcome of re-reviewing the same action.

REFUTATIONS TESTED AND REJECTED:
- Trust domain ("prefer host-sealed initial over LLM reviewer text")? False. initialVerdict is sourced from `permission.reviewer.verdict` (rationale-control.ts:1278-1281), which for LlmRiskClassifier is itself `maxVerdict(ruleVerdict, llmVerdict)` (risk-classifier.ts:983) — already LLM-preferring on ties. Both verdicts pass the identical sanitizer chain (normalizeAndSealRiskVerdict rationale-control.ts:752-775 → projectUiVerdict rationale-resume-contract.ts:408-410 → maskSensitiveData approval-gate.ts:798), so no asymmetry justifies preferring `a`. Reviewer-authored reason text already reaches the modal whenever reevaluated is strictly higher (test rationale-pr1-contract.test.ts:885-897).
- Process boundary? None — same main-process bundle.
- Layering/cycle? Refuted by a same-directory precedent: src/tools/pipeline/approval-memory-skip.ts:47 already does a runtime `import { RuleBasedRiskClassifier, maxVerdict } from "../../permissions/reviewer/risk-classifier.js"`. risk-classifier's runtime deps (audit/dlp-filter, shared/permission-reviewer-framework, permissions/sandbox-capability, permissions/sensitive-paths) reach nothing under tools/pipeline — no cycle. The only real cost is that rationale-pr1-contract is deliberately dep-light ("Frozen PR(1) contracts only", :19); that is a packaging preference, lazily resolvable by hoisting LEVEL_RANK+maxVerdict into a leaf module both import. Not a boundary.
- Lifecycle? None — pure function of two value objects, no boot ordering or instance state.
- Intentional divergence? git log -L110,113 shows the copy arrived whole in 49f1c35c with `>` and zero explanatory comment, while the authority's `>=` carries a documented rationale. Fresh-write divergence, not a considered override.

CONSOLIDATION: delete rationale-pr1-contract.ts:110-113, import the authority (or hoist both to a leaf). Expect no test breakage — no existing test pins the copy's tie behavior.


## mcp-ui-payload-generation-id-enumerators

**Capability** — The MCP-App card payload (`McpUiPayload`) that a rendered card carries back to main to fetch its HTML and call its tools.

**Wirings** — AUTHORITY (6 fields, incl. `generationId`): src/mcp/types.ts:504-526 (`generationId` declared at :507-508). PRODUCER that sets it: src/mcp/plugin-mcp-host.ts:241-251 (`generationId: this.generationId` at :244). CONSUMER that REQUIRES it: src/mcp/mcp-ui-backend-resolver.ts:118-120 — `if (loopback && !generationId) throw new Error("[plugin-loopback] card is missing its immutable generation id...")`, reached from src/ipc/domains/plugins.ts:1258 (uiResource) and :1318-1322 (callTool). RENDERER reads it: src/ui/renderer/components/McpAppView.tsx:430 and :491. RE-ENUMERATION A (runtime rebuild, drops it): src/engine/subagent-transcript.ts:79-89. RE-ENUMERATION B: src/lib/chat-stream-state.ts:102-108 (`StreamEvent.uiPayload`). RE-ENUMERATION C: src/lib/chat-stream-state.ts:164-170 (`ToolEntryItem.uiPayload`). RE-ENUMERATION D: src/ui/renderer/preview/preview-targets.ts:94-101 + :579-583.

**Observed drift** — CONFIRMED. All four re-enumerations list exactly five fields (serverId, resourceUri, slot, height, title) and omit `generationId`. Chronology proves the drift: `generationId` was added to McpUiPayload on 2026-07-23 (commit d1002936 "feat(plugins): commit atomic bundle generations"); the chat-stream-state enumerations date to 2026-04-24 (commit c461f529) and were never widened. src/engine/subagent-transcript.ts:79-89 is the one that drops it at RUNTIME (it constructs a new object), and its own comment at :73-74 justifies dropping `csp` — a field McpUiPayload deliberately never has (src/mcp/types.ts:520-525 "NOTE: deliberately NO `csp` here") — while saying nothing about generationId. Consequence: sub-agent transcripts are rendered "through the same TranscriptRenderer the main chat uses" (src/engine/subagent-runner.ts:2314-2315, entries at :2451), so a plugin-served card in a sub-agent transcript mounts, calls readUiResource with generationId=undefined, and hard-fails at mcp-ui-backend-resolver.ts:119. The main-chat path only escapes this because every other hop passes the object by reference (src/engine/turn/query-loop.ts:1472, src/ipc/handlers/chat-stream.ts:199, src/lib/chat-stream-state.ts:840).

**Verifier** — HOLDS — I tried to refute it three ways and all three refutations failed, and I executed both deciding functions rather than reading them.

EXECUTED PROOF (not inference):
1. `resolveMcpUiBackend('my-plugin', {loopback:{has:()=>true,...}}, undefined)` → throws `[plugin-loopback] card is missing its immutable generation id for 'my-plugin'`; same call with `'gen-1'` returns a backend. Deciding code: src/mcp/mcp-ui-backend-resolver.ts:117-122.
2. `SubAgentTranscriptAccumulator.onToolEnd(..., {serverId:'my-plugin', generationId:'gen-abc', resourceUri:'ui://my-plugin/card.html', slot:'chat'}, 5)` → stored payload is literally `{"serverId":"my-plugin","resourceUri":"ui://my-plugin/card.html","slot":"chat"}`. The field is gone at RUNTIME, from the object rebuilt at src/engine/subagent-transcript.ts:79-89.

CHAIN BETWEEN THEM (read, cited):
- Producer sets it: src/mcp/plugin-mcp-host.ts:241-251 (`generationId: this.generationId` at :244); authority type src/mcp/types.ts:504-526 (:507-508).
- Sub-agents inherit the full parent tool surface minus `agent_spawn`, so plugin-served cards are reachable there: src/engine/subagent-runner.ts:1764-1770 (`baseToolNames` from `toolRegistry.listAll()`), and plugin-source tools are explicitly force-activated at :1778-1785.
- Child `onToolEnd` feeds the accumulator: src/engine/subagent-runner.ts:2378-2380 (and the resume path at :3305-3307), via run-turn's wrapper src/engine/turn/run-turn.ts:290-320.
- Those entries render through the same renderer as main chat: src/engine/subagent-runner.ts:2314-2315 comment, ChatSidePanel.tsx:170-176 + :181-182 → TranscriptRenderer.tsx:476/560/628 → ToolGroupCard.tsx:258-259 and :330-332/:469, which mount `McpAppView` on `tool.uiPayload` presence with NO branch on kind or generationId.
- McpAppView calls src/ui/renderer/components/McpAppView.tsx:430 `readUiResource(payload.serverId, payload.resourceUri, payload.generationId)` → IPC src/ipc/domains/plugins.ts:1258 → the throw. Same for app-initiated tool calls: McpAppView.tsx:488-491 → plugins.ts:1318-1322.
- Failure surface: McpAppView.tsx:434-438 `.catch` sets `error`, so the sub-agent card renders the resolver's error string — a visibly dead card, every time, for any plugin-served card in a sub-agent transcript.

REFUTATIONS ATTEMPTED AND DEFEATED:
(a) "Renderer must not import main-process MCP types, hence the hand-rolled 5-field shape." Refuted: the renderer ALREADY imports the authority — McpAppView.tsx:30 `import type { McpUiPayload } from "../../../mcp/types.js"` and its prop is the full type at :121. src/mcp/types.ts has exactly one import (:8, a type). No import-boundary lint rule found in eslint config/scripts.
(b) "Dropping it is a deliberate trust decision because the sub-agent snapshot is persisted to a user-writable file." Refuted: the real persistence boundary exists and is honored a DIFFERENT way — persisted replay drops the WHOLE uiPayload (src/shared/chat-history.ts:164-180, comment at :167-169), asserted by src/ui/renderer/utils/__tests__/history.test.ts:301 `expect(tool).not.toHaveProperty("uiPayload")`. And sub-agent persistence does not include entries at all: `metadataForResult` at src/engine/subagent-runner.ts:2174-2182 persists only rounds/state/suspension. So the accumulator's snapshot is a LIVE forward; carrying generationId there crosses no file.
(c) "The rebuild exists for a real reason, so the omission is part of it." Half-true and still drift: the rebuild exists to DLP-mask child-authored `title` (subagent-transcript.ts:72-78, test at src/engine/__tests__/subagent-transcript.test.ts:47-73). But its own comment classifies `serverId`/`resourceUri` as structural identifiers left verbatim (:76-78) — `generationId` is host-minted at plugin-mcp-host.ts:244, not child-authored, so it belongs to exactly that class. The comment justifies dropping `csp` (a field McpUiPayload deliberately never has, types.ts:520-525) and says nothing about generationId.

MAIN-CHAT ESCAPE CONFIRMED: every other hop passes the object by reference — src/engine/turn/query-loop.ts:1472, src/ipc/handlers/chat-stream.ts:199 (`...(uiPayload === undefined ? {} : { uiPayload })`, whole object), src/lib/chat-stream-state.ts:840, src/api/platform-conversation-legacy-adapter.ts:84. So the 5-field type declarations are a type-level lie that runtime survives — only the one rebuild materializes the loss.

MERGEABLE: no process, trust, or lifecycle boundary is crossed. Same renderer bundle already consumes McpUiPayload; the accumulator runs in the same main process as the producer; there is no lazy-resolution problem.


## model-discovery-policy-three-predicates

**Capability** — Deciding whether a marketplace provider preset's `modelDiscoveryPolicy` permits a live network fetch of the model list, or requires a seeded/static list.

**Wirings** — Union SoT: `MARKETPLACE_PROVIDER_MODEL_DISCOVERY_POLICIES = ["static","models-api","openrouter-models-api","manual"]` at src/shared/marketplace-package-assets.ts:47-52. THREE predicates each re-enumerate members of that union: (1) src/engine/llm/model-list.ts:44-52 `modelDiscoveryPolicyAllowsFetch` — POSITIVE list `policy === undefined || policy === "models-api" || policy === "openrouter-models-api"`, gating the actual fetch at src/engine/llm/model-list.ts:585. (2) src/data/settings-normalization.ts:170 inside `marketplaceProviderPresetUsesSeededModelOptions` — NEGATIVE list `=== "manual" || === "static"`. (3) src/ui/renderer/tabs/LlmTab.tsx:347-351 `modelDiscoveryPolicyUsesSeededOptions` — NEGATIVE list, byte-identical to #2, gating the UI sync decision at src/ui/renderer/tabs/LlmTab.tsx:340.

**Observed drift** — No wrong answer for any of today's four values — the positive and negative lists are exact complements. But the two listing polarities mean a fifth union member resolves INCONSISTENTLY by construction, in opposite directions: #1 (positive) denies the fetch for an unrecognised policy, while #2 and #3 (negative) report "not seeded", so `shouldSyncModelList` (src/ui/renderer/tabs/LlmTab.tsx:333-345) returns true and the UI requests a live sync the engine then refuses at src/engine/llm/model-list.ts:585 — a silent empty dropdown rather than a policy-appropriate seed. #2 and #3 are additionally a straight duplicate of each other across the main/renderer split with no shared import. The union at src/shared/marketplace-package-assets.ts:47-52 is already `as const` and imported by both sides (src/engine/llm/model-list.ts:18, src/ui/renderer/tabs/LlmTab.tsx:44), so the shared home for the predicate exists and is simply unused.

**Verifier** — HOLDS, but as a latent (not live) drift — weaker than the ToolExecutor seed, and two of the candidate's supporting claims are wrong. I tried four refutations; three fail, one forces a reshape of the fix rather than killing it.

CONFIRMED — the three predicates exist as described and no guard couples them:
- SoT union: src/shared/marketplace-package-assets.ts:47-52.
- #1 POSITIVE list: src/engine/llm/model-list.ts:44-50, gating the fetch at src/engine/llm/model-list.ts:585-591.
- #2 NEGATIVE list: src/data/settings-normalization.ts:165-171, consumed at src/data/settings-normalization.ts:354 (`continue` drops a credential-scoped model-list cache entry).
- #3 NEGATIVE list, byte-identical to #2: src/ui/renderer/tabs/LlmTab.tsx:347-351, with FOUR consumers, not one: :340 (shouldSyncModelList), :379 (modelOptionsFor handshakeOnly), :495 (modelListStatesFromCache), :719 (activeSyncedModelOptions).

REFUTATION 1 — "main/renderer process boundary blocks sharing": FAILS. The renderer already pulls a RUNTIME value out of the SoT module (src/ui/renderer/tabs/LlmTab.tsx:42 imports `marketplaceProviderPresetSecretId`), as does the engine (src/engine/llm/model-list.ts:15-16), and src/data/settings-normalization.ts imports ../shared/* throughout (lines 2-43). The module has no node-only imports (src/shared/marketplace-package-assets.ts:1-18). A pure predicate there is reachable from all three call sites today.

REFUTATION 2 — "trust boundary: the renderer must not be trusted to decide": FAILS as a defense of duplication. Main genuinely re-derives the policy from its own installed presets and overrides the renderer-supplied value (src/ipc/domains/settings.ts:381-398, esp. :389 and :395), with unknown strings normalized to undefined at :374-379. That is an INPUT-provenance guard. It justifies each side evaluating on its own data; it does not justify three independent implementations of the classification function.

REFUTATION 3 — "actual drift?": PARTIALLY LANDS. There is no wrong answer for any live value; the lists are exact complements over {undefined, static, models-api, openrouter-models-api, manual}. Out-of-union runtime strings also resolve CONSISTENTLY, because both ingest paths normalize them to undefined (`cleanEnum` at src/shared/marketplace-package-assets.ts:328-334; `normalizeModelDiscoveryPolicy` at src/ipc/domains/settings.ts:374-379) and undefined means "fetch" on all three. So the drift is latent, not present.
What makes it a real vector rather than aesthetics: adding one string to src/shared/marketplace-package-assets.ts:47-52 is silently accepted at ingest (cleanEnum reads the array at :333, so the new value flows straight into installed presets), and NOTHING forces the three predicates to be revisited — grep shows the const array is referenced only at :333 and src/ipc/domains/settings.ts:376; there is no `Record<Policy,…>`, no `satisfies`, no exhaustive switch. The only test in the area hardcodes the members (`for (const modelDiscoveryPolicy of ["manual","static"] as const)` — src/engine/llm/__tests__/model-list.test.ts:279) so it cannot fail on a fifth. Type-checking stays green while three files disagree.

REFUTATION 4 — "they are different questions": LANDS, and reshapes the fix. `allowsFetch` and `usesSeededOptions` are logically independent (a policy could ship a seed AND permit a refresh); today's four values merely occupy the diagonal, which is why the two polarities look like complements. Collapsing them into ONE boolean would destroy expressible behavior. The correct consolidation is one authority TABLE keyed by the union in src/shared/marketplace-package-assets.ts exporting both columns — the user's own "table seam" pattern — not one predicate.

STRONGEST SUB-FINDING (independent of any fifth member): src/data/settings-normalization.ts:354 and src/ui/renderer/tabs/LlmTab.tsx:495 answer the SAME question — "must a seeded-policy preset's cached model list be discarded?" — main during settings normalization, renderer during cache hydration, with two separate implementations and no shared import. That pair is a straight duplicate with a proven-reachable shared home.

Also note #2's polarity is semantically off even today: the cache-drop at src/data/settings-normalization.ts:354 is really asking "could this preset ever have fetched?" (i.e. `!allowsFetch`), but is implemented as `usesSeededOptions`. Identical today; under a fifth member the engine refuses the fetch (model-list.ts:585) while the unrefreshable cache entry survives normalization forever.


## network-target-extraction

**Capability** — "which network host does this call reach" — network-target derivation from tool args

**Wirings** — (1) CATEGORY: `hasNetworkTarget` src/permissions/reviewer/host-risk-inspector.ts:267 (fields at host-risk-inspector.ts:176 `NETWORK_FIELDS = ["url","endpoint","uri"]`, scheme test `isNetworkUrl` at :246), decides `return "network"` at host-risk-inspector.ts:228. (2) VERDICT: `extractNetworkTarget` src/permissions/reviewer/risk-classifier.ts:348 (inline `["url","endpoint","host","uri"]` at :349) plus `extractNetworkHost` :364, driving the three network RULES at risk-classifier.ts:525-549.

**Observed drift** — ACTUAL — three divergences in the same question. (a) Field set: risk-classifier.ts:349 includes `"host"` in its candidate list; host-risk-inspector.ts:176 omits it and handles a bare `host` as a separate special case at :272-273 that accepts ANY non-empty string without URL validation. (b) Fallback scan: host-risk-inspector.ts:274-277 additionally scans EVERY top-level string value for URL-shaped content ("Default-strict: a network URL under any other key is still a network target"); risk-classifier has no such scan. So a URL under an arbitrary key makes the category `network` while the verdict rules find `target === null`, skipping both the graph.microsoft.com rule (risk-classifier.ts:526) and the trusted-host LOW rule (:533) and landing on the catch-all HIGH (:546). (c) Scheme: host-risk-inspector.ts:249-255 requires http/https/ws/wss; risk-classifier.ts:353 accepts any `new URL()`-parseable value. Every divergence I traced fails toward stricter, so this is a correctness/consistency defect rather than a hole — but it means the trusted-host allowlist (`TRUSTED_NETWORK_HOST_SUFFIXES`, risk-classifier.ts:308) is unreachable for exactly the calls the category layer had to work hardest to classify.

**Verifier** — HOLDS — verified by running both deciders on identical inputs, not by reading alone.

ONE CAPABILITY, PROVEN COUPLED. The two wirings are not parallel systems that happen to look alike; they are consecutive stages of one call. `inspectHostRisk` (host-risk-inspector.ts:208) is called at tools/pipeline/risk-classification.ts:62 from invocation-runner.ts:684, whose return is assigned to `invocationCategory` (invocation-runner.ts:684, `meta.category` at :694). That exact variable is passed as the `category` arg at invocation-authorization.ts:720-723 (interactive-auto) and :976-979 (headless) → reviewer-dispatch.ts:31/:99 → permission-manager.ts:1428-1430 `category: input.category` → `RULES` at risk-classifier.ts:501. So `hasNetworkTarget` decides WHETHER the network rules run, and `extractNetworkTarget` then re-answers the same question to decide WHICH host — twice, from the same argument object, with two different field lists. Enforcement is live: settings-defaults.ts:142 ships `hostClassifiesRisk: true` (pinned by settings-store.test.ts:759-774).

MEASURED DRIFT (scratch vitest, run then deleted; tree clean). Same URL, different key name, opposite verdict:
  {url: "https://api.openai.com/v1/chat"}     → cat=network, LOW  "network trusted host (api.openai.com)"
  {target: "https://api.openai.com/v1/chat"}  → cat=network, HIGH "network untrusted host"
  {url: "https://graph.microsoft.com/v1.0/me"}      → LOW  "network graph metadata read"
  {callback: "https://graph.microsoft.com/v1.0/me"} → HIGH "network untrusted host"
  {endpoint: "http://localhost:3000"} → MEDIUM "network localhost"; {target: same} → HIGH
Cause is exactly as claimed: host-risk-inspector.ts:275-277 scans EVERY top-level string for a URL ("Default-strict: a network URL under any other key is still a network target"; pinned by host-risk-inspector.test.ts:615-619), while risk-classifier.ts:349 knows only ["url","endpoint","host","uri"]. `target === null` skips the graph rule (risk-classifier.ts:528), the trusted-host rule (:535) and the localhost rule (:543), landing on catch-all HIGH (:550). Field-set drift is real: host-risk-inspector.ts:176 omits "host" and handles it as a special case at :272-273 accepting any non-empty string, vs. risk-classifier.ts:358's `/^[a-zA-Z0-9.-]+$/` — measured: {host: "not a hostname!!!"} → cat=network / HIGH.

REFUTATIONS ATTEMPTED AND DEFEATED.
(1) "Different questions — boolean over-inclusion vs. precise host, so the split is deliberate." Rejected: this file pair already establishes the opposite principle for its sibling derivation. host-risk-inspector.ts:22-27 states path containment "reuses the same `sensitive-paths` canonicalization as {@link RuleBasedRiskClassifier} … so containment math is identical across the two modules" — and both files do import it (host-risk-inspector.ts:35, risk-classifier.ts:41-44). Network extraction is the one derivation in this pair NOT given that treatment. Corroborating: `extractShellCommand` is ALSO duplicated whole between them (host-risk-inspector.ts:173 + :282-288 vs risk-classifier.ts:333-341) with an identical 4-key list — same shape, currently still in agreement. The network list is simply the copy that already drifted.
(2) "Process / trust / lifecycle boundary." No boundary exists. Sibling files in src/permissions/reviewer/, same Electron main process, same synchronous invocation stack (invocation-runner.ts:684 → invocation-authorization.ts:720). Nothing is late-bound.
(3) "Scope too narrow to matter." Host-derivation is gated to plugin/mcp at risk-classification.ts:57-59 — but that IS the untrusted-arg surface, and host-risk-inspector.ts:214 makes EVERY mcp-source call category=network regardless of args, so the network rules routinely receive arg shapes the 4-key extractor cannot read. Measured: mcp + {note:"hello"} → HIGH "network untrusted host" — a host-based reason string for a call with no host at all.


## plugin-config-write-four-wirings

**Capability** — Persist a plugin config value: write settings.json, mirror the runtime's in-memory `ConfigOverrideStore`, emit the change event, and make the RUNNING plugin instance observe the new value (which requires a restart, because the sandbox snapshots `configOverrides` at start).

**Wirings** — (A) settings-UI IPC `plugins.configSet` — src/ipc/domains/plugins.ts:962-1001. (B) `hostApi.config.set` — src/boot/steps/plugin-runtime/host-api-factory.ts:398-450. (C) plugin-webview bridge `pluginBridge.configSet` — src/ipc/domains/plugins.ts:2504-2539. (D) `plugins.configSecretSet` (strips the plain-config twin) — src/ipc/domains/plugins.ts:1016-1050. The single consumer they all feed is `buildPluginContext` at src/plugins/runtime/sandbox.ts:199-201, which reads `configOverrides[pluginId]` once at plugin start.

**Observed drift** — CONFIRMED behavioural drift on the reload half. (A) restarts unconditionally and FAILS the whole call if the restart does not return "started" (src/ipc/domains/plugins.ts:990-996), and emits a change event for the union of new+previous keys so deletions are observable (src/ipc/domains/plugins.ts:978-985). (B) restarts only when not nested in a lifecycle mutation and not already restart-pending (src/boot/steps/plugin-runtime/host-api-factory.ts:405-406, :438-443), and emits for one key only (:429). (C) does NOT restart at all (src/ipc/domains/plugins.ts:2532-2534 is the end of the handler) — a plugin's own UI can persist config that the live instance will never see until some unrelated restart. (D) does not restart either (src/ipc/domains/plugins.ts:1043-1047). The mirror call is even self-described as a copy: "Mirror the IPC handler" at src/boot/steps/plugin-runtime/host-api-factory.ts:425-427. One `persistPluginConfig(...)` authority would have made the C/D divergence impossible.

**Verifier** — CONFIRMED — one capability, four wirings, demonstrable drift. I attempted three refutations and all three failed.

ONE CAPABILITY (verified). The write core is literally the same 3-step sequence in all four sites: `settingsService.setPluginConfig` -> `pluginRuntime.setConfigOverride` -> `emitPluginConfigChange`.
- A: src/ipc/domains/plugins.ts:976-983
- B: src/boot/steps/plugin-runtime/host-api-factory.ts:423-429
- C: src/ipc/domains/plugins.ts:2531-2533
- D: src/ipc/domains/plugins.ts:1043-1046
The mirrored store is a boot-time cache of settings.json (src/boot/steps/plugin-runtime.ts:326 `buildPluginConfigOverrides(settingsService)` -> src/plugins/runtime/runtime-state.ts:282 `new ConfigOverrideStore(...)`), so every writer MUST mirror or the cache silently diverges from disk — the canonical "one authority, N writers" shape. Two of the sites say so in their own comments: "Mirror the IPC handler" (src/boot/steps/plugin-runtime/host-api-factory.ts:424-427); "Same secret-strip path used by `lvis:plugins:config:set`" (src/ipc/domains/plugins.ts:2527-2529); "the same record managed by PluginConfigTab via lvis:plugins:config:get/set" (src/ipc/domains/plugins.ts:2481-2483).

ACTUAL DRIFT (not aesthetic — each side has a branch/arg the others lack):
1. Reload. A restarts unconditionally and fails the call if the result is not "started" (src/ipc/domains/plugins.ts:990-996). B restarts only when not nested in a lifecycle mutation and not restart-pending (src/boot/steps/plugin-runtime/host-api-factory.ts:405-406, :438-443). C ends at src/ipc/domains/plugins.ts:2534 with no restart. D ends at :1047 with no restart.
2. Emitted key set. A emits the union of new+previous keys so DELETIONS are observable (src/ipc/domains/plugins.ts:978-984). B (:429), C (:2533) and D (:1046) emit exactly one key — a write through C that drops a key is unobservable to `onChange` subscribers, a write through A is not.
3. Secret handling diverges three ways for the same field: A strips silently (:974), C strips silently (:2530), B REJECTS the call with an error (src/boot/steps/plugin-runtime/host-api-factory.ts:399-403).
4. Aliasing. B `structuredClone`s the record before persisting (host-api-factory.ts:419-422) so the plugin never holds a reference to the stored object; A (:976-977) and C (:2531-2532) hand the settings-service return value straight into the runtime store.
5. Liveness guard. B checks `hostIncarnation.isActive()` (:411-415), A re-checks manifest identity before restart (src/ipc/domains/plugins.ts:987-988), C checks binding+UI revision (:2517-2521), D checks neither (:1029-1047).

REFUTATIONS ATTEMPTED AND DEFEATED:
- "C can't restart — it would deadlock inside `withPluginInstallLock`." FALSE. The lock is re-entrant: src/plugins/install-lifecycle.ts:252-256 returns `trackReentrantOperation(heldLock, fn)` when the lock is already held, and `restartPlugin` itself explicitly tolerates being called under a held lock (src/plugins/runtime/runtime-lifecycle.ts:790-793, :803).
- "C's no-restart is intentional policy." UNESTABLISHED as policy. The doc for this surface describes `bridge.config.set` as "write (server-side strips `format: \"secret\"` fields...)" with no reload contract either way (docs/ko/architecture/architecture.md:3713-3714), and the tests pin only persist+mirror, never the absence of a restart (src/ipc/__tests__/plugin-theme-replay.test.ts:715-741). The strongest real argument for C is that a restart re-loads the plugin and bumps the UI revision (src/plugins/runtime/runtime-lifecycle.ts:372 `markPluginUiRevision`), which retires the calling webview's own binding (src/ipc/domains/plugins.ts:2520, :2313; remount keyed on `runtimeRevision` at src/plugin-ui-host.tsx:319) — i.e. C would tear down the webview awaiting its own reply. That is a legitimate reason for a DIFFERENT reload policy at C, but it is a parameter, not a boundary: it argues for `persistPluginConfig(..., { reload: "none" })`, not for a fourth hand-rolled copy of the write core.
- "D must not restart because secrets are live." TRUE for the secret itself — `hostApi.getSecret` reads `settingsService.getSecret(key)` per call (src/boot/steps/plugin-runtime/host-api-factory.ts:626), never snapshotted. But D also deletes the plain-config twin (src/ipc/domains/plugins.ts:1040-1044), and THAT record IS snapshotted at start (src/plugins/runtime/sandbox.ts:197-201), so the running instance keeps the stale cleartext in `ctx.config` until an unrelated restart. So even D's non-restart has a real staleness consequence; it is a weaker case than A/B/C but not a clean boundary.


## plugin-disable-two-writers

**Capability** — Turn a plugin off: write `enabled:false` into its registry.json row and project that into runtime state

**Wirings** — A (live): `PluginRuntime.setPluginEnabled` — src/plugins/runtime/index.ts:857; registry write at src/plugins/runtime/index.ts:898; in-memory mirrors at src/plugins/runtime/index.ts:974 (`inactivePluginIds.add`) and :975 (`disabledPluginIds.add`). Sole caller: src/ipc/domains/plugins.ts:586 (CHANNELS.plugins.setEnabled). B: `PluginRuntime.disable` — src/plugins/runtime/runtime-lifecycle-capability-operations.ts:1216 → `disableLocked` :1224; guard at :1230; registry write at :1249; in-memory mirror at :1301 (`disabledPluginIds.add` only). Readers of the fact: `isPluginEnabled` src/plugins/runtime/index.ts:811; per-turn scope src/engine/turn/tool-scope.ts:81-93; execution gate src/mcp/plugin-runtime-delegate.ts:133; card projection src/plugins/runtime/index.ts:1135 (inline third copy of the predicate).

**Observed drift** — THREE concrete divergences. (1) Guard: B calls `this.deploymentGuard.canDisable(pluginId, actor)` at cap-ops:1230 — which delegates to `canUninstall` and refuses a user actor for an admin install (src/plugins/deployment-guard.ts:96, :80-85). A has NO guard; `grep -rn deploymentGuard src/` shows cap-ops:1230 is the ONLY canDisable/canUninstall call outside marketplace.ts:1441, so the live IPC toggle is entirely ungated. Nothing re-enables it afterwards — only reinstall/update sets `enabled: true` (marketplace.ts:2014, :2353, :2777). (2) Mirror: A adds to BOTH `inactivePluginIds` (974) and `disabledPluginIds` (975); B adds only `disabledPluginIds` (1301). `isPluginEnabled()` (index.ts:811) reads ONLY `inactivePluginIds`, so after B the registry says disabled while the runtime still answers enabled — the plugin's tools stay in per-turn model scope (tool-scope.ts:88) and stay executable at plugin-runtime-delegate.ts:133 until restart, when runtime-lifecycle.ts:620-621 seeds both sets from `plan.enabled`. (3) Row lookup: A matches `id === installClaim` and THROWS when absent (index.ts:895-897); B matches an alias set `{canonicalPluginId, ...knownInstallAliases}` and silently no-ops when absent (cap-ops:1243-1249).

**Verifier** — HOLDS, but the "two live wirings" framing is wrong — see correction. Root for all paths: C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks\.

ONE THING (confirmed, not inferred). Repo-wide grep for registry `enabled` mutation outside tests returns exactly two writes of `entry.enabled = false`: src/plugins/runtime/index.ts:898 (A) and src/plugins/runtime/runtime-lifecycle-capability-operations.ts:1249 (B). Nothing else writes the row false (src/boot/managed-marketplace.ts:19,31 is a bootstrap feature flag, not a registry row; src/plugins/registry.ts:515 is a docstring example). Both go through the same `updatePluginRegistry` transaction (src/plugins/registry.ts:518), the same `generationLifecycle.deactivateWithCommit` + `capabilityDependencyCommitScope` sequence (index.ts:958-967 vs cap-ops:1291-1297), the same `clearCapabilityBlockedRetry`/`beginPluginLifecycleOperation`/`preparation.clearFor` triple (index.ts:952-957 vs cap-ops:1285-1290), and the same unpublished-candidate branch with a byte-identical AggregateError cleanup block (index.ts:976-992 vs cap-ops:1305-1321).

NO BOUNDARY (four refutation attempts, all failed).
1. Different object/process? No. `PluginRuntime extends PluginRuntimeLifecycle` (index.ts:323) `extends PluginRuntimeCapabilityLifecycle` (runtime-lifecycle.ts:61), which is where `disable` lives (cap-ops:69,1216). A and B are two methods on the SAME instance.
2. Different trust domain (B = admin surface, A = user surface)? No — B has no production caller at all. Repo-wide (excluding dist/ bundles) `.disable(` resolves only to src/plugins/__tests__/runtime.test.ts:183,203,226,244, src/plugins/runtime/__tests__/lifecycle.test.ts:570,584, src/plugins/runtime/__tests__/capability-active-generation.test.ts:855,903,1094,1236, plus two docs. `disableLocked` is called only from cap-ops:1220. B's `actor` param is never supplied by production code.
3. Different lifecycle (A = "stays loaded", B = hot-unload)? Refuted by the tests on both sides. A also removes the plugin from `this.plugins`: set-enabled.test.ts:159-163 asserts `listPluginIds()` no longer contains it after `setPluginEnabled(id,false)`. B: runtime.test.ts:183-186 asserts the same. The comment at index.ts:1142-1144 claiming an inactive plugin "stays in `this.plugins` (loaded)" is stale relative to that test.
4. Different intended downstream effect (index.ts:305-310 says `onActiveStateChange` MUST NOT unregister tools, unlike `onDisable`)? Not realized. In the only wiring, src/boot/steps/plugin-runtime/lifecycle.ts:84-86 (`onDisable`) and :93-95 (`onActiveStateChange` with enabled=false) both reduce to the identical `lateBinding.conversationLoopRef.fn?.onPluginDisabled(pluginId)`.

DRIFT (confirmed, with two corrections in the correction field).
(1) Guard — confirmed and user-reachable. `canDisable` (src/plugins/deployment-guard.ts:99, delegating to `canUninstall` which refuses a user actor for `installSource==="admin"`, :80-85) has exactly one call site in the codebase: cap-ops:1230, on the dead path. The live path src/ipc/domains/plugins.ts:572-586 does sender validation and type checks only, then calls `setPluginEnabled` — no actor, no policy. And the UI does not compensate: in the same detail card the uninstall button is `disabled={saving || selectedPlugin.isManaged}` (src/ui/renderer/tabs/PluginConfigTab.tsx:975) while the enable toggle is `disabled={togglingId === selectedPlugin.id}` only (:946). So "managed plugin cannot be turned off by the user" is asserted by unit tests (runtime.test.ts:195-216) and by the architecture doc (docs/ko/architecture/plugin-deployment-model.md:642,672-674) and enforced by nothing that runs. It persists: runtime-lifecycle.ts:182-191 seeds both sets from `plan.enabled` on next boot.
(2) Mirror — the state divergence is real (A writes both sets, index.ts:974-975; B writes only `disabledPluginIds`, cap-ops:1301) but the claimed consequence is wrong; the real one is worse in kind. See correction.
(3) Row lookup — confirmed verbatim. A: `find(({id}) => id === installClaim)` + `throw` when absent (index.ts:894-897). B: alias set from `knownInstallAliases` + `if (entry) entry.enabled = false`, silent no-op when absent (cap-ops:1244-1249). A row keyed by an install alias is found by B and hard-errors in A; a missing row hard-errors in A and silently "succeeds" in B while B still mutates in-memory state, leaving registry-enabled/memory-disabled.
(4) NOT in the candidate: the teardown checklists differ item-by-item, the signature of copy-then-edit. B does `failedPluginIds.delete` (:1302), `invalidatePluginUiRevision` (:1303), `pendingRestartPreparations.delete` (:1265,:1284); A's disable branch does none of those three (index.ts:905-992). A does `cancelPendingRestart` (:861), B does `pendingRestartCancellations.get(...).cancel()` (:1218) — same intent, different mechanism.
(5) NOT in the candidate: A fires `onActiveStateChange` (index.ts:1054), B fires `onDisable` (cap-ops:1304).
(6) NOT in the candidate: doc drift. docs/ko/architecture/plugin-deployment-model.md:672-674 documents the disable IPC as `return pluginRuntime.disable(pluginId, "user")` — i.e. the doc describes the dead writer as the live one.

MERGEABLE. Same object, same process, same lock discipline (`withResolvedPluginInstallLocks` at index.ts:862 vs `withPluginInstallLock` at cap-ops:1219 — both plugin-install locks). The only thing merging forces is a product decision, not a boundary: whether the live toggle should now enforce `canDisable`. A's disable branch should delegate to one `disableCore(canonicalPluginId, actor)` (B's body, with A's alias-tolerant row lookup and A's both-sets mirror), leaving A's `else` re-enable branch as the only A-specific code. Lifecycle differences (A needs `installReceiptCacheRoot` for re-enable, index.ts:999) are on the enable side and do not touch the shared disable core.


## plugin-emit-denial-audit

**Capability** — Authorizing a plugin's `emitEvent(type, data)` against its declared `emittedEvents` and recording the denial.

**Wirings** — TWO gates implementing the same predicate on two paths into the same host bus. (A) SDK / hostApi path — src/boot/steps/plugin-runtime/host-api-factory.ts:482-501: `getDeclaredEmittedEvents(manifest)` (:484) then `canEmitEvent(type, declaredEmittedEvents)` (:485) then `pluginRuntime.assertPluginEventEmitAccess(pluginId, type)` (:498) then `emitEvent(type, {...data, pluginId})` (:500). (B) plugin-webview IPC path — src/ipc/domains/plugins.ts:2610-2651 (`ipcMain.handle(CHANNELS.pluginBridge.emitEvent, ...)`): `HOST_ONLY_EMIT_NAMESPACES.has(prefix)` (:2629-2632) then `canEmitEvent(type, getDeclaredEmittedEvents(manifest))` (:2636) then `pluginRuntime.assertPluginEventEmitAccess(...)` (:2642) then `emitHostEvent(type, {...data, pluginId})` (:2647). The comment at src/ipc/domains/plugins.ts:2624-2627 explicitly names (A) as the thing (B) "mirrors".

**Observed drift** — This is the seed's exact failure mode. On denial, (A) writes an audit row `plugin_emit_capability_denied eventType=... required=... declaredEmittedEvents=...` through `bootAuditLogger.log` (src/boot/steps/plugin-runtime/host-api-factory.ts:487-494) plus a structured `plog` CAPABILITY_DENY line (:495). (B) writes NOTHING — it returns `{ok:false, error: `missing-capability:${requiredCap}`}` at src/ipc/domains/plugins.ts:2637-2639 with no `auditLogger.log` call anywhere in the handler body (the only `auditUnauthorized` in it is the unrelated frame check at :2613). The handler has `auditLogger` in scope — it is used at src/ipc/domains/plugins.ts:2613 — so this is omission, not unavailability. Result: every capability-denied emit from a plugin webview is invisible in the audit trail, while the identical denial from the same plugin's SDK code is recorded. Second divergence, in the redundant direction: (B) re-checks `HOST_ONLY_EMIT_NAMESPACES` inline at :2629-2632 even though `canEmitEvent` already performs that exact check at src/plugins/capabilities.ts:207 — two spellings of the host-only rule on one path, zero audit on the other.

**Verifier** — CONFIRMED — one capability, two wirings, with drift that is historical rather than hypothetical.

ONE CAPABILITY (verified, not inferred):
- Same bus. `emitEvent` is a single module-level function (src/boot/types.ts:46); host-api-factory imports it at src/boot/steps/plugin-runtime/host-api-factory.ts:60, the IPC handler imports the same symbol as `emitHostEvent` at src/ipc/domains/plugins.ts:14. Both denials guard the same sink.
- Same predicate, and the codebase says so. `canEmitEvent` (src/plugins/capabilities.ts:202-214) carries the docstring "Extracted as a standalone function so both production emit paths (`createHostApi` + the plugin-webview IPC bridge) ... verify the same logic rather than each implementing their own guard" (src/plugins/capabilities.ts:198-200). Same manifest source on both sides (`getDeclaredEmittedEvents`, src/plugins/runtime/manifest-validation.ts:109).
- Same audit sink INSTANCE, so there is no lifecycle boundary. One AuditLogger is built at src/boot/steps/audit-notification.ts:40 (comment at :32 "Shared AuditLogger instance (plugin runtime + hooks + gate)"), handed to the IPC deps bag as `auditLogger: ctx.bootAuditLogger` (src/boot/assemble-services.ts:70) and to the HostApi factory as `bootAuditLogger` (src/boot/steps/plugin-runtime/host-api-factory.ts:156, :203). Both gate bodies execute in the Electron main process (ipcMain.handle at src/ipc/domains/plugins.ts:2610; the factory closure captures the main-process bus and logger).
- The codebase already puts denial-audit INSIDE the shared authority for the other half of emit authorization: `assertEventEmitAccess` audits its own denials via `opts.auditLog` (src/plugins/runtime/access-control.ts:72, :81) and both paths call it (host-api-factory.ts:498, plugins.ts:2641). Ownership denials are uniformly audited; capability denials are not — that asymmetry has no principle behind it.

DRIFT IS REAL AND HISTORICAL:
- A audits: audit row + plog CAPABILITY_DENY at src/boot/steps/plugin-runtime/host-api-factory.ts:487-495. B does not: src/ipc/domains/plugins.ts:2636-2639 returns `missing-capability:${requiredCap}` with no log; `auditLogger` is in scope and used 23 lines earlier (:2613), so omission not unavailability.
- `git blame -L 2610,2645 src/ipc/domains/plugins.ts` shows three separate commits touched one side only: d0808a4b3 (2026-05-01) wrote B's gate while A already had the audit row from ddac7ba5 (M4 #77); 1b37fbac2 (2026-05-01) added B's inline host-only branch; c3cf50a3f (2026-07-11) retrofitted `canEmitEvent` onto B and updated its comment to say "same predicate as the SDK hostApi.emitEvent path" — while still not carrying over the audit row. That is the drift signature, in the log.

REFUTATIONS TRIED AND REJECTED:
- Trust domain / attribution: B's pluginId is host-established, not plugin-claimed — `resolvePluginFromSender` resolves via the host-side webview registry with generation + UI-revision checks (src/ipc/domains/plugins.ts:2304-2320). An audit row from B is as trustworthy as one from A.
- Error shape (A throws, B returns an envelope): reporting-layer only. A shared authorize() returning a decision preserves both, including the two test-pinned codes (`host-only-namespace:host` at src/ipc/__tests__/plugin-theme-replay.test.ts:757; the `missing-capability:` prefix). Note the stated reason for the prefix ("renderer/preload compatibility", src/ipc/domains/plugins.ts:2635) is thin: no production consumer parses it — the only reader is a preload test fixture using a stale capability string (src/__tests__/plugin-preload.test.ts:210 `missing-capability:event-emit`).
- Audit-flooding from an untrusted renderer: the same handler already writes an audit row on frame-check failure (src/ipc/domains/plugins.ts:2613), so the codebase does not hold that policy; and A is equally spammable by a plugin loop.
No process, trust, or lifecycle boundary separates the two gate bodies — merge is lazily resolvable into one main-process authorize function called from both.


## plugin-event-bridge-three-derivations

**Capability** — Decide which plugin-emitted events are forwarded to the renderer over CHANNELS.pluginBridge.event

**Wirings** — (1) src/boot/plugins.ts:137-174 `export function registerPluginEventBridge(pluginRuntime, mainWindow)` — has NO importer anywhere in src/ (grep for `registerPluginEventBridge` returns only its own definition plus ipc-bridge.js imports); (2) src/boot/steps/ipc-bridge.ts:98-123 same name, same signature, same channel — the LIVE one, imported at src/boot/steps/conversation-wiring.ts:29 and called at :266 and :272; (3) src/boot/__tests__/plugin-event-bridge.test.ts:59-81 `registerBridge`, a third in-file copy labelled "mirrors real registerPluginEventBridge".

**Observed drift** — Four concrete divergences. (a) The private-namespace gate `classifySubscription(eventType) === "private" -> skip` exists at src/boot/plugins.ts:147-153 (unwired) and at src/boot/__tests__/plugin-event-bridge.test.ts:66-67 (test mirror) but is ABSENT from the live src/boot/steps/ipc-bridge.ts:105-118, which subscribes every declared type verbatim. (b) Host-derivation of `${id}.auth.changed` for any manifest with an `auth` block exists only in the live copy (src/boot/steps/ipc-bridge.ts:78-89); src/boot/plugins.ts:145 reads `manifest.emittedEvents` only. (c) 100ms coalescing for `*.transcript.updated` exists only in the live copy (src/boot/steps/ipc-bridge.ts:114-116). (d) `win.once("closed", unsubscribeAll)` auto-dispose exists only in the live copy (src/boot/steps/ipc-bridge.ts:121). The test consequence is the seed's exact failure mode: src/boot/__tests__/plugin-event-bridge.test.ts:119-131 ("does NOT forward private-namespace events") calls `registerBridge` (the local mirror) at :124, NOT the production function it imports at :15 — so the asserted gate can never go red because production never had it, while the R3 auth tests at :173/:187/:205/:219 do call the real one.

**Verifier** — CONFIRMED, with one framing correction (see `correction`). I tried to refute it four ways and failed on the core claim; I did find one severity-limiting fact.

WHAT I VERIFIED BY READING THE DECIDING CODE

1. Wiring (1) is genuinely dead, not "maybe imported somewhere". `src/boot/plugins.ts:137` `registerPluginEventBridge` has zero importers: the only non-test hits for the name are its own definition, the live one at `src/boot/steps/ipc-bridge.ts:98`, the import at `src/boot/steps/conversation-wiring.ts:29`, the two call sites `src/boot/steps/conversation-wiring.ts:266` and `:272`, the AppServices key `src/boot/types.ts:240`, its assembly `src/boot/assemble-services.ts:112` (`registerPluginEventBridge: ctx.replacePluginEventBridge` — the live one via ctx), and the re-register on window recreate `src/main/main-window.ts:386`. The repo itself already classifies it as dead: `knip-baseline.json:230-234` grandfathers `{"type":"exports","file":"src/boot/plugins.ts","name":"registerPluginEventBridge"}`.

   Refutation attempt that failed: I checked whether the two allowlists that mention the name pin the dead export — `src/ipc/__tests__/channel-inventory.test.ts:168` and `src/contract/__tests__/contract-version-freeze.test.ts:170` are both SERVICE_KEYS lists for `AppServices`, i.e. they pin the service key at `src/boot/types.ts:240`, not the `boot/plugins.ts` export. Nothing holds the dead copy alive.

2. All four claimed divergences are real, at the cited lines.
   (a) private gate: `src/boot/plugins.ts:147-153` calls `classifySubscription` and skips `"private"`; the test mirror does the same at `src/boot/__tests__/plugin-event-bridge.test.ts:66-67`; the LIVE loop `src/boot/steps/ipc-bridge.ts:105-118` never calls it, and its collector `src/boot/steps/ipc-bridge.ts:69-92` filters only on `typeof t === "string" && t.trim()`. A repo-wide grep for `classifySubscription` confirms the live bridge is the one path that dropped it (present at `src/plugins/capabilities.ts:208`, `src/plugins/runtime/access-control.ts:36` and `:71`, `src/boot/plugins.ts:83` and `:147`).
   (b) `${id}.auth.changed` host-derivation: only `src/boot/steps/ipc-bridge.ts:86-89`; `src/boot/plugins.ts:145` reads `manifest.emittedEvents` alone.
   (c) 100ms coalescing for `*.transcript.updated`: only `src/boot/steps/ipc-bridge.ts:114-116` via `makeCoalescingSend` (`:35-62`) and `isTranscriptEvent` (`:22-24`).
   (d) `win.once("closed", unsubscribeAll)`: only `src/boot/steps/ipc-bridge.ts:121`; the dead copy returns a bare disposer at `src/boot/plugins.ts:171-173`.

3. THE TEST CONSEQUENCE IS PROVEN BY EXECUTION, not by reading. The file imports the real bridge at `src/boot/__tests__/plugin-event-bridge.test.ts:15` but the security test "does NOT forward private-namespace events" (`:119-131`) calls the local mirror `registerBridge` at `:124`. I wrote a throwaway probe (`test/zz-tmp-bridge-drift-probe.test.ts`, since deleted; tree verified clean) that ran that exact fixture — a manifest with `emittedEvents: ["audit.log.entry"]` — through the PRODUCTION `registerPluginEventBridge` from `src/boot/steps/ipc-bridge.js`. Result under `node scripts/run-vitest-under-electron.mjs`:
     PROBE_RESULT sent=[{"channel":"lvis:plugin:event","eventType":"audit.log.entry","data":{"secret":true}}]
     AssertionError: expected [ { …(3) } ] to have a length of +0 but got 1
   So the assertion the suite advertises as protecting the private-namespace boundary is false of production and green anyway — the seed's exact "assertion that cannot go red" failure mode. Three other tests share the mirror (`:103`, `:138`, `:154`); only `:89`, `:173`, `:187`, `:205`, `:219` touch the real function.

4. No legitimate-boundary defence survives. Both `src/boot/plugins.ts` and `src/boot/steps/ipc-bridge.ts` are main-process boot modules against the same bus (`onEvent` from `src/boot/types.ts`) and the same channel (`CHANNELS.pluginBridge.event`, `src/contract/app-contract.ts:350`), consumed by the same `PluginRuntime`. No process split, no trust split, no lifecycle that resists lazy resolution — the live copy already handles the only real lifecycle nuance (window recreate) via `ctx.replacePluginEventBridge` at `src/boot/steps/conversation-wiring.ts:269-274`.

MERGE SHAPE: delete `src/boot/plugins.ts:132-174` and its `knip-baseline.json:230-234` entry (which must go in the same change or the baseline is stale), delete the mirror `src/boot/__tests__/plugin-event-bridge.test.ts:59-81`, and repoint `:103`, `:124`, `:138`, `:154` at the imported production function. That repoint makes `:119-131` go red — which is the point: it forces the explicit decision of whether the live bridge should reinstate the `classifySubscription(type) === "private"` skip, a decision currently hidden by the mirror.


## plugin-event-bridge-two-impls

**Capability** — Bridging plugin `manifest.emittedEvents` from the host event bus to the renderer over `CHANNELS.pluginBridge.event`.

**Wirings** — TWO exported functions with the identical name `registerPluginEventBridge` and identical signature `(pluginRuntime, BrowserWindow) => () => void`: (A) src/boot/plugins.ts:137 — iterates `manifest.emittedEvents ?? []` (src/boot/plugins.ts:145), calls `classifySubscription(eventType)` at src/boot/plugins.ts:147 and skips `private` verdicts with a warn (src/boot/plugins.ts:148-153), then `mainWindow.webContents.send(...)` at src/boot/plugins.ts:158. (B) src/boot/steps/ipc-bridge.ts:98 — iterates `collectPluginEventTypes(pluginRuntime)` (src/boot/steps/ipc-bridge.ts:69-90), then `win.webContents.send(...)` at src/boot/steps/ipc-bridge.ts:109. Only (B) is wired in production: src/boot/steps/conversation-wiring.ts:29 imports it and calls it at src/boot/steps/conversation-wiring.ts:266 and again inside `ctx.replacePluginEventBridge` at src/boot/steps/conversation-wiring.ts:272. (A) is imported nowhere — src/boot/assemble-services.ts:13 and src/boot/steps/conversation-wiring.ts:28 import only `registerPluginNotifications` from that module.

**Observed drift** — Four concrete divergences, in both directions. 1. SECURITY GATE PRESENT IN THE DEAD ONE ONLY: (A) refuses to bridge private-namespace events via `classifySubscription` (src/boot/plugins.ts:147-153); (B) has no `classifySubscription` call at all (grep of src/boot/steps/ipc-bridge.ts is empty). (A)'s docstring at src/boot/plugins.ts:133-134 still states "Only events that pass classifySubscription('public') are forwarded with webContents.send" — a guarantee the live path does not provide. Nothing upstream restores it: schemas/plugin-manifest.schema.json `emittedEvents` is `{type:array, items:{type:string,minLength:1}}` with no namespace constraint, and src/plugins/runtime/manifest-validation.ts:109-113 `getDeclaredEmittedEvents` filters only for non-empty strings. So a manifest declaring `emittedEvents: ["audit","dlp","memory.private"]` makes the live bridge subscribe the host bus for those namespaces and IPC-forward their payloads to the renderer; the only surviving filter is the preload one at src/preload/internal-api-surface.ts:910-916 — defense collapsed from two layers to one. 2. FEATURE PRESENT IN THE LIVE ONE ONLY: (B) host-derives `${manifest.id}.auth.changed` for any manifest with an `auth` block (src/boot/steps/ipc-bridge.ts:83-87); (A) has no such derivation, so restoring (A) would silently break `usePluginAuthStatuses`. 3. (B) coalesces `*.transcript.updated` at 100ms via `makeCoalescingSend` (src/boot/steps/ipc-bridge.ts:117-119, definition src/boot/steps/ipc-bridge.ts:34-61); (A) sends every event unthrottled. 4. (B) validates and trims each entry (`typeof t === "string" && t.trim()`, src/boot/steps/ipc-bridge.ts:74-75) and auto-disposes on `win.once("closed")` (src/boot/steps/ipc-bridge.ts:124); (A) does neither.

**Verifier** — VERIFIED, with two material corrections (see `correction`). No legitimate boundary exists: both functions run in the MAIN process, subscribe the SAME bus (`onEvent` from src/boot/types.ts), send the SAME channel `CHANNELS.pluginBridge.event`, take the same `(PluginRuntime, BrowserWindow) => () => void` shape, and have the same per-window disposer lifecycle. Nothing is lazily-unresolvable. Merge = delete src/boot/plugins.ts:132-174.

WIRING CONFIRMED. Live: src/boot/steps/ipc-bridge.ts:98, imported at src/boot/steps/conversation-wiring.ts:29 and called at :266 and :272. Dead: src/boot/plugins.ts:137 — zero importers repo-wide; src/boot/assemble-services.ts:13 and conversation-wiring.ts:28 import only `registerPluginNotifications` from that module. The codebase already knows it is dead: knip-baseline.json:230-234 baselines `{file: "src/boot/plugins.ts", name: "registerPluginEventBridge"}` as an unused export. The two freeze tests that mention the name (src/contract/__tests__/contract-version-freeze.test.ts:170, src/ipc/__tests__/channel-inventory.test.ts:168) pin the SERVICES-surface key set by src/boot/assemble-services.ts:112 (`registerPluginEventBridge: ctx.replacePluginEventBridge`), not the dead function — they do not keep it alive.

ALL FOUR CLAIMED DIVERGENCES CONFIRMED. (1) `classifySubscription` gate present only at src/boot/plugins.ts:147-153; grep of src/boot/steps/ipc-bridge.ts returns nothing. Nothing upstream restores it: schemas/plugin-manifest.schema.json:288-294 constrains `emittedEvents` to `{type:string, minLength:1}` with no namespace rule, and src/plugins/runtime/manifest-validation.ts:109-117 `getDeclaredEmittedEvents` filters only non-empty strings. (2) `${manifest.id}.auth.changed` host-derivation only at src/boot/steps/ipc-bridge.ts:86-89. (3) coalescing only at src/boot/steps/ipc-bridge.ts:114-116 via `makeCoalescingSend` (src/boot/steps/ipc-bridge.ts:35-62). (4) trim/validate at src/boot/steps/ipc-bridge.ts:74-75 and auto-dispose at src/boot/steps/ipc-bridge.ts:121 (candidate cited :124 — off by three).

GIT CONFIRMS ONE-SIDED EVOLUTION: commit b2ea1225 ("chore(0.5.2): host-derive <id>.auth.changed") touched only src/boot/steps/ipc-bridge.ts; `git log -L 137,174:src/boot/plugins.ts` shows the dead copy has not been functionally revised since. That is supersession leftover, which is why divergences 2-4 all point the same direction (live has, dead lacks).


## plugin-method-map-derivation-four-ways

**Capability** — binding a plugin generation's manifest-declared tools to its runtime handlers (the toolName -> handler map) and reporting tools skipped for a missing handler

**Wirings** — Shared authority: `buildMethodMap` at src/plugins/runtime/plugin-loader.ts:69, used by the restart path (src/plugins/runtime/runtime-lifecycle.ts:1082) and the prepared-artifact path (src/plugins/runtime/runtime-lifecycle-capability-operations.ts:1460). Two hand-rolled re-implementations of the same loop: boot load at src/plugins/runtime/runtime-lifecycle.ts:332-350, addPlugin at src/plugins/runtime/runtime-lifecycle-capability-operations.ts:594-611.

**Observed drift** — ACTUAL, two independent divergences. (1) Skip telemetry: three sites emit the structured phase event `plog("warn", {pluginId, phase: PluginPhase.REGISTER_TOOL_SKIP, toolName, reason:"missing_handler"})` — runtime-lifecycle.ts:336, runtime-lifecycle.ts:1083, capability-operations.ts:1461-1465 — while the addPlugin copy emits an unstructured `log.warn(`missing handler '${toolName}' — tool disabled`)` at capability-operations.ts:597 with no pluginId and no phase, so a tool silently dropped during an install/enable is absent from the plugin-phase stream the other three feed. (2) Duplicate-name rule has three variants for the same map: `this.methodMap.has(toolName)` (runtime-lifecycle.ts:340, throws even when the colliding entry is the same plugin's own), owner-aware `owner && owner !== manifest.id` (capability-operations.ts:602, and again post-start at :678), and self-purge-then-owner-check (src/plugins/runtime/runtime-publication-state.ts:45-52).

**Verifier** — CONFIRMED, with one sub-claim narrowed (see correction).

ONE CAPABILITY — proven by the code, not by resemblance. The derivation is already factored into a single pure authority: `buildMethodMap(manifest, instance, onMissingHandler)` at src/plugins/runtime/plugin-loader.ts:69-84 — iterate `declaredRuntimeMethods(manifest)`, look up `instance.handlers[toolName]`, skip+report if absent, else set. The two hand-rolled copies are byte-for-byte the same algorithm: src/plugins/runtime/runtime-lifecycle.ts:333-339 and src/plugins/runtime/runtime-lifecycle-capability-operations.ts:595-601. Both even call the SAME shared `declaredRuntimeMethods` helper (runtime-lifecycle.ts:333, capability-operations.ts:595) for the first half of the computation and then re-implement the second half by hand.

NO MODULE/LIFECYCLE EXCUSE EXISTS. Both files already import `buildMethodMap` (runtime-lifecycle.ts:37, capability-operations.ts:44) and already call it elsewhere in the same file (runtime-lifecycle.ts:1082, capability-operations.ts:1460). The two sites that DO call it are the ones with MORE lifecycle machinery — restart carries cancellation/`isCurrent()` (runtime-lifecycle.ts:1070, 1114) and prepared-artifact carries `withPluginInstallLock` (capability-operations.ts:1490) — which disproves any claim that boot-load or addPlugin lifecycle context obstructs the helper. The helper takes no lifecycle state at all. The module's own header states it consolidates the pattern "that every instantiation path (load / addPlugin / restartPlugin / reloadPlugin) shares" (plugin-loader.ts:4-9): load and addPlugin are named as in-scope and are exactly the two that never adopted it. This is incomplete adoption, not a boundary.

DRIFT (1) — CONFIRMED, real and consumer-visible. Three sites emit the structured phase event via `plog`: runtime-lifecycle.ts:336, runtime-lifecycle.ts:1083, capability-operations.ts:1461-1465, each with `{pluginId, phase: PluginPhase.REGISTER_TOOL_SKIP, toolName, reason:"missing_handler"}`. The addPlugin copy emits `log.warn(\`missing handler '${toolName}' — tool disabled\`)` at capability-operations.ts:598. Verified that this is not a cosmetic difference in two independent ways: (a) different logger namespace — `plog` writes to `createLogger("plugin-lifecycle")` (src/plugins/lifecycle-log.ts:22, :145) while that `log` is `createLogger("plugin-runtime")` (capability-operations.ts:60); (b) no structured fields at all — no pluginId, no phase, no toolName field, no reason. `PluginPhase.REGISTER_TOOL_SKIP` = "lifecycle:register:tool:skip" (lifecycle-log.ts:63), and the documented consumption model is grepping `'"phase":"lifecycle:...'` over the jsonl log (lifecycle-log.ts:5-11), which also states new code MUST use `plog()`. So a tool silently dropped during install/enable is invisible to the phase stream the other three feed — the same class of consequence as the seed finding's missing `callbacks`. A grep confirms capability-operations.ts:598 is the ONLY missing-handler site in src/ not using plog.

PER-SITE MESSAGE WORDING IS NOT AN OBSTACLE: the three adopting sites already emit different message text through the callback ("...missing handler", "...after restart", "...in prepared artifact"), which is precisely what the `onMissingHandler` seam exists for (plugin-loader.ts:64-68, :11-13). Merging preserves site-specific wording.

MERGEABLE: same process (main-process PluginRuntime class hierarchy, same `this`, same `this.methodMap`), same trust domain, same file, same already-present import. The only structural entanglement is that the two inline loops interleave the duplicate-owner check with the map build (runtime-lifecycle.ts:340-350, capability-operations.ts:602-613), which `onMissingHandler` does not cover. That is not a boundary: addPlugin itself already runs the identical owner check as a standalone post-loop pass over `methods.keys()` at capability-operations.ts:677-690, proving the check does not need to be interleaved. Nothing outside the local `methods` map is mutated during the loop, so hoisting the check after `buildMethodMap` is behavior-preserving.


## private-namespace-predicate-open-coded-in-preload

**Capability** — Classifying an event type as private (`memory.private`, `settings.apiKey`, `audit`, `dlp`) so it can never be subscribed to.

**Wirings** — SoT: `classifySubscription` at src/plugins/capabilities.ts:222-236, whose private branch is `for (const priv of PLUGIN_PRIVATE_NAMESPACES) { if (eventType === priv || eventType.startsWith(`${priv}.`)) return "private"; }` (src/plugins/capabilities.ts:227-231). Used correctly by src/plugins/capabilities.ts:208 (`canEmitEvent`), src/plugins/runtime/access-control.ts:36 and :71, and src/boot/plugins.ts:83 and :147. Second wiring: src/preload/internal-api-surface.ts:910-912 re-implements the identical predicate inline inside `onPluginEvent` — `[...PLUGIN_PRIVATE_NAMESPACES].some((ns) => eventType === ns || eventType.startsWith(`${ns}.`))` — importing only the raw Set (src/preload/internal-api-surface.ts:18), never the classifier.

**Observed drift** — No behavioural divergence in the prefix arithmetic today — both spell `=== ns || startsWith(ns + ".")`. The divergence that already exists is in COVERAGE, not arithmetic: `classifySubscription` returns a three-way verdict whose callers act on "private" as one branch of a policy that also recognises `PUBLIC_HOST_EVENT_TYPES` (src/plugins/capabilities.ts:232) and `PUBLIC_EVENT_NAMESPACES` (src/plugins/capabilities.ts:234); the preload copy is a boolean deny-check that will not track any future refinement to that policy — e.g. a private namespace expressed as an exact event name rather than a prefix, which is exactly the reason `PUBLIC_HOST_EVENT_TYPES` was split out from `PUBLIC_EVENT_NAMESPACES` at src/plugins/capabilities.ts:137-142. This matters more than it otherwise would because of `plugin-event-bridge-two-impls`: the live main-side bridge dropped its `classifySubscription` call, so src/preload/internal-api-surface.ts:910-916 is currently the ONLY enforcement of the private-namespace rule on the renderer delivery path — and it is the copy, not the authority.

**Verifier** — HOLDS, but as the WEAKEST form of the seed shape — rank it below findings with observed divergence.

REFUTATION ATTEMPTS, ALL FAILED:

(1) Process boundary (preload vs main)? REFUTED BY THE FILE ITSELF. src/preload/internal-api-surface.ts:18 ALREADY imports `PLUGIN_PRIVATE_NAMESPACES` from `../plugins/capabilities.js`, and `classifySubscription` is in that SAME file at src/plugins/capabilities.ts:222-236. capabilities.ts is a pure leaf — I read all 236 lines and it contains ZERO import statements, no electron/node deps. The authority module is already in the preload bundle graph; importing one more named export from it crosses nothing and costs nothing. There is no boundary here to defend.

(2) Different trust domain / different subject? REAL BUT NOT A BOUNDARY. `classifySubscription`'s doc (src/plugins/capabilities.ts:216-221) frames it as PLUGIN subscription policy, while the preload gate applies to the HOST renderer's `lvisApi.onPluginEvent` (built at src/preload/internal-api-surface.ts:191, exposed at src/preload.ts:51 and :65, consumed by src/ui/renderer/hooks/use-plugin-auth-status.ts:158). So the SUBJECT differs — but the PREDICATE does not. Merging is a pure substitution of `classifySubscription(t) === "private"` for `[...SET].some(...)`: identical truth value, no change to who is gated. A different subject does not justify a second implementation of "what counts as private."

(3) Three-way verdict vs boolean? Not a boundary — the boolean IS the `=== "private"` projection of the same branch (src/plugins/capabilities.ts:227-231).

(4) Lifecycle? None. Both sides are pure functions over a module-level frozen-by-convention Set.

CONFIRMED "ONE THING": the preload comment self-declares the relationship — src/preload/internal-api-surface.ts:908-909 literally says "Mirrors capability enforcement in plugins/capabilities.ts." A self-declared mirror of a named authority function, importing that authority's data but not its logic, is the shape. Five other call sites use the authority correctly: src/plugins/capabilities.ts:208, src/plugins/runtime/access-control.ts:36 and :71, src/boot/plugins.ts:83 and :147. The preload is the sole open-coder.

CONFIRMED "CAN DRIFT" — AND IS COMPLETELY UNPINNED: no test exercises the preload branch at all. src/__tests__/preload-shape.test.ts:394 asserts only `"onPluginEvent": "function"`. Every private-namespace test calls the AUTHORITY directly and never the copy: src/boot/__tests__/event-namespace-policy.test.ts:30-35, src/__tests__/cross-plugin-events.test.ts:157-161 and :177 (that last one is near-tautological — it asserts `classifySubscription` twice and never touches the delivery path, see its own admission at :174-176). Consequence: refine the private branch at src/plugins/capabilities.ts:227-231 and every test goes green while src/preload/internal-api-surface.ts:910-912 silently fails open; delete the preload branch entirely and nothing goes red either. Refinement precedent is in this exact file — PUBLIC_HOST_EVENT_TYPES (src/plugins/capabilities.ts:142-144) was split out of PUBLIC_EVENT_NAMESPACES precisely because namespace-prefix matching was too coarse for exact host-owned names (rationale at :136-141). The symmetric refinement on the private side is the drift scenario.

WHY IT'S ONLY WEAK-HOLD (be honest with the caller): there is NO actual drift today. Both sides spell `=== ns || startsWith(ns + ".")` identically, and the only mutable input — the Set at src/plugins/capabilities.ts:151-156 — is a shared import, so data can never diverge. The divergence surface is one line of prefix arithmetic, not a 12-arg constructor. Unlike the ToolExecutor seed, there is no second INSTANCE and no arg-list asymmetry to point at.


## renderer-tool-file-target-extraction

**Capability** — "which file paths did this tool call touch" — the derivation that turns a ToolEntryItem's input into openable file targets for the chat UI

**Wirings** — src/ui/renderer/preview/preview-targets.ts:219 `collectPathStrings` (feeds the chat preview target list at preview-targets.ts:518) and src/ui/renderer/utils/action-panel-activity.ts:79 `collectPathStrings` (feeds the action panel's file-change/read rows at action-panel-activity.ts:212 and :226). Both modules also carry their own verbatim copies of the supporting tables: PATH_KEYS at preview-targets.ts:131 vs ACTION_PANEL_PATH_KEYS at action-panel-activity.ts:14 (byte-identical 10 keys); READ_TOOL_PATTERN at preview-targets.ts:144 vs action-panel-activity.ts:10 (identical regex); WRITE_TOOL_NAMES at preview-targets.ts:145 vs FILE_CHANGE_TOOL_NAMES at action-panel-activity.ts:9 (identical 3 names); URL_PATTERN at preview-targets.ts:146 vs ACTION_PANEL_URL_PATTERN at action-panel-activity.ts:12.

**Observed drift** — Four concrete divergences, not aesthetic. (1) Glob guard exists on ONE side only: preview-targets.ts:224 `if (isGlobPattern(item)) return;` (helper at preview-targets.ts:195, comment "Glob patterns are tool arguments, never files"); action-panel-activity.ts:79-97 has no glob check at all, and its `looksLikeFilePath` (action-panel-activity.ts:55) accepts any string containing `/` (line 62), so a `glob_files` call renders `src/**` as a touched file in the action panel and not in the preview list. (2) Patch-body extraction exists on ONE side only: action-panel-activity.ts:90-91 special-cases the `patch` key through `extractPatchPaths` (defined at action-panel-activity.ts:99); preview-targets.ts:219-234 has no patch branch, so `apply_patch` yields the patched file paths in the action panel and not in the preview list. (3) Recursion policy differs: action-panel-activity.ts:80 caps at depth 4 and only descends non-path-keyed children while `depth < 2` (line 92); preview-targets.ts:221 delegates to `visitUnknown` (preview-targets.ts:260) with no such non-path-key depth restriction. (4) The path-shape predicate differs: `isLikelyPath` (preview-targets.ts:166) requires separator AND extension in its generic clause, with bare filenames accepted only when the KEY vouches (preview-targets.ts:227-228); `looksLikeFilePath` (action-panel-activity.ts:55-65) accepts a bare `foo.md` unconditionally at any key. Note the kind predicates did NOT drift — preview-targets.ts:329-330 and action-panel-activity.ts:27-33 both fall back to `tool.category` — which shows the two copies are being maintained in parallel by hand and have already gone out of sync on the extraction half.

**Verifier** — CONFIRMED. I tried to refute this on four fronts and every defense collapsed; the strongest evidence is a runtime JOIN the candidate did not cite.

THE JOIN (kills the "different purposes" defense). The action panel's derived string is not an independent summary — it is resolved back against the OTHER copy's output by exact string equality. ActionPanel.tsx:149-156 renders each activity row as a button calling `onOpenItem(item.target!, web)`; ChatView.tsx:626 wires that to `routeActivityItem` -> `routeActivity` (ChatView.tsx:321), which at ChatView.tsx:328-330 does `previewModel.targets.find(c => "path" in c && c.path === target)`. `previewModel` is `collectChatPreviewModel(...)` (ChatView.tsx:32, :206) — i.e. the preview copy of `collectPathStrings` (preview-targets.ts:518). So the design REQUIRES both derivations to emit byte-identical strings for the same tool input. Every divergence is by construction a lookup miss, falling to the dead-end branch at ChatView.tsx:333-336 (`workspaceTabs.addTab("file-browser")`) — a row that opens an unrelated file tree. The "preview must be strict because it opens things / action panel can be loose because it only summarizes" defense is therefore false: both feed the same open action.

NO BOUNDARY. Same process and bundle (both under src/ui/renderer/). Same consumer component (ChatView.tsx:32 and :37). Same lifecycle — both are pure sync functions over the same array, memoized side by side (ChatView.tsx:206 `collectChatPreviewModel({entries: visibleEntries...})`, ChatView.tsx:315 `computeActionPanelActivity(entries)`). Same input type: chat-stream-state.ts:206 declares `tools: ToolEntryItem[]`, and preview-targets.ts:10 derives `ToolItem` as that exact element type. Nothing to lazily resolve.

DRIFT EXECUTED, NOT INFERRED. I lifted both deciding functions verbatim into a scratch script and ran them on the real builtin schemas (glob_files = `pattern` + `path?` + `limit`, file-tools.ts:84-88; grep_files, file-tools.ts:90-96). Results, divergence in BOTH directions:
- `{pattern:"src/**/*.ts", limit:200}` -> preview `[]`, actionPanel `["src/**/*.ts"]`
- `{pattern:"foo/bar", include:"**/*.ts", ...}` -> preview `[]`, actionPanel `["foo/bar","**/*.ts"]`
- `{query:"index.ts"}` -> preview `[]`, actionPanel `["index.ts"]`
- `{path:"src/components"}` (dir, no ext) -> preview `[]`, actionPanel `["src/components"]`
- `{a:{b:{c:{path:"/x/deep.ts"}}}}` -> preview `["/x/deep.ts"]`, actionPanel `[]` (REVERSE direction the candidate missed: action-panel-activity.ts:92 `depth < 2` non-path-key cap vs preview's uncapped `visitUnknown`, preview-targets.ts:260 — so readFileCount/writtenFileCount under-report relative to the preview file list)
- `{path:"report.md"}` -> SAME (key vouches on both sides; the one case that agrees)
glob_files is `category: "read"` (file-tools.ts:333) and its name matches READ_TOOL_PATTERN (action-panel-activity.ts:10), so `isReadTool` is true (action-panel-activity.ts:31-33) and the pattern string reaches the read rows at action-panel-activity.ts:226.

DRIFT HISTORY IS ONE-SIDED, PROVEN. The glob guard came from commit 2455275d (PR #1445). `git show --stat` lists 25 files: it added `isGlobPattern` to src/ui/renderer/preview/preview-targets.ts AND created a copy in src/tools/file-read-core.ts:42 — and did NOT touch src/ui/renderer/utils/action-panel-activity.ts (last touched by 37fa806a / c7d969fa). The fix landed in two of three copies and skipped the third. Both surviving renderer copies are pinned by their own tests (action-panel-activity.test.ts:76-95 pins `looksLikeFilePath("report.txt")===true` and the patch branch; preview-targets tests were extended in the same 2455275d), which is exactly the hand-maintained-in-parallel signature.

THIRD COPY IS A LEGITIMATE BOUNDARY — DROP IT FROM THE MERGE. src/tools/file-read-core.ts:42 `isGlobPattern` is byte-identical to preview-targets.ts:196, but that module is main-process: it imports `node:os` (`expandTilde`, file-read-core.ts:30-34) and serves a security ordering (glob reject -> sensitive -> sandbox boundary, file-read-core.ts:52-67). The renderer cannot import it. Merge the two renderer copies into one; leave file-read-core alone.

Supporting evidence the candidate did not cite: the URL half of the same module pair has drifted too. preview-targets.ts:208-217 `collectUrls` is uncapped and keeps raw `match[0]`; action-panel-activity.ts:67-77 caps at `depth > 4` and strips trailing `[),.;]+` (lines 71-72). Same two-copy shape, same pair of files.


## tool-pair-invariant-two-impls

**Capability** — Repairing the assistant-toolCalls ↔ tool_result pairing invariant on a message slice before it is persisted (providers 400 on an unpaired tool_use).

**Wirings** — (1) src/engine/conversation-history.ts:241 `normalizeToolPairInvariant` — the authority; called from ConversationHistory.repairToolPairInvariant (:98), .restore (:123), .trim (:163), and from the session loader at src/engine/turn/session.ts:258 and :394. (2) src/ipc/domains/chat.ts:311 `removeOrphanToolUse` — a second, independent implementation, used only by chat:fork at src/ipc/domains/chat.ts:751 on the slice that is then written to a NEW session file (src/ipc/domains/chat.ts:758).

**Observed drift** — The second implementation was written against a legacy Anthropic raw-content-block shape and is now a guaranteed no-op on `GenericMessage`. It collects resolved ids from `m.role === "user" && Array.isArray(m.content)` blocks whose `block.type === "tool_result"` (src/ipc/domains/chat.ts:315-321) — but `UserContentPart` is only `text | image | file` (src/engine/llm/types.ts:192-196), so `resolvedIds` is always empty. It then scans backwards for `m.role === "assistant" && Array.isArray(m.content)` (src/ipc/domains/chat.ts:325) — but assistant `content` is typed `string` (src/engine/llm/types.ts:200) and tool calls live on the sibling `toolCalls` field, which the function never reads; the loop `break`s on the first message. The real authority keys on exactly those fields (src/engine/conversation-history.ts:262-266). Consequence is bounded rather than fatal — the forked session is written unrepaired, then repaired and rewritten when loaded (src/engine/turn/session.ts:258-265, which logs a `repaired invalid tool history` warning) — but the fork path's own guard has silently been dead since the message shape changed.

**Verifier** — CONFIRMED — I tried to refute it and could not. (Worktree line numbers differ from the candidate's; corrected below. Root: C:\Users\ikcha\workspace\lvis-project\lvis-app\.worktrees\plugin-callbacks)

ONE CAPABILITY, verified: both wirings exist to repair the assistant.toolCalls <-> tool_result pairing on a message slice before it is persisted.
- Authority: `normalizeToolPairInvariant` at ...\src\engine\conversation-history.ts:258, consumed at :99 (repairToolPairInvariant), :133 (restore), :173 (trim, with `preserveOpenToolTail`), and imported by ...\src\engine\turn\session.ts:13 for use at :260 (loadSession) and :399 (branchFromCheckpoint).
- Second impl: `removeOrphanToolUse` at ...\src\ipc\domains\chat.ts:462, not exported, exactly one call site — chat:fork at ...\src\ipc\domains\chat.ts:920. No test references it (grep over src returned only the definition and that call site).

THE PARALLEL THAT KILLS THE "different job" defense: chat:fork (chat.ts:913-927) and `branchFromCheckpoint` (session.ts:395-407) are the same four-step sequence — slice -> repair -> `rehydrateToolResultArtifacts` -> `saveSession(newId, ...)`. branchFromCheckpoint uses the authority (session.ts:399) with the comment "Repair tool-pair invariant" (session.ts:397-398); chat:fork uses the private copy. Same operation on the same message type, two implementations.

DRIFT IS ACTUAL, NOT AESTHETIC — and I verified it by running the deciding code rather than reading types. I copied the body of `removeOrphanToolUse` (chat.ts:462-485) verbatim and fed it the exact slice chat:fork produces (cut right after an assistant message whose tool_result was sliced off): output === input, orphan `toolCalls[0].id` still present; same for the multimodal-user variant. Root causes:
- Backward loop (chat.ts:474-476) tests `m.role !== "assistant" || !Array.isArray(m.content)` and `break`s. Assistant `content` is typed `string` (...\src\engine\llm\types.ts:207), so `Array.isArray` is false and the loop breaks on its first iteration in every case. Tool calls live on the sibling `toolCalls` field, which the function never reads. The authority keys on exactly `message.role === "assistant" && message.toolCalls` (conversation-history.ts:280).
- `resolvedIds` (chat.ts:464-473) looks for `block.type === "tool_result"` inside user array content, but `UserContentPart` is only text|image|file (llm/types.ts:199-203). I checked the two gates that could admit a foreign block at runtime and both whitelist: `normalizeLocalUserContentParts` (...\src\main\subscription-attachment-input.ts:251, emits only text/image/file) runs on every restore (conversation-history.ts:128), and the import validator rejects non-string assistant content outright (chat.ts:235) and constrains user parts (chat.ts:231, 267-268). Fork reads in-memory history (chat.ts:913), so both gates apply.
- Capability gap beyond the dead branches: `removeOrphanToolUse` has no handling for an orphan tool_result at all; the authority does (conversation-history.ts:310-323). And where the authority strips only the unpaired toolCalls and keeps assistant text (conversation-history.ts:289-306), the copy deletes the whole message — a worse policy that branchFromCheckpoint already declined.

NO LEGITIMATE BOUNDARY:
- Process: the ipcMain handler (chat.ts:910) and ConversationLoop both run in main. No IPC/process hop.
- Trust: input on both sides is the same in-memory `GenericMessage[]` already normalized by restore. No untrusted-vs-trusted asymmetry.
- Lifecycle/layering: the authority is a pure exported free function over an array — session.ts:13 already imports it outside the class, and chat.ts already imports engine modules (chat.ts:18-19, :68). Nothing to lazily resolve.

CONSEQUENCE — bounded, as claimed, and I confirmed the containment: the unrepaired slice is written at chat.ts:927, then `loadSession(newId)` at chat.ts:938 runs the authority at session.ts:260-268 (logs "repaired invalid tool history", re-saves) and `history.restore` normalizes again (conversation-history.ts:133). So no provider 400 via this path. The cost is a guard that has silently been dead since the message shape changed, plus a window where the new session file on disk violates the invariant for any reader that is not `loadSession`.

MERGE: delete `removeOrphanToolUse` and call `normalizeToolPairInvariant(slice).messages` at chat.ts:920, mirroring session.ts:399 (optionally logging removedMessages/removedToolCalls the same way).


## tool-path-argument-extraction

**Capability** — "which filesystem paths does this tool call actually touch" — path-argument extraction from finalInput via the tool's pathFields

**Wirings** — (1) ENFORCEMENT: `extractTargetFilePaths` src/tools/pipeline/path-extraction.ts:24, called at src/tools/invocation-runner.ts:1070, feeding Layer 0 sensitive hard-block + Layer 1 out-of-dir via PermissionManager.checkPathScope (invocation-runner.ts:1084, 1141). (2) CATEGORY: `extractCallPaths` src/permissions/reviewer/host-risk-inspector.ts:652, called at host-risk-inspector.ts:230 inside `inspectHostRisk`, which is wired at src/tools/pipeline/risk-classification.ts:61 and reached from invocation-runner.ts:626. (3) VERDICT: `extractDeclaredPaths` src/permissions/reviewer/risk-classifier.ts:454, called by the write/read RULES at risk-classifier.ts:583, 592, 603, 616. `getDottedFieldValue` is copied verbatim into all three: path-extraction.ts:48, host-risk-inspector.ts:669, risk-classifier.ts:431.

**Observed drift** — ACTUAL, AND STRUCTURAL. Only copy (1) resolves the argument before canonicalizing: `resolveToolPathForPermission` (path-extraction.ts:58-64) expands `~`/`~\` to homedir() and then `pathResolve(pathResolve(cwd), expanded)` against the invocation's `executionCwd`. Copies (2) and (3) hand the RAW argument straight to `canonicalizePathForMatch`, whose first step is `pathResolve(rawPath)` (sensitive-paths.ts:139) — i.e. relative to `process.cwd()`, with no tilde expansion. This is not an oversight that can be patched in place: `HostRiskSignals` (host-risk-inspector.ts:179-193) and `ToolInvocationContext` (risk-classifier.ts:99-115) have no cwd field at all, and the call site at risk-classification.ts:61-66 passes only `source/finalInput/pathFields/allowedDirectories` — so the reviewer half is incapable of seeing the cwd that enforcement uses. `executionCwd` genuinely differs from `process.cwd()`: it is `sessionProjectRoot ?? getDefaultProject().projectRoot ?? process.cwd()` (src/engine/conversation-loop.ts:558-561), threaded at src/engine/turn/query-loop.ts:1207. Concrete divergence: for `{path: "notes.md"}` in a session bound to a user-picked project root, enforcement checks `<projectRoot>/notes.md` while the risk rules check `<process.cwd()>/notes.md`; if the project root is a granted additionalDirectory and process.cwd() is the default workspace, the classifier can return "write at allowed-dir leaf" LOW (risk-classifier.ts:609) for a path enforcement resolves elsewhere. For `{path: "~/.ssh/id_rsa"}` the classifier sees `<cwd>/~/.ssh/id_rsa`, judges it contained, and rates it MEDIUM via isDeepInsideAllowed (risk-classifier.ts:603) rather than HIGH.

**Verifier** — CONFIRMED as one capability with drifting wirings; refutation attempts failed.

ONE CAPABILITY (not a boundary). All four derivations answer "which absolute filesystem paths does this call touch", from the same inputs (`tool.pathFields` + the same `finalInput` object + the same `invocationAllowedScope.directories`), in the same process, on the same synchronous call stack (invocation-runner -> permission-manager -> classifier). `getDottedFieldValue` is byte-identical in path-extraction.ts:48-56, host-risk-inspector.ts:669-677, risk-classifier.ts:431-439. Both reviewer copies state the intent is the real target, not the literal argument: host-risk-inspector.ts:648-651 ("the containment check below is what closes the traversal vector") and risk-classifier.ts:441-452 ("Without canonicalization an attacker can pass ~/.lvis/... and bypass the sandbox-write check"). No trust-domain, process, or lifecycle split: same object identity for finalInput, same main process, no serialization boundary between them.

DRIFT IS REAL AND EXECUTED, not aesthetic. Copy (1) resolves before canonicalizing (path-extraction.ts:58-64: tilde expansion + pathResolve against the invocation `executionCwd`); copies (2)(3)(4) hand the raw argument to canonicalizePathForMatch whose first step is bare `pathResolve(rawPath)` (sensitive-paths.ts:139-140) i.e. relative to process.cwd(). I ran both resolution bodies side by side (scratchpad/pathcmp.cjs): with executionCwd=C:\Users\ikcha\projects\demo, "notes.md" -> enforcement C:\Users\ikcha\projects\demo\notes.md vs reviewer <process.cwd()>\notes.md; "~/.ssh/id_rsa" -> C:\Users\ikcha\.ssh\id_rsa vs <process.cwd()>\~\.ssh\id_rsa; absolute inputs agree. Relative and tilde args are first-class, not hypothetical: MoveFileInputSchema documents "absolute or relative to session cwd" (file-tools.ts:117-118) and FileTool.resolvePath expands tilde at execution (file-tools.ts:128-131). executionCwd genuinely differs from process.cwd(): `sessionProjectRoot ?? getDefaultProject().projectRoot ?? process.cwd()` (conversation-loop.ts:589-593), threaded at query-loop.ts:1219, and allowedDirectories are built FROM it (buildAllowedScope -> computeDefaultAllowedDirectories(cwd), allowed-directories.ts:318-322, 71-72, 84-85) so process.cwd() is normally NOT in the allow-list.

REACHABLE CONSEQUENCE (opposite direction from the one claimed, see correction): for a headless turn writing a relative path, enforcement passes Layer 1 (invocation-runner.ts:1193-1302) while the rule classifier re-resolves to <process.cwd()>/notes.md and fires "write outside allowed dirs" HIGH (risk-classifier.ts:596-598) -> the call is deferred, and the LLM reviewer prompt (risk-classifier.ts:694-699) reasons about a path the host never checked. Nothing pins the current behavior: every classifier test uses absolute paths only (risk-classifier.test.ts:152, 175, 180, 196, 255, 691...), so the divergence cannot fail a test — the "can it fail?" gap.

MERGEABLE (lazily resolvable). The authority is already threaded to the drifting side and then dropped: PermissionEvaluationContext carries both `executionCwd` and the enforcement-resolved `targetFilePaths` (evaluation-context.ts:19-22), it is built from the real values (invocation-runner.ts:747-758, 1317-1321), and it is passed into dispatchReviewer (reviewer-dispatch.ts:107 and 234; ReviewerDispatchInput.evaluationContext, permission-manager.ts:278) — but buildReviewerContext (permission-manager.ts:1426-1449) never forwards it, so extractDeclaredPaths re-derives from raw. Likewise executionCwd is a live local at the risk-classification call site (invocation-runner.ts:669, 684 -> invocation-services.ts:58-67 -> risk-classification.ts:61-67). Verdict-cache identity already varies with cwd indirectly, since cacheCtx keys on allowedDirectories (permission-manager.ts:1395) which are cwd-derived — so consolidation does not open a cache-replay hole.

---

# Round 2 — axes the first pass did not cover

Axes: ambient context carriers, error/audit paths, main-versus-renderer
duplicate logic, config defaults and normalization.

Candidates raised: 89 · verified: 43 · confirmed mergeable: 22

## Two outcomes worth reading before trusting this list

**`subagent-spawn-cancellation-signal-not-threaded` — FIXED.** The spawn path
passed `undefined` where the resume path in the same file passes
`cancellation.signal`, so `interruptRun` reported CANCELED while the child ran
its full `cappedRounds` and the second interrupt answered "not running". One
argument. See the cancellation-signal PR.

**`approval-allowed-choices-renderer-rederivation` — FALSE POSITIVE. Do not
implement.** It claimed `ApprovalRequest.allowedChoices` was "declared but never
read" in the renderer. It is read: `ToolApprovalDialog.tsx:555-558` derives
`isHostConstrainedToOneShot` from exactly that field and feeds it into
`approvalIsOneShot`, which gates the durable buttons — with a comment stating
the reason ("must not offer a durable choice that the approval gate will
reject"). The claim survived an adversarial verification pass that was supposed
to refute it. Treat every entry below as a lead to re-check, not a finding to
act on.

## Confirmed candidates


### webview-preferred-flow-seven-wirings

**Capability** — The default + accepted value-set for `webView.preferredFlow` (the host policy for opening plugin-delegated external URLs).

**Wirings** — ONE declared default: `src/data/settings-defaults.ts:87` (`preferredFlow: "in-app"`). SEVEN further statements of it: (1) `src/data/settings-normalization.ts:816` `const VALID_WEBVIEW_FLOWS = ["in-app","system-browser"]` — module-private, NOT exported, so nothing else can share it; (2) `src/data/settings-normalization.ts:818-834` `normalizeWebView` validates + warn-logs + falls back to `DEFAULT_SETTINGS.webView`; (3) `src/data/settings-store.ts:1214` calls it — on the DISK-LOAD path only; (4) `src/data/settings-store.ts:808-810` the IPC patch path is a bare `this.settings.webView = { ...this.settings.webView, ...partial.webView }` with NO validation; (5) `src/boot/steps/plugin-runtime/external-url.ts:69` `?? "in-app"`; (6) `src/boot/steps/plugin-runtime/app-preference.ts:72` returns `settingsService.get("webView")?.preferredFlow` with NO fallback; (7) `src/ui/renderer/tabs/WebTab.tsx:27` `useState<WebViewPreferredFlow>("in-app")` plus `WebTab.tsx:37` `next === "in-app" || next === "system-browser"` re-spelling the value set.

**Observed drift** — ALREADY DIFFERS in two ways. (a) Load validates, patch does not: `settings.update` hands its raw payload straight to `patch()` (`src/ipc/domains/settings.ts:758` → `settingsService.patch(partial)`; the handler validates only `a2aRemote`, `llm.activeChatRuntime` and the Foundry baseUrl before that call), so `updateSettings({webView:{preferredFlow:"yes"}})` persists `"yes"` to lvis-settings.json. `normalizeWebView` only coerces it on the NEXT boot — the exact 'recovery-style fallback at load instead of validating at the boundary' pattern the appearance branch's own comment (`src/data/settings-store.ts:763-768`) says is forbidden. (b) Two consumers of the same field handle a missing value differently: `external-url.ts:69` substitutes `"in-app"` while `app-preference.ts:72` hands plugins `undefined` — so `hostApi.getAppPreference("webView.preferredFlow")` and the host's actual routing decision can report different things. Contrast the structurally identical siblings: `src/shared/sidebar-tab.ts` (`DEFAULT_SIDEBAR_TAB` + `SIDEBAR_TABS` + `isSidebarTab`) and `src/shared/initial-app-mode.ts` (`DEFAULT_APP_MODE` + `normalizeAppMode`) were both extracted into shared SoT modules consumed by every layer. webView never was, and `VALID_WEBVIEW_FLOWS` being a private const is why.


### appearance-patch-skips-bundleid-and-schemaversion

**Capability** — Validation + default fallback for the persisted `appearance` block (bundleId, schemaVersion, followSystem, language, font).

**Wirings** — Authority on the load path: `src/data/settings-normalization.ts:739` `normalizeAppearance` — gates `schemaVersion === 2` (line 746), coerces an unknown `bundleId` to `DEFAULT_BUNDLE_ID` via `VALID_BUNDLE_IDS` (lines 751-755), coerces `language` via `normalizeLocale` (line 762), validates `font`. Called once, from `src/data/settings-store.ts:1200` (disk load). Second wiring on the write path: `src/data/settings-store.ts:759-806` re-implements a PARTIAL subset — `nextAppearance` is built by spreading `appearanceRest` unchecked (lines 776-779), then `language` IS validated (lines 787-789) and `font` IS validated (lines 791-806). Third wiring in the renderer: `src/ui/renderer/theme/ThemeProvider.tsx` `setBundle` does `isBundleId(id) ? id : DEFAULT_BUNDLE_ID` before sending `updateSettings({appearance:{schemaVersion:2,...patch}})`.

**Observed drift** — ALREADY DIFFERS — the patch path validates 2 of 5 subfields. `bundleId`, `schemaVersion` and `followSystem` ride the unchecked spread at `settings-store.ts:776-779`, while `language` and `font` get boundary validation 10 lines below under a comment that explicitly states the rule being broken ('validate at every trust boundary … instead of dropping on next load', lines 763-768, 785-786). Concrete failure: an `updateSettings({appearance:{schemaVersion:1}})` persists; on the next boot `normalizeAppearance` sees `schemaVersion !== 2` and no v1 legacy keys, falls through both branches, and returns `{...DEFAULT_SETTINGS.appearance}` — silently wiping the user's bundleId AND language. Only the RENDERER currently guards bundleId (`ThemeProvider` `isBundleId`), i.e. the guard lives on the untrusted side of the IPC boundary and not on the trusted side.


### system-settings-normalization-duplicated-load-vs-patch

**Capability** — Per-field validation + fallback for the 10-field `system` settings block.

**Wirings** — Load path: `src/data/settings-normalization.ts:876-991` `normalizeSystem`, called from `src/data/settings-store.ts:1215`. Write path: `src/data/settings-store.ts:811-917` hand-re-implements the same ten fields inline (closeBehavior, appMode, localApiServer, launchAtStartup, launchMinimized, sidePanelWidth, sidebarWidth, the three split percents, sidebarActiveTab, pinnedProjectRoots), each with its own guard + warn line.

**Observed drift** — ~105 lines mirrored against ~115. I diffed the guards field-by-field and they currently AGREE (same type checks, same `clampSidebarWidth` / `clampSidePanelSplitPercent` / `normalizePinnedProjectRoots` calls, same `Math.max(SIDE_PANEL_MIN_WIDTH, Math.round(x))` for sidePanelWidth) — so this is duplication risk, not present divergence. The semantic difference between the two is real and intended (load falls back to `DEFAULT_SETTINGS.system`, patch keeps the prior value), which is why I would call the fallback TARGET a legitimate boundary; what is not legitimate is that the FIELD ENUMERATION and the per-field type guards are written twice, so adding a new `system` field means remembering two sites. The repo already recognises this failure mode and fixed one instance of it: `SIDE_PANEL_SPLIT_KEYS` (`src/data/settings-normalization.ts:870-874`) was extracted with a `satisfies readonly (keyof SystemSettings)[]` precisely 'so a new split-bearing tab kind is added in exactly one place' — the other nine fields never got that treatment.


### out-of-allowed-dir-ack-and-retype-renderer-only

**Capability** — Whether a Layer-1 directory widening with adjacency warnings (.env/.git/.ssh/credentials adjacent) may be persisted, and which directory string gets persisted.

**Wirings** — (1) MAIN CANONICAL GATE: src/permissions/permission-slash.ts:197-204 refuses `dispatchPermissionDirCommand({verb:"allow"})` when `validation.adjacencyWarnings.length > 0 && !cmd.acknowledgeWarnings`, returning `requiresAcknowledgement: true`. (2) MAIN, pick-root path: src/ipc/domains/workspace.ts:126-140 + :665-690 implements a main-owned one-time ack TOKEN round trip so a folder pick cannot silently widen the scope. (3) RENDERER, approval path: src/ui/renderer/components/permissions/OutOfAllowedDirCard.tsx:62-65 computes `retypeOk` / `adjacencyBlocking` / `persistEnabled` and disables allow-session (:185) and allow-always (:202) accordingly.

**Observed drift** — The approval path's main side unconditionally BYPASSES main's own gate: src/tools/invocation-runner.ts:825 and :862 both hardcode `acknowledgeWarnings: true` on the allow-always and allow-session persists. Nothing on the wire carries the acknowledgement — `ApprovalDecision` (src/permissions/approval-gate.ts:234-257) has only requestId/choice/rememberPattern/elicitationContent/nonce/hmac. So on this path the adjacency gate exists ONLY in the renderer checkbox, while the other two wirings enforce it in main. Same for the re-type confirmation: src/tools/invocation-runner.ts:818-822 takes `decision.rememberPattern` verbatim as the persisted `approvedDirectory` with no check that it equals `suggestedParent`, yet the main-process comment at src/tools/invocation-runner.ts:723-725 asserts "the user must re-type the suggested path before persisting (phishing defense)" as if it were a system property. Layer-0 (sensitive path / filesystem root) IS still re-validated in main via permission-slash.ts:195, so the gap is precisely the adjacency ack + the re-type binding.


### mcp-elicitation-schema-two-parsers

**Capability** — Which MCP `elicitation/create` requestedSchema shapes the host supports, and what a filled form may contain.

**Wirings** — (1) MAIN: src/mcp/mcp-elicitation-resolver.ts:37-38 (`MAX_ELICITATION_FIELDS = 12`, `ELICITATION_FIELD_NAME_RE`), :85-92 `parseEnumValues`, :93-129 `parseSupportedElicitationSchema`, :135-146 `isValidFieldValue`, :148-163 `validateElicitationContent` — decides accept vs decline at :207-215. (2) RENDERER: src/ui/renderer/components/ToolApprovalDialog.tsx:71-73 re-declares the SAME two constants as separate literals, :79-97 `scalarLabel`/`buildEnumOptions`, :107-159 `parseElicitationFields`, :208-240 `buildElicitationContent`. Load-bearing, not advisory: `isUnsupportedElicitationForm` (:436) feeds `approveDisabled` (:483-487), so the renderer's verdict decides whether the user can approve at all.

**Observed drift** — Concrete divergence on `enum` members. Main's `parseEnumValues` (:85-92) accepts any JSON scalar, and `isJsonScalar` accepts the empty string. The renderer's `buildEnumOptions` (:87-97) maps each member through `scalarLabel` (:79-85), which returns `""` for an empty-string member; that member is filtered out, `options.length !== raw.length`, and the function returns `undefined` — which at :139 makes the WHOLE schema `{supported:false}`. So a schema like `{properties:{choice:{enum:["","a"]}}}` is fully valid to main but renders as "Requested form schema is not supported." (:852-857) with the approve button disabled — the request can only be denied. The duplicated literals at :71-72 have the same failure mode for any future relaxation: raising main's field cap to 16 or widening the name regex would leave the renderer refusing to render, and every such request unapprovable.


### external-navigation-url-credential-rule

**Capability** — Whether a URL is safe to hand to an external-navigation sink (in-app browser tab, webview, shell.openExternal).

**Wirings** — Four. (1) RENDERER SoT: src/ui/renderer/preview/url-safety.ts:16-29 `normalizeBrowserNavigationUrl` — http(s) only AND rejects `url.username || url.password`. (2) MAIN webview policy: src/main/side-browser-webview.ts:7-15 `isHttpUrl` — http(s) AND credentials (matches). (3) MAIN `shell.openExternal` IPC: src/shared/external-url.ts:19-33 `validateExternalUrl` — protocol ONLY, no credential check; wired at src/ipc/domains/settings.ts:529-537. (4) MAIN plugin host API: src/boot/steps/plugin-runtime/external-url.ts:57-67 — protocol only, no credential check.

**Observed drift** — The credential rule is present in 2 of 4 wirings, and the renderer file's own doc comment (src/ui/renderer/preview/url-safety.ts:13-14) asserts "The main process independently re-validates on the IPC / webview boundary … with the same protocol + credential rules" — true of wiring (2), false of wiring (3), which is the sink a renderer actually reaches. An UNTRUSTED URL reaches wiring (3) without ever passing wiring (1): src/ui/renderer/components/McpAppView.tsx:486 binds the MCP-app bridge's open-link adapter as `openLink: (url) => getApi().openExternalUrl(url)`, and src/ui/renderer/components/mcp-app-bridge/handlers/on-open-link.ts:26-30 forwards the app-supplied string verbatim. That handler's own doc (:5-7) describes main as "scheme-validat[ing]" — i.e. it documents the narrower rule, so the two comments disagree about what main does. `https://trusted.example@evil.tld/` is refused by the renderer's validator and accepted by shell.openExternal.


### workspace-denial-code-three-renderer-tables

**Capability** — Turning a main-process IPC denial code into the message the user sees.

**Wirings** — (1) SHARED renderer table: src/ui/renderer/format-ipc-error.ts:8-250 `COMMON_IPC_ERROR_MESSAGES` + `resolveIpcErrorKey` (:275-280). (2) Local override table: src/ui/renderer/components/chat-side-panel-workspaces.tsx:371-378 `formatOpError` codeMap. (3) Local typed switch: src/ui/renderer/components/chat-side-panel-preview.tsx:209-223 `fileErrorKey`. (4) A consumer that maps nothing: src/ui/renderer/hooks/use-add-project-folder.ts:45 returns `null` on `!res.ok`, discarding the code.

**Observed drift** — Main emits `path-not-allowed` and `sensitive-path` from src/ipc/domains/workspace.ts:102-106 (`directoryDenyCode`), :189, :213 and src/ipc/domains/preview.ts:46-47. Neither code exists in the shared table at format-ipc-error.ts:8-250 (`invalid-path` is there at :85; the other two are not). Only wirings (2) and (3) know them. So a Layer-0 denial reaching any other surface falls through `formatIpcError` to `"${raw}${errorSuffix}"` (:298-299) and shows the raw kebab-case code — which is exactly the leak the comment at src/ipc/domains/workspace.ts:90-95 says these codes were introduced to prevent — and via wiring (4) the "Add project folder" flow shows nothing at all for a sensitive-path refusal. Advisory/display rather than gating, but the failure is user-facing.


### plugin-lookup-key-normalization-duplicate

**Capability** — Normalizing a plugin id / package spec / display name to the key used to match a plugin against a marketplace catalog item.

**Wirings** — (1) MAIN: src/plugins/marketplace.ts:119-129 `normalizePluginLookupKey`, consumed by `catalogItemMatchesPluginId` (:131-136) at :1193 to decide `stillManagedByCatalog`. (2) RENDERER: src/ui/renderer/tabs/PluginConfigTab.tsx:93-103 — a byte-identical copy of the same eight-step chain (`@scope/` strip, `@version` strip, `lvis-plugin-` strip, `plugin-` strip, non-alnum→dash, trim dashes), consumed by `findPluginDoctorMarketplaceItem` (:105-127) and `resolvePluginDoctorInstallKey` (:129-143), whose result is passed straight into a real install at :623-629 (`installMarketplacePlugin(installKey)`).

**Observed drift** — None observed — the two bodies are currently identical (verified by normalized-body hashing across the whole tree; they are the only non-trivial verbatim renderer/main function duplicate). Reporting it as the shape rather than as a live bug: it is load-bearing on the renderer side (the derived key selects which catalog package gets installed) with no shared module and no test binding the two, so a one-sided edit silently changes which package the Doctor repair installs.


### plugin-view-label-duplicate

**Capability** — The display label for a plugin UI extension.

**Wirings** — (1) src/ui/renderer/api-client.ts:14-16 `getPluginViewLabel` (main window renderer). (2) src/plugin-ui-host.tsx:99-101 — verbatim identical body (`displayName?.trim() || title || pluginId`) in the plugin-shell bundle.

**Observed drift** — None observed; both bodies identical. Purely cosmetic (a label), so low value — noted only because the same file already documents the corrective pattern at src/plugin-ui-host.tsx:103-106 ("Partition naming moved to `shared/plugin-partition.ts` so main + renderer stay byte-identical (#498)"), i.e. the codebase already fixed the sibling case and left this one.


### rationale-anchor-permission-context-dead-carrier

**Capability** — Carry the turn's request anchor + rationale eligibility provenance to the code that authorizes one tool invocation.

**Wirings** — (1) The `RationaleConversationRuntime`, built from `bounds.requestAnchor` + `currentRationaleProvenance` at query-loop.ts:961-981 and threaded as `rationaleRuntime` through `executeConversationBatch`, where it is the actual gate (`runtime?.requestAnchor` at executor-implementation.ts:402 and :441) and reaches authorization via `rationaleBatchContext`. (2) `permissionContext.requestAnchor` + `permissionContext.rationaleProvenance`, written at query-loop.ts:1214-1219 inside the `satisfies ConversationExecuteOptions` literal.

**Observed drift** — Carrier (2) is dead three independent ways. (a) Neither field is declared on `ToolPermissionContext` — executor-contract.ts:67-88 is the sole declaration (all importers alias it) and lists only headless/allowedPluginIds/approvalCacheKey/additionalDirectories/getAdditionalDirectories/trustOrigin/pluginPanelUserAction/pluginOperation/expectedMcpServerId/userIntent/onTurnDirectoryGrant/onSessionDirectoryGrant. It survives `satisfies` only because the props arrive through a conditional spread, which TypeScript exempts from excess-property checking. (b) No consumer reads them: every `requestAnchor` read under src/tools is off the rationale runtime/coordinator objects (executor-implementation.ts:402,441; rationale-host-coordinator.ts:332,359,505-534; rationale-host-service.ts:114-155), never off a permission context — whereas `permissionContext.userIntent` IS read at approval-purpose.ts:64 and reviewer-dispatch.ts:108,236, so this is not a category where nothing is read. (c) The two carriers compute provenance differently — query-loop.ts:1217 hardcodes `rationaleProvenanceFor(true, toolTrustOrigin)` while the live carrier at :961-963 passes `bounds.requestAnchor !== undefined`; they coincide only because the spread's own condition implies it. The fields do survive the `{...permissionContext}` rebuild at invocation-runner.ts:669-672 and are handed to `auditWriter.auditToolCall`, which reads only `context.trustOrigin` (audit-entries.ts:51-53), so nothing leaks — they are simply inert.


### usage-range-day-key-utc-vs-kst

**Capability** — Which calendar day a usage row belongs to, and what "today" / "N days ago" mean for the lvis:usage:range date filter

**Wirings** — MAIN AUTHORITY: src/engine/usage-stats.ts:153 (`const KST_OFFSET_MS = 9*60*60*1000`), :155-157 (`dateKey(d) = new Date(d.getTime()+KST_OFFSET_MS).toISOString().slice(0,10)`), :361-362 (`const d = dateKey(ts); return d >= opts.dateFrom && d <= opts.dateTo`). IPC entry: src/ipc/handlers/usage.ts:19-22 (`handleUsageRange`), src/ipc/domains/usage.ts:18. RENDERER WIRING A (drifted): src/ui/renderer/components/UsageDashboard.tsx:16-18 (`todayKey()` = `new Date().toISOString().slice(0,10)`), :20-24 (`daysAgoKey(n)` via `setUTCDate`), :26-32 (`presetToDates`), :108-118 (`api.getUsageRange(range)`). RENDERER WIRING B (correct): src/ui/renderer/components/StarredView.tsx:78-89 (`koreaDateKey` via `Intl.DateTimeFormat` timeZone `Asia/Seoul`), :195-196, :294 (`getUsageRange({ dateFrom: selectedKey, dateTo: selectedKey })`).

**Observed drift** — Real and provable. Main keys every trend point and every range-filter comparison on the KST civil date; UsageDashboard produces the UTC civil date; StarredView produces the Asia/Seoul civil date. Two renderer callers of the same IPC therefore ask main different questions. Concretely: at 2026-08-06 01:00 KST (= 2026-08-05 16:00 UTC), main's `dateKey` for a turn happening right now is `2026-08-06`, but `todayKey()` returns `2026-08-05`, so `presetToDates("7d")` sends `dateTo: "2026-08-05"` and the filter `d <= opts.dateTo` excludes every turn of the current KST day — the dashboard shows zero usage for today for a 9-hour window every day, and the 7d/30d/90d windows are shifted one day. StarredView is unaffected because it feeds main a KST key. Load-bearing: the string decides which rows main returns, not how they are painted.


### slash-load-session-lookup-two-wirings

**Capability** — Resolving a `/load <partial-session-id>` prefix to a session and switching to it

**Wirings** — RENDERER WIRING (the one that runs for typed input): src/ui/renderer/hooks/use-send-message.ts:166-183 — `if (trimmed === "/load" || trimmed.startsWith("/load "))`, `const listed = await api.chatSessions();` (line 172, NO options passed), `listed.sessions.find((session) => session.id.startsWith(requested))`, then `sessionLoad(match.id, ...)`. The IPC it lands on defaults the query: src/ipc/handlers/chat.ts:557-559 (`limit ... : 20`) and :567-569 (`kind ... : "main"`). MAIN WIRING: src/engine/turn/commands.ts:74-86 — `case "load"`, `const sessions = self.listSessions();` (no limit), `sessions.find((s) => s.id.startsWith(targetId))`, `self.loadSession(match.id)`; backed by src/engine/conversation-loop.ts:719-725 → `memoryManager.listSessions(undefined)`.

**Observed drift** — Actual, and the two sides answer differently on the same input. The renderer searches at most the 20 most recent `kind: "main"` sessions (the IPC's silent defaults); the main dispatcher searches ALL sessions with no kind filter. `/load abc123` for a session that is 21st-most-recent, or for a routine-kind session, resolves in main's implementation and returns `app.sessionNotFound` in the renderer's. The renderer copy is load-bearing: use-send-message.ts intercepts before `api.chatSend`, so for typed composer input main's `case "load"` never runs and its broader lookup is unreachable — it stays live only for the CLI / local-api entry points (src/cli/commands.ts, src/api/local-api.ts), which is exactly the split that lets the two drift unnoticed.


### cost-and-token-format-four-wirings

**Capability** — Rendering a token count / a USD cost as a human string

**Wirings** — src/lib/turn-summary-format.ts:52-57 `formatTokens` (guards `!Number.isFinite(n) || n <= 0` → "0"; M at 1 decimal) — NOTE: no non-test consumer; only its sibling `formatDuration` (:25) is imported, by src/ui/renderer/components/WorkGroup.tsx:4. src/ui/renderer/utils/cost-format.ts:3-7 `formatTokens` (M at 2 decimals, no finite guard) and :9-13 `formatCost` (3 tiers). src/ui/renderer/components/TokenCostBadge.tsx:63-67 `formatTokens` (M at 1 decimal, no finite guard) and :69-75 `formatCost` (5 tiers). src/ui/renderer/components/StarredView.tsx:100-102 `formatCost` (Intl currency, maximumFractionDigits 4). src/lib/cost-estimator.ts:80-84 `formatCostBadge` (`~$` prefix, 2 tiers), consumed by src/ui/renderer/components/TokenProgressRing.tsx:190.

**Observed drift** — Actual and observable. 2,500,000 tokens renders "2.50M" in the usage dashboard (cost-format.ts) and "2.5M" in the assistant card badge (TokenCostBadge.tsx). A cost of 0.5 renders "$0.50" (cost-format), "$0.500" (TokenCostBadge), "$0.5" (StarredView), "~$0.50" (formatCostBadge) — four spellings of one number in one app. NaN/negative renders as "NaN"/"-5" in both renderer `formatTokens` copies but "0" in lib/turn-summary-format.ts, i.e. the only copy carrying the guard is the dead one. Advisory (display), not gating — reported because the axis explicitly covers "how a name/path/size is formatted" and because the guard asymmetry is real drift, not aesthetic similarity.


### skipped-plugin-update-map-main-and-renderer-normalizers

**Capability** — Normalizing / reading / matching the persisted `settings.marketplace.skippedPluginUpdates` map (which plugin updates stay hidden)

**Wirings** — MAIN: src/boot/steps/post-boot.ts:234 (`RESERVED_SKIPPED_PLUGIN_UPDATE_KEYS = new Set(["__proto__","constructor","prototype"])`), :236-248 `readSkippedPluginUpdates`, :250-257 `isSkippedPluginUpdate`, :259-263 `normalizeSkippedPluginUpdateKey`. RENDERER: src/ui/renderer/hooks/use-marketplace-updates.ts:12 (`RESERVED_SKIPPED_UPDATE_KEYS` — a second literal Set with the same three members), :100-105 `normalizeSkippedPluginUpdates`, :107-114 `copySkippedPluginUpdates`, :116-129 `createSkippedPluginUpdateMap`/`putSkippedPluginUpdate`, :131-135 `normalizeSkippedPluginUpdateKey`, :137-145 `filterSkippedUpdates`.

**Observed drift** — No behavioural divergence today — I diffed both key normalizers, both reserved-key sets, both null-prototype map builders and both skip predicates and they agree on every input. This is a pure two-carrier duplication of the same persisted-map contract: the producer (renderer writes the map) and the consumer (main filters the update-check against it) each own a private copy of the reserved-key denylist and the trim/empty rules, with no shared module and no cross-side test. Adding a fourth reserved key or changing the trim rule on one side silently un-hides (or permanently hides) an update on the other. Reported as drift-prone-by-construction rather than drifted.


### dismissed-announcement-id-normalizer-duplicate

**Capability** — Normalizing the persisted `settings.marketplace.dismissedAnnouncementIds` list (which marketplace banners stay dismissed)

**Wirings** — MAIN: src/boot/steps/post-boot.ts:294-303 — `normalizeDismissedAnnouncementIds` (guards `!Array.isArray(ids)`, keeps `typeof id === "number" && Number.isSafeInteger(id)`, dedupes via Set, sorts ascending). RENDERER: src/ui/renderer/hooks/use-marketplace-announcements.ts:7-15 — same-named function with the same Set/`Number.isSafeInteger`/sort body, typed `Iterable<unknown>` with the Array.isArray guard hoisted to its caller (:55-57).

**Observed drift** — None today — the two bodies are line-for-line equivalent (post-boot.ts:296-302 vs use-marketplace-announcements.ts:8-14 are literally identical statements). Same shape as the skipped-update pair: the renderer writes the array, main filters every announcement push against it, and each side re-derives "what counts as a valid dismissed id" independently. Neither side bounds the list length, so if a cap is ever added it has to be added twice. Lower value than the skipped-update pair because the predicate is smaller and has no denylist.


### plugin-install-network-disclosure-predicate-shadow

**Capability** — "Does this plugin's networkAccess grant need a pre-install disclosure?" — one predicate, `hasNetworkAccessDisclosure` in src/shared/network-access.ts:55-61, which is `allowedDomains.length > 0 || !!reasoning || allowPrivateNetworks === true`.

**Wirings** — (1) Main deep-link consent surface: src/main/lvis-deep-link.ts:142 calls the shared predicate and src/main/lvis-deep-link.ts:154-156 prints the `allowPrivateNetworks` line. (2) Renderer routing: src/ui/renderer/tabs/MarketplaceTab.tsx:275 (`needsInstallDisclosure`) calls the SAME shared predicate to decide whether an install must go through the consent dialog. (3) Renderer dialog: src/ui/renderer/dialogs/PluginInstallDialog.tsx:30 re-declares a LOCAL `const hasNetworkAccessDisclosure = allowedDomains.length > 0 || !!reasoning;` and gates the whole disclosure panel on it at src/ui/renderer/dialogs/PluginInstallDialog.tsx:64.

**Observed drift** — The local shadow drops the `allowPrivateNetworks === true` term the shared predicate has. For a plugin whose manifest declares only `networkAccess: { allowPrivateNetworks: true }` (no domains, no reasoning): MarketplaceTab.tsx:275 says disclosure IS needed and routes to the dialog (MarketplaceTab.tsx:574-576); the dialog's shadow says false and renders no panel at all — so the private-network warning block at PluginInstallDialog.tsx:90-94 is unreachable in exactly the case it was written for. The user then clicks Install and MarketplaceTab.tsx:731 sends `networkAccessAcknowledged: true`, which becomes the `networkAccessAcknowledgement` main verifies at src/plugins/marketplace.ts:195. Net: the user acknowledges a private-network grant they were never shown, while the deep-link path for the same plugin does show it.


### policy-managed-editability-drops-admin-file-branch

**Capability** — "Is the explicit-approval policy user-editable?" Main's write authority is `adminFileExists || userFile.managed === true` (src/permissions/policy-store.ts:186-195: `if (adminFile !== null) throw "Policy is managed by IT (admin-dir file exists)"` FIRST, then `if (existing?.managed === true) throw`).

**Wirings** — (1) Main: src/permissions/policy-store.ts:178-196 `savePolicy`, surfaced as `{ok:false,error:"managed"}` at src/ipc/domains/permissions.ts:705-706. (2) Renderer: src/ui/renderer/tabs/PermissionsTab.tsx gates the checkbox on `policyManaged` alone — early-return at :350, `disabled={policyManaged || policyBusy}` at :746, lock glyph at :751, admin banner at :753.

**Observed drift** — The renderer's predicate has only the `managed` term; main has the admin-file term too. `readPolicyFile` (src/permissions/policy-store.ts:80-95) returns the admin JSON verbatim, and `loadPolicy` case-3 returns `{...adminFile, source:"admin"}` (:137) while case-4 only escalates `managed` when `adminFields.managed === true` (:153). So an admin policy.json that omits (or false-sets) `managed` yields `managed:false` with `source:"admin"|"merged"` — the renderer renders an ENABLED, unlocked, unbannered checkbox that main unconditionally refuses. The renderer already fetches `policySource` (PermissionsTab.tsx:103, used for the banner text at :755) so the missing branch is available and simply unused. Fail-closed in main, so this is a correctness/UX drift, not an escalation.


### plugin-config-schema-validation-only-wired-in-renderer

**Capability** — "Does a plugin config value satisfy the plugin's declared `manifest.configSchema` (type / required / minimum / maximum / minLength / maxLength / pattern / enum)?"

**Wirings** — (1) Main authority: src/plugins/config-schema.ts:78 `compileConfigSchemaValidator`, whose own docstring says it compiles the schema "into an AJV validator ... for runtime / IPC validation" and that "callers fall back to the existing sanitizePluginConfig path". (2) Renderer: src/ui/renderer/tabs/PluginConfigSchemaForm.tsx:37-61 `fieldError`, a hand-rolled re-implementation of minLength/maxLength/pattern/minimum/maximum.

**Observed drift** — `compileConfigSchemaValidator` has ZERO production callers — the only references in the repo are its own definition and src/plugins/__tests__/config-schema.test.ts:26,276. The main-side write path src/ipc/domains/plugins.ts:960-999 (`plugins.configSet`) only calls `stripSecretFields` + `settingsService.setPluginConfig`; the plugin-facing `hostApi.config.set` (src/boot/steps/plugin-runtime/host-api-factory.ts:392-397) only checks the `format:"secret"` rule. So the entire schema constraint set is enforced nowhere in main. Worse, the surviving renderer wiring does not gate either: `fieldError`'s result is bound to a local `error` at PluginConfigSchemaForm.tsx:236 and rendered as red text at :393, while every Save button's `disabled` is `!isCleartextDirty(key) || fieldSaving[key]` (:334-336, :364-366, :385-387) — never `error`. The renderer also never checks `enum` membership or `required`-on-save (`required` at :137 only drives the asterisk at :241). One authority, two carriers, both no-ops.


### context-fill-ring-omits-preflight-factor-and-overrides

**Capability** — "How full is the context, i.e. how close is this turn to the auto-compact / preflight boundary?"

**Wirings** — (1) Main: src/engine/auto-compact.ts:58-73 `getModelPreflightThreshold` = runtime slider override > `LVIS_DEV_PREFLIGHT_OVERRIDE` env > `min(getPreflightThreshold(window), floor(tpmDefault * 0.8))`, where `getPreflightThreshold` is `floor(usable * 0.8)` (src/shared/context-budget.ts:39-43). Consumed as the real trigger at src/engine/turn/compaction.ts:58 and :372. (2) Renderer: src/ui/renderer/hooks/use-context-budget.ts:56 sets the ring denominator to `getUsableContext(...)` with NO 0.8 factor, and :105-108 sets `effectiveBudget = min(usable, tpmLimit)` — again no 0.8.

**Observed drift** — Four branches main has that the renderer copy lacks: the 0.8 preflight factor, the `tpmDefault * 0.8` cap, the dev env override, and the runtime slider override written via src/ipc/domains/dev.ts:35. Observable consequence: auto-compact fires when the renderer's ring reads 80%, so the `>= 0.95` red banner at src/ui/renderer/ChatView.tsx:713 is effectively unreachable before a compact, and the hook's own docstring claim (use-context-budget.ts:38 / auto-compact.ts:48-49, "hit 100% at the compact threshold") is false by a factor of 1.25. Advisory (display + warning banners only), but the number it displays is a re-derivation of a main-process decision.


### tool-vs-turn-duration-two-rounding-rules

**Capability** — "Format a millisecond duration for the transcript." src/lib/turn-summary-format.ts:1-8 explicitly states the two formatters "stay in sync on rounding rules ... so the cumulative total never visually contradicts the per-tool slices it sums."

**Wirings** — (1) src/lib/turn-summary-format.ts:25-47 `formatDuration`, used by src/ui/renderer/components/WorkGroup.tsx:118 for the turn total. (2) src/ui/renderer/utils/format-duration.ts:13-20 `formatToolDuration`, used by src/ui/renderer/components/ToolGroupCard.tsx:27 and :58 for the per-tool badge.

**Observed drift** — They are already out of sync in three places. (a) Hours: `formatDuration` has an `ms >= 3_600_000` branch producing `1h 03m` (:45-47); `formatToolDuration` has none, so a 1-hour tool renders `60m 0.0s`. (b) Whole seconds: `formatDuration` drops the decimal when the fractional part is < 0.05 (`1m 12s`, :38-40); `formatToolDuration` always keeps it (`1m 12.0s`). (c) Zero/negative: `formatDuration` returns `"0s"` for `ms <= 0` (:26); `formatToolDuration` returns `""` for negatives and `"<0.1s"` for 0. Off the main/renderer axis strictly speaking (both consumers are renderer components), but it is the same one-rule-two-implementations shape with confirmed divergence against an explicit in-code sync claim.

---

# Implementation pass — landed, refuted, and deferred

A later pass took the survey as a list of LEADS and re-verified each candidate
against the deciding code before touching it, per the round-2 warning. Scope was
STRUCTURAL consolidations only: no user-observable behaviour change. Results
below; correct the entries above by reading this section alongside them.

## Landed

| candidate | single authority after | PR |
| --- | --- | --- |
| `plugin-lookup-key-normalization-duplicate` | `src/shared/plugin-lookup-key.ts` | #1873 |
| `private-namespace-predicate-open-coded-in-preload` | `classifySubscription`, `src/plugins/capabilities.ts` | #1874 |
| `dismissed-announcement-id-normalizer-duplicate` | `src/shared/marketplace-announcements.ts` | #1876 |
| `skipped-plugin-update-map-main-and-renderer-normalizers` | `src/shared/skipped-plugin-updates.ts` | #1878 |
| `plugin-view-label-duplicate` | `src/shared/plugin-view-label.ts` | #1882 |

Each survived re-verification with the survey's factual claims intact. Two
corrections to detail, neither affecting the verdict:

- `plugin-lookup-key-normalization-duplicate` — the two *matchers* around the
  normalizer are NOT mergeable and were left alone: main compares over
  `[id, slug, name, packageName, packageSpec]` of a `PluginMarketplaceItem`, the
  renderer over `[id, name, packageSpec]` of a `MarketplaceItem`, plus a
  literal-id fast path and a `pluginType` skip. Only the shared derivation moved.
- `skipped-plugin-update-map-...` — the survey reported the two normalizers as
  agreeing on every input, which holds, but there are two spelling differences it
  did not name: the non-object guard (`!input` on main vs `input === null` in the
  renderer) and the value-type check (`typeof v === "string" ? v.trim() : ""` vs
  `if (typeof v !== "string") continue`). Both pairs are equivalent on every
  input; the merged body keeps main's guard and the renderer's value check.

## Refuted

### `rationale-anchor-permission-context-dead-carrier` — REFUTED

The entry claims the carrier is "dead three independent ways", specifically
"(b) No consumer reads them". **(b) is false.**
`src/engine/turn/__tests__/rationale-host-wiring.test.ts` — a 10-test suite named
"RequestAnchor and rationale provenance host wiring" — uses
`permissionContext` as its sole observation point for that wiring:

- `:82-88` captures every `options.permissionContext` handed to
  `executeConversationBatch`;
- `:142` reads `permissionContexts[0].requestAnchor` and asserts the anchor's
  `inputOrigin`, DLP-sanitized `sanitizedIntent` and `rationaleRoundBudget`;
- `:194-196` and `:251-253` assert the NEGATIVE case — a `file-content`-origin
  turn must NOT carry `requestAnchor` or `rationaleProvenance`, i.e. the
  conditional spread itself is the thing under test.

Verified by deleting the two fields from the spread at query-loop.ts:1229-1234:
`Tests 4 failed | 41 passed` in that suite, against `Tests 10 passed (10)` on
`origin/main`. So the fields are inert in PRODUCTION but load-bearing as the
test's window onto anchor seeding and taint monotonicity. Removing them is not a
structural no-op — it requires re-pointing a security-wiring suite at the live
carrier (`prepareRationaleConversationRuntime` /
`executeRationaleAwareConversationBatch` args), which is a judgement call about
what that suite verifies, not a consolidation.

The entry's other claims DO hold and are worth keeping:
`ToolPermissionContext` (executor-contract.ts:68, the sole declaration) declares
neither field; the literal survives `satisfies ConversationExecuteOptions` only
because TypeScript exempts spread-in properties from excess-property checking;
and the two carriers compute provenance differently (query-loop.ts:1232
hardcodes `rationaleProvenanceFor(true, ...)` while the live carrier at :972-975
passes `bounds.requestAnchor !== undefined`).

I also attempted and FAILED to refute the "nothing forwards the whole object"
half: `AuditWriter.fireLifecycleEvent` (audit-writer.ts:69-96) takes the context
but passes only `context.trustOrigin` to the script-hook manager, and the
`{...permissionContext}` rebuild at invocation-runner.ts:727-730 is consumed
through named fields only. So the extra fields never reach a hook payload, an
audit row, or a cache key.

## Deferred as behaviour-changing — do NOT file these as structural

Each is a real duplicate whose consolidation would change what a user sees or
what is permitted, so it needs an owner decision, not a refactor:

- `max-verdict-composition` — verified: authority `risk-classifier.ts:84` is
  `LEVEL_RANK[b.level] >= LEVEL_RANK[a.level]` (ties prefer `b`, documented on
  the line above and pinned by a test); the private copy
  `rationale-pr1-contract.ts:110-113` is `rank[b.level] > rank[a.level]` (ties
  prefer `a`). Merging changes the sealed `effectiveVerdict`'s reason string on
  ties, and that string reaches the approval modal.
- `cost-and-token-format-four-wirings` — verified: `formatTokens` in
  `src/lib/turn-summary-format.ts:52` has NO non-test importer (the only
  importer of that module outside its own test is `WorkGroup.tsx:4`, which takes
  `formatDuration` alone), so the survey is right that the only copy carrying the
  `!Number.isFinite(n) || n <= 0` guard is the dead one. Unifying the live copies
  changes rendered strings (`2.50M` vs `2.5M`, `$0.50` vs `$0.500` vs `$0.5`).
- `tool-vs-turn-duration-two-rounding-rules` — same shape: consolidating changes
  the rendered badge text.
- `plugin-install-network-disclosure-predicate-shadow` — merging makes the
  private-network warning panel appear where it currently does not. That is the
  correct fix, and it is a behaviour change.
- `assistant-package-lifecycle-channels-templated` — found while closing the
  deep-link half of `install-result-broadcast-shape-five-declarations`. The
  plugin lifecycle channels now go through `CHANNELS.plugins.*` and one payload
  constructor (`buildInstallFailureResult`), but
  `src/main/lvis-deep-link.ts:171-182` `assistantPackageChannels` still builds
  the agents/skills equivalents by template — `lvis:${ns}:install-progress`,
  `-result`, `uninstall-result` — against `CHANNELS.agents.*` /
  `CHANNELS.skills.*` in `src/contract/app-contract.ts:212,221`. Those are
  addressable structurally. What makes the entry behaviour-changing is the
  payload half: agents and skills carry `agentId` / `skillId`
  (`src/ui/renderer/types.ts:1164-1167`, produced at
  `src/main/lvis-deep-link.ts:289,318`) and their failures still send
  `error: err.message` raw. Routing them through the plugin payload type is a
  field-addition sweep across three channel families, and adopting the stable
  code changes what the status-bar toast renders for every agent/skill install
  failure. NOT a quick win: the channel-constant part looks like a five-minute
  structural edit and the payload part is the actual entry.
- `webview-preferred-flow-seven-wirings`,
  `appearance-patch-skips-bundleid-and-schemaversion`,
  `system-settings-normalization-duplicated-load-vs-patch` — the
  *constant/value-set* half is extractable structurally, but each entry's actual
  finding is that the IPC patch path skips validation the load path applies.
  Extracting the constants without closing that gap moves the drift rather than
  removing it, and closing it rejects settings writes that currently persist.

# Status as of 2026-08-07 — supersedes the "Implementation pass" section above

That section stops at PR #1882 and lists five landed candidates. It is badly out
of date: roughly forty PRs from this survey have landed since. Read THIS section
for the current state of any entry; the ones above remain accurate about what
they describe, only incomplete about what has since been done.

## The finding that matters most for how you use this document

**Six entries were re-verified as ALREADY FIXED before anyone wrote code**, each
by a different worker, each naming the commit that closed it:

| entry | closed by |
| --- | --- |
| `install-result-broadcast-shape-five-declarations` | one `PluginInstallResultPayload` in `contract/app-contract.ts:794`; phantom `preparing?` gone |
| `delisted-admin-uninstall-bypasses-cleanup` | `e485b9ef` |
| `approval-gate-sandbox-capability-fallback` | `dbc39ab0` |
| `reviewer-containment-copy-inert` | `daefbe4c` + `c53bad1f` |
| `network-target-extraction` | `7d3e8a5a` — `src/permissions/reviewer/network-target.ts` |
| `host-secret-key-allowlist-three-authorities` | `1b142363` |

Plus one entry — `outgoing-turn-dlp-redaction` — that an earlier reviewer
REFUTED as "centralized, not leaky" and which turned out to be a **real PII
leak**: the refutation checked the main-chat replay lanes, all of which do route
through `ipc/domains/chat.ts:579`, and missed that side chat parses its own
payload and calls the loop directly. Fixed in #1930.

**So this document lies in both directions.** Treat every entry as a LEAD, not a
finding, and re-verify against the deciding code before writing anything. Grep
for the PRODUCER of every field involved — comments in this codebase actively
describe removed lanes as live, which has produced unreachable code more than
once.

## Landed since #1882

`ensure-install-ready-three-wirings` #1929 · `plugin-execution-gate-parity` #1928
· `subagent-prompt-scope-two-authorities` #1931 · `outgoing-turn-dlp-redaction`
#1930 · `workspace-root-lifecycle-wirings` #1933 · `approval-request-isreadonly`
#1932 · `message-token-cost-two-estimators` #1937 · `plugin-skill-turn-scope`
#1936 · `mcp-ui-payload-generation-id-enumerators` #1941 · `sensitive-path-two-denylists`
#1934 · `plugin-surface-directory-scope` #1942 · `model-discovery-policy-three-predicates`
#1943.

Not from the survey but found while working it, and worth knowing about:

- **#1938** — `plugin-artifact-store.test.ts` and `marketplace-installer.test.ts`
  were failing full-suite runs for the entire round. Not flake and not
  contention: the assertion passed and the **`finally` block** threw `ENOTEMPTY`.
  `makeTmpDir` roots scratch dirs at `process.cwd()`, and on Windows a scanner or
  the shell indexer holds a handle briefly after the last write. The tell was a
  failure in **69ms on a serialized run** — contention cannot produce that.
  `plugin-artifact-store.ts:71-80` already documents these codes and
  `retryOnTransientFsLock` retries them; the teardown was the one filesystem
  caller not using its own module's remedy. Full runs went to 983-986 files
  green, 0 failed, immediately after.

## Open

`host-secret-gate-two-implementations` (branch `agent/w7-secret-gate`) ·
`permission-config-broadcast-four-wirings` (#1935) · the `install-result`
deep-link residual (#1944) · `tool-pair-invariant-two-impls`,
`shell-structural-command-deny-two-wirings`, `plugin-shell-frame-predicate`,
`tool-result-image-wire-vendor-gate` (branches pushed, PRs pending).

Filed as issues rather than fixed: **#1939** (`hostSecrets.read[]` collection
bounds unenforced on the signed-whitelist path — the manifest path is covered by
AJV, the whitelist path has no schema leg at all, so the TS gate is the only one
and it is missing two of three bounds).

## Untriaged, deliberately not called flake

- `subagent-runner.test.ts > tags the persisted sub-agent session ... resume
  metadata` — failed once under load; the output was piped through `grep` and
  lost, so assertion-vs-teardown is unknown. Recorded as unknown rather than
  guessed from the test name.
- `replaceStagedFile` (`src/lib/atomic-file.ts:41-53`) retries `EPERM`/`EACCES`/
  `EBUSY` on win32 with **4 attempts and 60ms of total backoff**, against the
  sibling `retryOnTransientFsLock`'s 10 attempts / ~1750ms — roughly 29x apart,
  for the same Windows lock class, neither aware of the other. The sibling's own
  docstring says these locks "typically clear within a few hundred
  milliseconds", which 60ms does not cover. Observed once as a real production
  frame (`permission-settings-store.ts:570`, assertion never reached, `%TEMP%`
  not cwd — so neither #1938's class nor the cwd-staging contributor). One
  occurrence, no idle-machine reproduction: a design question, not a confirmed
  bug.
